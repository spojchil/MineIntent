# MineIntent 贡献规范

## 方向与证据

- [产品宪法](./PRODUCT_CONSTITUTION.md)是产品根目的和硬边界的唯一候选入口；草案必须由维护者公开确认准确版本并
  合入默认分支后才生效。
- 早期 commit、Issue 和 PR 是恢复原始愿景的重要证据，但不因最早、已合并或内容完整就自动成为当前决定。
- 产品负责建立真实且无预设玩家特权的选择条件，不替 AI 优化陪伴、独立、亲密、拒绝或其他具体社会结果。
- 产品目的、产品假设、技术路线、风险策略和验收代理必须分开记录。
- 代码、测试和绿色 CI 只能证明实现状态，不能代替产品确认。

涉及玩家身份、关系、注意、控制权、记忆语义或合法输入拒绝的 PR，正文必须列出：

1. 新增或改变了哪些产品假设。
2. 哪些准确文本已由维护者确认，来源在哪里。
3. 明确没有决定什么，以及什么证据会推翻当前方案。
4. 是否替 AI 预选了关系或行动结果；若没有，怎样证明启动者身份不会形成隐藏特权。

## 工作跟踪

非琐碎工作使用 GitHub Issue。未决设计留在 Issue 中，不复制成长期提案文档；实现完成后由 PR 和 commit
保存结论、取舍与验证。Milestone 只表示可验收增量，不用作永久想法列表。

## 分支与 Pull Request

- 从最新 `main` 创建短期分支，使用 `feat/`、`fix/`、`docs/`、`refactor/`、`test/` 或 `research/` 前缀。
- `main` 只通过合并 PR 变更。
- 一个 PR 解决一个清晰问题，关联 Issue，说明目标、方案、验证、产品假设和影响。
- 大型机械重构不得静默改变模型触发集合、输入主体、数据可见性或控制权。
- 合并后删除短期分支；历史由 Git 和 GitHub 保存。

## 验证

提交前至少运行：

```sh
corepack pnpm install --frozen-lockfile
corepack pnpm check
corepack pnpm check:docs
corepack pnpm test
python3 -m unittest discover -s agent-service -p 'test_*.py'
```

涉及 Minecraft 行为时，还应提供相应的服务器或真实模型证据，并明确哪些是实测、哪些是推算。
模型输出“成功”不能代替游戏状态验证。

## 文档

项目级活文档保持最小：产品宪法、运行指南、Paper 验证指南和来源索引。README 只放短的当前能力与入口。

- 不新增描述“系统现在怎样”的长期状态页；当前事实以代码、类型、测试和运行证据为准。
- 不复制 GitHub Issue、PR 或旧分支正文作仓库内档案；用不可变 commit/PR/Issue 链接索引。
- 稳定的设计理由写进 commit message 或紧贴代码的注释。
- 只有带条件、方法和结果的耐久实测发现，才值得另行保存；不要把推测写成发现。

## AI 辅助开发

允许使用 AI，但 AI 生成的产品前提不能自我批准。提交者应阅读实际 diff、运行相关验证，并确保：

- 未把生产密钥、私人聊天、玩家数据或世界文件发送给不应接收的服务。
- 未让测试夹具只包含支持当前假设的主体或角色。
- PR 描述不把“已合并”“测试通过”写成维护者接受产品方向的证据。
- 需要维护者判断的内容以简短产品语言单独呈现。

人工创建的 Issue、PR 和审查交流使用简体中文；代码标识、协议名和 Conventional Commit 类型可以使用英文。
