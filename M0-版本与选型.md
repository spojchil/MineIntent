# M0：版本与选型

> 状态：M0 已完成。协议、Paper、DataVersion、依赖提交、源码物料和 EULA 均有本地证据；M1 连接示例与 30 分钟保持验收也已完成，后续证据见 `进度日志.md`。
> 记录时间：2026-08-01（Asia/Shanghai）。

## 结论

- 网络目标钉为 **Minecraft Java 26.1.x，协议号 775**。
- M0 实测服务器为 **Paper 26.1.2 build 74**，离线模式，Java 25；状态端口返回 `version.protocol = 775`。
- 本版本的世界 DataVersion 为 **4790**。协议号和 DataVersion 是两个独立的版本轴：握手/状态使用 775，存档和 `/data` 解析使用 4790。
- Rust 客户端底座锁为 **azalea 0.16.0+mc26.1**，源码锚点为提交 `a253702e2546a95cce68dc4a3b8b00a4c3570c1f`。不使用当前 azalea 主线的 26.2 代码。
- 采用“**azalea 协议 + 客户端物理/世界层，自有薄适配层**”的分层方式：复用编解码、登录、压缩、keep-alive、客户端物理、区块/实体解析；自有适配层负责 MineIntent 事件语义、事实来源标记、快照、motor、观察和跨进程协议。
- 不直接使用 `ClientBuilder::new()` 作为后端入口。它装入的默认 bot 插件包含会改变产品语义的自动重生、自动重连、资源包自动接受等行为；后端必须显式禁用或包裹这些行为，并把服务器确认和客户端预测分开记录。

## 版本证据

### Paper 状态 ping

启动命令使用预置的 `supplies/tools/jdk-25/` Java 25 和 `supplies/paper/26.1.2/paper-26.1.2-74.jar`，工作目录为 `supplies/paper/26.1.2/first-run/`。服务端启动输出确认：

```text
Paper 26.1.2-74-ver/26.1.2@e4e17fc
Implementing API version 26.1.2.build.74-stable
Running Java 25 (OpenJDK 64-Bit Server VM 25.0.4+7-LTS)
Starting Minecraft server on *:25565
Done (14.212s)! For help, type "help"
```

随后用原始 Java 状态协议向 `127.0.0.1:25565` 发送 protocol 775 的 handshake 和 status request，得到：

```json
{"description":"A Minecraft Server","players":{"max":20,"online":0},"version":{"name":"Paper 26.1.2","protocol":775}}
```

本地物料和运行证据：

| 事实 | 证据 |
|---|---|
| Paper build 74、下载 URL、官方 SHA-256 | `supplies/paper/builds-26.1.2.json` 的 `id=74`、`channel=STABLE`、`downloads.server:default.checksums.sha256`；`supplies/SHA256SUMS` |
| 本地 Paper jar 与清单一致 | `paper-26.1.2-74.jar` SHA-256 为 `1d70b1dab9cf4a6de615209a536f3a45a2186240253c428213ce2188ab95e5f7` |
| 26.1.2 元数据和 Java 要求 | `supplies/mojang/26.1.2/26.1.2.json`：`id=26.1.2`、`type=release`、`javaVersion.majorVersion=25` |
| 运行日志 | 本机运行时输出保存在被忽略的 `supplies/paper/26.1.2/first-run/m0-server.stdout.log`；不作为发布物料提交 |

外部参考：

- [Mojang：Minecraft Java Edition 26.1](https://www.minecraft.net/en-us/article/minecraft-java-edition-26-1)
- [Paper：26.1](https://papermc.io/news/26-1/)
- [Paper 26.1.2 build 74 下载对象](https://fill-data.papermc.io/v1/objects/1d70b1dab9cf4a6de615209a536f3a45a2186240253c428213ce2188ab95e5f7/paper-26.1.2-74.jar)
- [Mojang 版本索引](https://piston-meta.mojang.com/mc/game/version_manifest_v2.json)

### DataVersion 与协议号

官方 26.1.2 server jar 的去混淆源码已落在 `supplies/decompiled/26.1.2-server/`，共 4789 个 `.java` 文件。关键锚点：

- `net/minecraft/DetectedVersion.java:28` 构造 `DataVersion(4790, "main")`。
- `net/minecraft/SharedConstants.java:23` 定义 `RELEASE_NETWORK_PROTOCOL_VERSION = 775`，`WORLD_VERSION = 4790` 在同文件前部。
- 对 Paper 首启世界的 `supplies/paper/26.1.2/first-run/world/level.dat` 做 gzip/NBT 读取，得到 `Data.DataVersion=4790`。

因此后续实现必须遵守：

1. 握手时只发送 775；不把 4790 填到协议字段。
2. 读存档、`/data` 或注册表同步数据时使用 DataVersion/运行时注册表，不把所有版本差异归因于协议号。
3. 发现带 snapshot 高位标志的协议号时拒绝连接；当前 775 是正式 release 值。

### 官方源码与 EULA

官方 26.1.2 `client.jar`、`server.jar` 和从 bundler 抽取的 `server-inner.jar` 均在 `supplies/mojang/26.1.2/`，校验记录在 `supplies/SHA256SUMS`。`server-inner.jar` 已用 `supplies/tools/vineflower-1.12.0.jar` 反编译到 `supplies/decompiled/26.1.2-server/`；源码中的类名、方法名和字段名可直接作为物理/协议语义 oracle。

EULA 存档为 `supplies/eula/EULA.html`，校验记录同样在 `supplies/SHA256SUMS`。本次只使用官方 jar 做本地阅读和行为对照，不把官方代码或 jar 放入本仓库的发布物。相关条款锚点：

- `EULA.html:300-303`：可以安装 Java 版服务端并主持在线游戏，但不得未经许可分发 Mojang 制作的内容。
- `EULA.html:305-309`：允许创建原创 Mods/tools/plugins；不得分发包含实质官方代码或内容的 Modded Version，第三方工具不得伪装成官方。
- `EULA.html:311`：原创内容归创建者，但官方代码、内容及其衍生物的权利仍归 Mojang/Microsoft。

外部参考：[Minecraft EULA](https://www.minecraft.net/en-us/eula)、[Removing obfuscation in Java Edition](https://www.minecraft.net/en-us/article/removing-obfuscation-in-java-edition)。

## azalea 选型与主动行为审计

### 版本锁定

`supplies/azalea` 的提交 `a253702e2546a95cce68dc4a3b8b00a4c3570c1f` 的提交标题为 `0.16.0`，其 `Cargo.toml` 的 workspace version 为 `0.16.0+mc26.1`，README 在该提交声明支持 `26.1`。当前克隆的 `main` 已更新到 26.2，不能把当前工作树 HEAD 当作本项目依赖版本。

- 本项目 `Cargo.toml` 以 `azalea = { version = "=0.16.0", ... }` 锁定该发布包；
  crates.io 包的版本元数据仍为 `0.16.0+mc26.1`，同时关闭会改变产品语义的默认插件，
  只启用所需的 `log` feature。
- [azalea 0.16.0+mc26.1 文档](https://docs.rs/crate/azalea/0.16.0%2Bmc26.1)
- [azalea 0.16.0 发布提交](https://github.com/azalea-rs/azalea/tree/a253702e2546a95cce68dc4a3b8b00a4c3570c1f)

### 默认插件审计

`azalea/src/builder.rs:46-49` 的 `ClientBuilder::new()` 装入 `azalea_client::DefaultPlugins` 和 `DefaultBotPlugins`。`azalea/src/bot.rs:203-214` 的 `DefaultBotPlugins` 进一步装入以下行为：

| 行为 | 本地源码锚点 | 结论 |
|---|---|---|
| keep-alive 回复 | `azalea-client/src/plugins/packet/game/mod.rs:924-937` | 收到服务端 keep-alive 后立即触发 `ServerboundKeepAlive`；这是协议必需行为，同时发出 `KeepAliveEvent` 可记录。 |
| 客户端物理预测 | `azalea-client/src/plugins/movement.rs:49-78, 300-430`；`azalea-physics` | `GameTick` 中自动计算速度、碰撞、跳跃、潜行/冲刺和位置；`send_position`/`send_player_input_packet` 自动发包。预测位置不能直接当作 `server_observed`。 |
| 服务端传送确认 | `azalea-client/src/plugins/packet/game/mod.rs:419-443` | 收到 `ClientboundPlayerPosition` 后应用相对修正，自动发送 `ServerboundAcceptTeleportation`，随后发送修正后位置。该行为必须进入事件/事实审计。 |
| 自动重生 | `azalea/src/auto_respawn.rs:9-33`；由 `bot.rs:209` 加入 | `DeathEvent` 后自动写入 `PerformRespawnEvent`，再发送 `ServerboundClientCommand(PerformRespawn)`。后端默认禁用，死亡与重生必须由后端策略显式记录。 |
| 自动重连 | `azalea/src/auto_reconnect.rs:20-113`；由 `bot.rs:213` 加入 | 默认插入 5 秒延迟，断线/初始连接失败后重新加入；`ClientBuilder::start` 默认会持续重试。后端默认禁用，按 MineIntent reconnect 状态机自行发事件。 |
| 资源包自动接受 | `azalea/src/accept_resource_packs.rs:9-62`；由 `bot.rs:210` 加入 | 收到资源包后主动发送 `Accepted` 和 `SuccessfullyLoaded`。这不是当前最小后端需要的隐式行为，默认禁用或改成显式策略。 |
| 乐观方块更新/回滚 | `azalea-client/src/plugins/interact/mod.rs:80-150`；`mining.rs:494-530`；`block_update.rs:34-48` | 挖掘完成时先修改本地世界并记录旧状态，服务端更新/ack 后再同步或回滚。该本地状态属于 `client_predicted`，不能作为世界快照事实。当前 M1/M2 不暴露挖掘工具。 |
| 聊天收发 | `azalea-client/src/plugins/client_chat/`、`azalea/src/client_impl/chat.rs` | 协议层解析收到的 player/system/disguised chat，`Client::chat` 发出聊天或命令包；适配层须保留发送者、时间和原文。 |
| 区块/实体/玩家列表 | `azalea-client/src/plugins/chunks.rs`、`packet/game/mod.rs`、`azalea_entity` | 客户端世界和实体组件由服务端包驱动更新；适配层仍须区分收到的服务端状态和本地物理预测。 |

这里的“默认禁用”不是删除 azalea 能力，而是使用 `DefaultPlugins`/`DefaultBotPlugins` 的可禁用 PluginGroup 组装方式，或从 `new_without_plugins()` 开始按白名单装入。M1 会先验证该组装方式，再实现后端自己的生命周期状态机。

## M0 验收状态与下一步

- [x] 协议号：Paper status ping 的 `version.protocol` 实测为 775。
- [x] Paper：26.1.2 build 74 在 Java 25 下完整首启并监听 25565。
- [x] DataVersion：源码常量和首启 `level.dat` 均为 4790。
- [x] azalea：锁定发布提交 `a253702e` / `0.16.0+mc26.1`，完成默认主动行为清单。
- [x] 官方源码：server jar 已校验、抽取、反编译，源码树可检索。
- [x] EULA：已阅读并记录与本项目相关的源码阅读、原创工具、分发边界。

M1 已进入实现：用显式禁用自动重生/自动重连/资源包接受的 azalea client 组装连接本地 Paper，后端事件流记录登录、keep-alive、断线和重连事实；不把 azalea 的默认高层行为直接映射为 MineIntent 语义。30 分钟干净世界证据与重连专项见 `进度日志.md`。
