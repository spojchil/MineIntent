# 中期更新 13（2026-08-03）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-12，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## NEW-11 裁定：A（验收侧按三档制第二档裁定）

**Participant 统一事件队列实现与 SEAM-10 同型的有限 admission**，不保留无界 channel，不采用饱和即 Faulted：

1. **分类降级**：可由快照/后续观察重建的普通事实在饱和时可丢弃，且必须在**丢失发生的流位置**形成 omission/overflow 事实——这是全系统同一条诚实纪律的第四处成员（AgentFrame omittedEvents / 视口 truncated / 事件流 overflow marker / 本条），沉默不得伪装成完整。
2. **不可丢类走有界背压**：addressed chat、scope/lifecycle control 不丢弃、不越序（journal gate 下后到聊天不得越过既有序），生产者短暂阻塞优于静默丢失。
3. **terminal 独立保留位**；stop cancellation 必须能唤醒所有等待者，停机有界。
4. **与 A7 同一套全局 ticket/FIFO 规则**——backend 与 Participant 的 admission 是一个模型的两段，不各造一套。
5. **容量初值由实现定、登记后可调**（同 directed 上限 16、chat 上限 8 的既定模式）；数值与依据写入进度日志。
6. **验收条件**：确定性饱和测试证明——journal/model 长期卡住时内存有上界、chat 不越序、stop 在期限内完成；A6 登记的 worker/dispatcher 环反例在 Participant 侧同样有具名回归关闭。

安全处理照台账：B6 其余生命周期/帧/身份修复不受阻；「端到端全队列有界」的宣称在本条验收条件全绿前不得作出。

## 台账

NEW-11 已裁定；后续新增待决从 NEW-12 起。维护者保留否决权（第二档），异议走下期更新翻案。
