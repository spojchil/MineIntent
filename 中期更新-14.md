# 中期更新 14（2026-08-03）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-13，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## NEW-12 裁定：1B + 2B + 3A（修订版：source 只取 cause，不做 direct 回退）（验收侧按三档制第二档裁定）

三个子问题都能从 oracle 与既有裁定唯一推出，核对锚点如下；与代理建议的唯一差异在 3A 的 source 规则。

### 1. Updated：B（迁移等价）

对可建立**逐包 post-state** 的 `entityUpdate` 等价来源发 `Updated`，`changed` 沿 oracle 恒为 `[]`（minecraft-backend.ts:447-448）；不得用批次处理后的最终 ECS 猜 post-state（A7 Spawn/Move 反例既有）。`changed` allowlist（选项 A）属移植后增强，不夹带。

### 2. Animation：B（继续不生产）

TS 声明了 variant（contracts.ts:157）但从未注册生产监听器（minecraft-backend.ts:356-358 只有 entityUpdate/entityHurt）。等价迁移 = variant 保留、不发布。六个 `AnimationAction` 的字符串冻结随未来引入时再铸版本。

### 3. Hurt：canonical = `ClientboundDamageEvent`，source **只取 cause entity**

oracle 的上游行为已查实：mineflayer entities.js:377-380（1.20+ 路径）——`entityHurt` 由 `damage_event` 包驱动，source = `bot.entities[packet.sourceCauseId - 1]`，**只用 sourceCauseId**，查不到实体即缺席；没有 direct 回退。因此：

- 只以 `ClientboundDamageEvent` 为 canonical Hurt；`HurtAnimation` 与 `Animate::Hurt` 视为视觉/动画事实，不重复产 Hurt（canonical 唯一化从构造上排除双发）。
- `possibleSourceEntityKey` **只在 cause entity 存在且该 id 在同 epoch entity shadow 中时写入**（等价 oracle 的 `bot.entities` 查找门，minecraft-backend.ts:454-462 的 `#isEntityLike(source)` 同义）。
- **cause 缺席时不用 direct entity 回退**——代理建议的回退是超出 oracle 的信息增强（会在 oracle 缺席处出现值），按等价纪律剔除；登记为移植后可选增强，届时随版本另议。
- `entity_status` 1/2 的旧版路径（entities.js:16,24）为 pre-1.20 遗留，26.1 目标下不在范围。

### 4. 安全边界照台账

A7 真实 schedule 继续只发布已有逐包权威证据的 Spawned/Moved/Removed；本裁定解除 Updated/Hurt 的接线阻塞，Animation 维持不接线。block、sound、light、armor、transport 不受影响。

## 台账

NEW-12 已裁定；后续新增待决从 NEW-13 起。维护者保留否决权（第二档），异议走下期更新翻案。
