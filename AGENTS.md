# AGENTS.md

本文件是 AI 编码助手的**唯一真相源**。`CLAUDE.md` 内容为 `@AGENTS.md`，不再单独维护。

本文件描述工作规则，不具有产品权威——产品判断只在[产品正文](./docs/产品.md)里。

## 开始工作

1. 阅读[产品正文](./docs/产品.md)，只把其中明确状态的准确文字作为产品判断。
2. 阅读[文档入口](./docs/README.md)，按任务进入运行、验证、架构或历史材料。
3. 阅读目标代码、类型和测试；对当前行为的判断以产生方和可重复运行证据为准。
4. 用 `git status` 检查工作区，保留与任务无关的现有改动。

## 产品与证据

- 不根据代码存在、测试通过、内容已合并或多个文档互相引用，自行补出产品决定。
- 引用产品判断时使用 `产品.md` 的条目编号或准确的提案 Issue，不在其他文档重新改写一套产品定义。
- 产品文字不清楚、来源状态不明或判断彼此冲突时，向维护者询问。
- 陈述仓库事实时给出文件、符号、commit、Issue、PR 或运行结果锚点；产生方容易读取时优先核对产生方。

## 修改代码和文档

- 提新模块、工具或抽象前先用 `rg` 搜索已有实现。
- 改动保持在用户授权范围内，不顺手重构无关代码，不覆盖其他人的工作。
- 当前实现结构变化时更新 `docs/architecture.md` 的版本绑定和对应说明。
- 路径变化时更新 `docs/history/index.md` 的迁移表；不要复制历史正文冒充现行说明。
- 不提交或回显密钥、令牌、私人聊天、世界存档、未脱敏日志和本地运行数据。

## 验证与交付

- 按[验证指南](./docs/guides/validation.md)运行与改动相符的最小充分检查。
- 区分自动化检查、真实运行证据和推断；不要用绿色测试声称它没有覆盖的行为已经得到验证。
- 交付时说明改了什么、验证了什么、哪些问题仍未决定。
- `main` 只通过 Pull Request 变更；发布流程见[贡献流程](./CONTRIBUTING.md)。

## 个人本地规则

仓库根目录存在 `AGENTS.local.md` 时，会话开始读取一次，其内容**扩展**本文件的团队规则，不覆盖。该文件由 `.gitignore` 的 `*.local.*` 罩住，不入库。
