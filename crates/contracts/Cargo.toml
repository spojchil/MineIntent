[package]
name = "mineintent-contracts"
version = "0.0.1"
edition = "2021"
description = "MineIntent Agent、capability、Information 与 Minecraft backend 进程内契约"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
