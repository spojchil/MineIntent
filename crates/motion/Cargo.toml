[package]
name = "motion"
description = "运动：位移与朝向的意图工具，经模块一写口执行，机械终止在世界侧"
version = "0.0.1"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
agent = { path = "../agent" }
dispatch = { path = "../dispatch" }
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }

[lints]
workspace = true
