[package]
name = "screens"
description = "界面互斥域：单槽屏位与聊天框工具"
version = "0.0.1"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
agent = { workspace = true }
dispatch = { path = "../dispatch" }
parking_lot = { workspace = true }
render = { path = "../render" }
serde_json = "1"
world = { path = "../world" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }

[lints]
workspace = true
