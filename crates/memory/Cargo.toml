[package]
name = "memory"
description = "记忆：单文件长期记忆的文件语义与整文改写工具（Free 类）"
version = "0.0.1"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
agent = { path = "../agent" }
dispatch = { path = "../dispatch" }
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }

[lints]
workspace = true
