# A6 进度日志

## 实现边界

- 本批只改 `crates/backend/**`，新增本日志；`.codex-tmp/progress-a6.jsonl` 按要求记录阶段进度。未改 contracts、middle、根 manifest/lock、supplies、移植计划、总进度或其它进度日志。
- 新增公开 `MinecraftBackendFacade`，实现冻结的 `MinecraftBackendApi`、`MinecraftMotorDriverApi` 和全局 backend `Subscription` 入口。facade 的生产 `new` 只验证/规范化配置并创建 dispatcher；真正的 start 在内部专属 OS 线程中创建 current-thread Tokio runtime、`LocalSet`，调用现有 `run_with_handle(RuntimeHandle, RunConfig)`。
- 诊断 `runtime::run`、`main.rs` 的 stdin/stdout 入口保留。新增 `RunConfig::emit_stdout`：诊断默认开启，生产 facade 映射为关闭；生产事件只从 RuntimeHandle 的订阅进入 facade dispatcher。`auto_stop` 同样在生产 facade 关闭，生命周期由 composition root/Facade API 管理。
- cfg(test) seam 只驱动真实 RuntimeHandle 的生命周期 admission、event envelope、command completion 和 stop 路径，不连接网络、不启动 Paper；生产路径没有 fake runtime 或第二套生产 lifecycle 状态机。

## Oracle 与冻结契约映射

- `MinecraftBackendApi::start`：admission 期间只接受一个 owned session；真实 runtime worker 驱动当前 attempt。`wait_for_start` 只在该 session 的真实 ready、terminal fault/close、Stopped-before-ready 或调用控制（cancel/deadline）时完成；旧 session/旧 ready 不回放，重复/并发 start 以现有 `InvalidCommand { field: "lifecycle" }` fail closed。
- `MinecraftBackendApi::stop`：使用现有 `RuntimeHandle::stop`/`initiate_stop`，等待 runtime 权威状态 `Stopped`、worker 完成并 join；重复 stop 复用同一 session/stop admission，保持幂等。idle stop 只构造 handle、订阅并走 concrete stop admission，不调用 `run_with_handle`，epoch 保持 0、没有连接 attempt 或 runtime worker。
- `state`：直接读取 `RuntimeHandle::state()` 的 `BackendState`，初始无 session 精确为 `Idle`。Connecting/LoggingIn/Spawning/Ready/Dead/Reconnecting/Stopping/Stopped/Faulted 均由 runtime admission 写入，facade 不从回调猜测字段。
- `snapshot`：只取当前 ready session 的真实 runtime snapshot，并做合同 DTO 转换；not-ready、snapshot 缺失和 stale 均返回现有 `BackendError`，不返回默认快照。
- `observation_source`：绑定当前真实 runtime observation source 和 connection epoch；每个读/subscribe/异步 viewport 操作前后重新核对 session、epoch、ready 与 snapshot，旧 source 返回 `StaleEpoch`/现有 not-ready 错误。
- `motor`：绑定当前 session/epoch，look/move 委托真实 runtime command queue 并等待 `CommandCompletion`；release_all 同步等待真实 completion，重复释放幂等且 generation-safe。取消/deadline 先取消在途 completion，再等待物理释放 settlement。
- `send_chat`：校验单行非空输入后委托当前真实 RuntimeHandle；runtime command/terminal 错误映射到已有 `BackendError`，不新增 wire/kind。
- `subscribe`：所有 backend event 进入一个容量为 256 的全局 SyncSender FIFO dispatcher；每个 callback 在 registry 锁外同步执行，panic 由 `catch_unwind` 隔离。unsubscribe/Drop 先在 registry 线性化关闭、移除并阻止 pending delivery，再对已经开始的 callback 有界等待；callback 可重入 state/subscribe/stop/unsubscribe 自己或其它订阅，不持有 registry 锁等待。
- strict v2 event 集合没有新增 producer；entity/block/sound 真实 producer、SEAM-10 bounded/overflow、light、Participant/app 保持 deferred。

## 管理侧 correctness 与竞态线性化

### CommandCompletion settlement

`CommandCompletionState::finish` 使用 `finish_lock` 取得唯一 finishing ownership，先写 `settled_result`，清理 `active_release`，取出并发送 oneshot，最后以 Release store 发布 `settled` 并通知 condvar/Notify。`wait_settled` 只在最后发布位可见后返回，`release_all` 的 blocking wait 读取同一 settled result；因此取消调用方不会早于物理 release bookkeeping、结果发布返回。具名 runtime 测试为 `command_completion_settled_signal_waits_for_release_and_result_publication`。

### lifecycle 时间与状态事实

Ready 和两条 Died admission 路径各自只调用一次 `now_utc().to_rfc3339()`，同一字符串同时写入 `BackendState::{Ready,Dead}` 与 `emit_at` 的 strict v2 envelope `occurred_at`。具名测试 `lifecycle_state_timestamps_match_the_strict_v2_lifecycle_facts` 逐项对账；facade 只读取 runtime state。

### start/stop control 优先级

`wait_for_start` 和 command waiter 在进入及每次唤醒前做 preflight；select 使用 biased 顺序 cancellation > deadline > runtime completion/notify。start 的 preflight cancel/deadline 也会向已 admission 的 session 发起既有 stop，避免 immediate control 留下 owned worker。stop 自身已先 admission stop，控制错误不会重新启动或伪造终态。Stopped/ready 与控制同 poll 时不会覆盖已经可见的控制。

### Stopped-before-ready 与 terminal mapping

dispatcher `observe_event(Stopped)` 在尚未有 ready/terminal result 时写入稳定 `BackendError::NotReady { state: "stopped" }` 并 notify pending start；若 ready 已成功，Stopped 只设置 stopped 标志，不改写 ready result。实际 `Faulted { failure }` 和不可重试 `ConnectionClosed { close }` 分别保留原始 `BackendFailure`/`BackendClosed { close }`；`session_error_for` 在 Stopped 时优先返回已有 SessionOutcome terminal，只有不存在实际 error fact 时才返回稳定 not-ready 状态，不合成 close 时间/code。

### dispatcher callback → stop 重入

停止完成条件不再依赖同一个 dispatcher 消费 Stopped envelope：`RuntimeSession::stop_resources_released` 直接读取真实 `RuntimeHandle::state() == Stopped` 并要求 `worker_done`，随后 `join_worker`。Stopped envelope 仍留在有界全局 FIFO，listener 返回后由 dispatcher 按顺序处理和交付。具名 multi-thread 测试 `facade_callback_sync_stop_waits_on_runtime_state_without_dispatch_deadlock` 在 callback 内用本地 runtime `block_on(facade.stop(...))`，1 秒内拿到 Ok，检查最终 Stopped、worker/join 状态，并验证 recording listener 随后收到 `event-2` Stopped；没有靠生产 timeout 或 `UNSUBSCRIBE_WAIT` 解死锁。

## 测试与 mutation 证据

具名 facade 测试覆盖：

- `facade_start_ready_and_initial_state_are_runtime_owned`
- `facade_terminal_failure_maps_without_new_error_kind`
- `facade_control_cancellation_wins_simultaneous_deadline`
- `facade_start_deadline_is_explicit_and_stoppable`
- `facade_stop_is_idempotent_and_final_stopped_is_visible`
- `facade_stopped_before_ready_closes_pending_start_without_fake_close`
- `facade_idle_stop_publishes_stopped_without_connection_or_worker`
- `facade_concurrent_start_is_rejected_without_old_ready_replay`
- `facade_snapshot_and_source_reject_not_ready_and_stale_use`
- `facade_subscription_fifo_panic_unsubscribe_and_reentry_are_bounded`
- `facade_unsubscribe_linearizes_before_waiting_for_active_callback`
- `facade_callback_sync_stop_waits_on_runtime_state_without_dispatch_deadlock`
- `facade_motor_completion_cancellation_and_release_are_generation_safe`
- `facade_production_config_keeps_runtime_events_in_process`

最终准确命令结果：

- `cargo test -p mineintent-backend facade_ --lib --locked --offline -- --nocapture`：14 passed，0 failed。
- `cargo test -p mineintent-backend --lib --locked --offline`：102 passed，0 failed，0 ignored。
- `cargo test --workspace --all-targets --locked --offline`：exit 0；backend 102/102，contracts/middle 所有 target suites 均通过。
- `cargo check --workspace --all-targets --locked --offline`：exit 0，完成 dev profile check，无 warning。
- `cargo fmt --all -- --check`：exit 0。
- `cargo +stable fmt --all -- --check`：exit 0。
- `git diff --check`：exit 0；仅 Git 报现有 LF/CRLF normalization warning，无 diff error。
- 之前一次 120 秒工具上限导致 workspace test exit 124、未产生失败断言；同一命令在 600 秒有界上限重跑 exit 0，未把超时当作绿灯。

真实 mutation：临时把 `observe_event(Stopped)` 的稳定映射从 `state: "stopped"` 改成 `state: "idle"`，运行 `facade_stopped_before_ready_closes_pending_start_without_fake_close`，按预期红灯：`left: NotReady { state: "idle" }`、`right: NotReady { state: "stopped" }`。随后恢复 production code，该具名测试再次 exit 0；工作树不保留 mutation。

## 进程、边界与风险

- 最终 PowerShell 进程审计无 `cargo`、`rustc`、backend test/runtime 残留；未启动在线网络、Paper、Codex、Claude 或其它子代理。
- 未做真实 Minecraft 网络连接级集成测试；离线测试通过最小内部 seam 覆盖协调、真实 runtime handle admission 和 resource ownership，生产路径仍实际调用 Azalea runtime。
- stop 可在 runtime 权威 Stopped 且 worker 已 join 时返回；此刻 Stopped envelope 可能仍在全局 FIFO 中，随后继续按严格 FIFO 交付，这是为 callback→stop 重入而保留的明确边界，不是终态猜测或重复事件状态机。
- 生产 dispatcher 使用单一有界队列；不会用每订阅永久无界队列规避所有权。队列满时上游 event admission 会受背压，这是已选 bounded resource boundary，未新增 overflow wire 语义。

## 明确 deferred / 禁止越界

- 本批不实现真实 entity/block/sound producer、SEAM-10 bounded/overflow 语义、light、Participant、app composition root；不改变事件集合。
- 不修改 `crates/contracts/**`、`crates/middle/**`、根 manifest/lock、`supplies/**`、`移植计划/**`、总进度或其它进度日志；不漂移冻结 trait、wire/kind。
- 不 merge/rebase/cherry-pick/push，不改 main。若后续发现冻结公共契约不足，按要求只写 `.codex-tmp/A6-需要决策.md` 并停止，不自行扩展公共契约。
