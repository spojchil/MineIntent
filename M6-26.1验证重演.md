# M6：26.1 生命周期与对称性重演

目标服务器为 Paper 26.1.2 build 74，协议号 775，离线模式，临时监听端口为 `25567`。所有现场运行产物均在被 Git 排除的 `server-run/` 下；探针复用仓库公开的 `RuntimeHandle`、事件信封和快照协议，不改变主 MineIntent 产品仓库。

## 通过条件

每个场景都以服务端观察到的协议边界为准：

- 死亡/重生：`died` 后由调用方显式调用 `respawn()`，必须看到 `respawn_packet_dispatched`、新的 `ready`/快照和 `respawned`；不允许用自动重生掩盖失败。
- 维度切换：必须看到 Overworld → Nether → Overworld 的两个 `dimension_changed`，并且返回后的快照维度正确。
- 断线重连：必须看到 `connection_closed`、`reconnect_scheduled`、`reconnect_requested`，随后新连接 epoch 的 `ready` 和新快照；不得复用旧世界观察缓存。
- R01 对称性：两名不同用户名交换两个目标位置后，去除用户名、实体键和事件编号，归一化的水平坐标集合、维度和 alive 状态必须相同。

## 实测结果

| 场景 | 结果 | 证据 |
| --- | --- | --- |
| 死亡/重生 | 通过；调用方立即请求，运行时 100ms settle 后发 `PERFORM_RESPAWN`，最终 `respawned` 且 `alive=true` | `server-run/m6-respawn/m6-respawn-formal.log` |
| 维度往返 | 通过；`minecraft:overworld` → `minecraft:the_nether` → `minecraft:overworld` | `server-run/m6-probe/m6-dimension-v4.log` |
| 断线重连 | 通过；新 epoch 再次 `ready`，且无 panic | `server-run/m6-probe/m6-dimension-v4.log`、`server-run/m4-rcon/java-m5.stdout.log` |
| 双人对称性 | 通过；两轮归一化结果相同：`[(0.5,-7.5),(4.5,-7.5)]`，两人均 alive | `server-run/m6-symmetry/m6-symmetry-final.log` |

## 运行时边界

`respawn()` 是显式命令；死亡事件不会自动调用它。100ms 只是服务端死亡状态落地的协议时序缓冲，不是自动重生策略。自动重连仍按 `RunConfig::reconnect_delay` 控制，资源包接受保持关闭。未完成的产品选择继续留在主仓库的 Q04/Q11 裁决项中。

## 复跑入口

当前现场探针是临时验证代码，位于 `server-run/m6-respawn/`、`server-run/m6-probe/` 和 `server-run/m6-symmetry/`，它们被 `.gitignore` 排除；正式提交只保留 Rust 运行时、协议文档和本报告，不把特定用户名、世界状态或临时日志当作产品逻辑。
