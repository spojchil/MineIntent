# 中期更新 08（2026-08-03）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-07，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## 帧构成裁定（维护者 2026-08-03 批，第三档）——被动供给面总定稿

判据（维护者确立，standingOnBlock 裁决的推广）：**不打开界面就能看到的才进帧**。帧 = 被动推送；主动读取（view、未来的关注/历史工具）与之并存不冲突。

### 1. 帧段清单（新构成）

| 段 | 内容 | 备注 |
|---|---|---|
| chat | 未读聊天**内容**（含本次触发消息），上限初值 8 条，超出以 omitted 计数诚实标注 | 主动的关注/历史工具（W08a/W08b）属移植后功能包，与被动帧不冲突 |
| sound | 声音观察 | 现行不变 |
| viewport | **全量视口（暂定 C0）** | 即 B3 已实现形态，零返工；C1 摘要帧列为移植后实验项 |
| pose | 坐标、朝向 | 现行不变（F3 注册处） |
| status | 血量、饥饿（常显）；**盔甲值**（为 0 省略——对应原版盔甲条 0 时本就隐藏，与 UI 行为逐字对应） | 盔甲韧性不进帧（见 §4 待核）；0 省略仅限「原版 UI 本身也隐藏」的项，不推广为通用默认省略（更新-06 §3 不省略裁定不变） |
| effects | 药水效果（反胃/饥饿/凋零等 HUD 可见项） | **留槽不实现**，后续登记 |
| hotbar | 9 格**全量** + 当前选中栏位 + 主手物品 + 副手物品 | 新增 |
| world | dimension 保留 | timeOfDay **移出帧**留槽（洞内不可见，判据不过） |
| light | 自身位置光照值 | **新增段**（F3 可见，"洞里黑"的诚实信号）；backend 需暴露自身位置光照读取——A2 面新增小缺口，随 participant runtime 阶段排期 |

### 2. 移出帧的项（连带豁免名单增补）

| 旧结构 | 裁定 | 直达形态 |
|---|---|---|
| `standingOnBlock`（ViewportValues） | 维护者 2026-08-02 裁：**整体删除**（视觉通道白拿；诚实替代 = directed 查脚下坐标） | ViewportValues 五段变四段；legend/prompt 同步；TS 相关测试登记映射 |
| `inventory` 全量（AgentFrame） | 帧只保快捷栏。**打开物品栏是未来的 GUI 能力面**：阻塞/停止动作的界面语义、装备与快捷栏物品交互、2×2 合成、配方书、药水剩余时间显示——整体归能力阶梯，移植后逐项裁定 | AgentFrame inventory 段替换为 hotbar 段 |
| `world.timeOfDay` | 移出帧，留槽待议 | world 段仅 dimension |
| events | **不是帧常驻段**：轮内中断事实走 `observationAfter`（NEW-07 2A 已裁）；跨 run 积压汇入开场帧 `events[]` 维持现行；omissions 纪律不变 | 无形状变化，仅定位澄清 |

### 3. 实现形态：帧段注册表

- 帧组装实现为**注册式**：每段一个注册项（键、schema、采样器、显隐条件如"为 0 省略"），增删字段 = 注册项变更。information runtime 的 provider/registry 架构是现成地基，不另造。
- 注册表只降低**代码**改动成本，不豁免契约纪律：模型可见构成每次变化仍须版本铸新（SEAM-07 不同名漂移）+ prompt 制品同步 + 登记。本裁定落地时 context/frame 版本由实现侧铸新并登记（预计 agent-context v5 或帧段独立版本号，实现定夺后写入台账）。

### 4. 待核事实

- **盔甲韧性是否在原版 HUD 显示**：默认按「仅物品 tooltip 可见」处理（不进帧）。可用 supplies 去混淆客户端核查 HUD/Gui 渲染代码裁定；若查实 HUD 显示，韧性并入 status 段并登记。

### 5. 施工顺序

participant runtime 与装配根按本裁定的帧构成**直达实现**（帧构成部分不移植 oracle 旧形态；runtime 其余行为仍等价迁移）。C1 摘要帧、effects 段、光照段实现、韧性核查按登记顺序排在其后，不阻塞 runtime 开工。
