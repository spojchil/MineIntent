# C5 进度日志

## 合同映射

- `mineintent.agent-context.v5`：在 contracts 新增严格 discriminator、stable `{memory}` 复用 v4、`world/pose/status/hotbar/chat/sound/light/events/omissions` typed DTO；禁止旧 `player/self/viewport/effects/inventory/timeOfDay`。v3/v4 类型与 fixture 未改写。
- hotbar 使用稀疏字符串槽位 `"0"` 到 `"8"`，tuple count 只要求正 `u32`；没有新增 64 上限。`65` 与 `u32::MAX` round-trip，零、小数、负数拒绝；armor=0 省略，非零为原版 `1..=20`。
- chat 使用内部稳定 sequence 组装，wire 不泄漏 sequence；窗口保留最新八条并诚实计算 `omitted`，普通条目为 `text`，移动条目为 `moved:events`，player_chat 全文事件恰一次，普通事件保持 `{type,summary}`。
- sound/omissions 直接复用 Information 的 `SoundValues`/`SoundObservation`/`InformationOmission`。v5 边界拒绝显式 null、空 recentSounds/omissions、nested unknown、缺 sound 必填字段及非法 omission 字段/枚举；fixture 使用完整 SoundObservation，未伪造 effects omission。
- `mineintent.viewport.v2` full/directed DTO 与唯一 kernel→model presenter 已建立；full 删除 `standingOnBlock`，保留必需且可为 null 的 `lookedAtBlock` 键，directed 的总量、重复坐标、`why` 五值纪律保留在 serde 与 presenter 边界。
- `mineintent.viewport-frame.v2` 成功严格持有完整 typed `ViewportFullV2`，失败保持 `viewport:null + unavailable`；旧 viewport-frame.v1 fixture/type 只保留回放兼容。tool-result 外壳仍为 v1。
- production `view` capability、`BackendRoundViewportSampler` 与 round driver 共用 v2 presenter/anchor；未改 Information provider/kernel wire，也未装配 Participant、wake registry、backend facade、真实 light producer 或 app wiring。

## 测试证据

- `cargo test -p mineintent-contracts --test agent_context_v5 --test viewport_v2 --locked --offline`：v5 5/5，viewport v2 3/3。
- `cargo test -p mineintent-middle --test agent_context_v5 --test view_capability --test agent_round_frame --locked --offline`：assembler 5/5，view 11/11，round frame 16/16。
- workspace 全量：`cargo test --workspace --all-targets --locked --offline` 通过；覆盖既有 backend、contracts、middle 测试及 v3/v4、v1 replay。
- `cargo check --workspace --all-targets --locked --offline` 通过。
- `cargo fmt --all -- --check`、`cargo +stable fmt --all -- --check` 通过；`git diff --check` 退出 0。Git 仅报告工作树 LF/CRLF 转换提示，无 diff whitespace 错误。
- 管理进程收紧 `lookedAtBlock` 必需键后，重新执行 viewport/view/round-frame 定向测试与上述全 workspace 门禁，全部通过。

## Mutation

临时把 assembler 中“已存在同一 trigger sequence 时不追加事件”的分支改为再次追加，执行：

`cargo test -p mineintent-middle --test agent_context_v5 --locked --offline`

具名测试先红：`fake_backed_v5_assembler_matches_exact_fixture_and_deduplicates_trigger_once`、`assembler_marks_boundary_trigger_and_counts_older_trigger_as_omitted`。恢复 exactly-once 分支后，同命令 5/5 通过。证据同步写入 `.codex-progress.jsonl`。

## 边界与后续

本切片没有实现 Participant Runtime、事件唤醒 registry、concrete backend facade、真实 light producer 或 app composition root；light 只接受显式 fake/source 输入。Information provider/composer/kernel 旧 DTO 与回放 wire 保持不变。工作树保持未提交。
