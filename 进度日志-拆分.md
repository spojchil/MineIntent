# backend runtime 与测试架构拆分日志

## 2026-08-04：开工基线

- 基线提交：`08ee7b2`。
- `cargo test --workspace --all-targets --locked --offline --quiet`：592 通过、0 失败。
- `mineintent-backend` lib：189 通过、0 失败。
- 工作区既有未跟踪文件仅为三份 `需要决策-A7-*.md` 草稿；本任务不改动。
- `runtime.rs` 实际末行约 17,500；其中一个约 2,100 行的 entity producer 内联测试模块、一个约 6,800 行的综合测试模块。

## 约束与注释纪律

- 第一阶段只移动与调整最小模块可见性；每批先跑 backend 测试，再跑 workspace check。
- 第二阶段才做抽象去重和行为修复；不得把“移动”和“语义变化”混在同一个验证批次。
- 每个结果文件硬上限 2,000 行，目标不超过 1,500 行；测试文件同样适用。
- 注释解释不变量、锁顺序、身份/状态迁移、协议事实来源与 fail-closed 原因；不复述语法。
- crate 外公开路径保持不变；跨子模块优先 `pub(super)`，每次升格在本日志登记。

## 初始模块地图

生产代码按现有职责切为以下边界；名称可在编译依赖证据下微调：

- `config`：运行配置、重连调度、transport phase token 与校验。
- `event_dispatch`：事件编号、有限 dispatch lane、runtime subscriber broker 与丢失段。
- `subscription`：observation listener 的预留、回调和退订静默语义。
- `light`：light cache、维度几何、armor 计算。
- `observation_state`：原子 frame facts、实体 residual 与 observation generation。
- `attempt`：attempt admission、source fence、attempt token↔epoch 一一映射。
- `source_admission`：canonical packet/lifecycle source 的捕获、复核与发布。
- `lifecycle`：连接、重连、transport phase、death/respawn/close reducer。
- `shutdown`：命令 admission、movement release、stop watchdog 与最终停止。
- `frame`：snapshot 刷新与原子 frame capture。
- `handle`：稳定的 `RuntimeHandle` 公共门面。
- `observation_source` / `conversion`：协议观察 API、viewport worker 和 DTO 转换。
- `command`：command completion、执行与队列消费。
- `plugins` / `client` / `entrypoint`：Azalea/Bevy producer、Client/Swarm 事件入口和诊断启动入口。

测试先拆为共享 fixture 加窄行为套件：

- entity producer：`ownership`、`packet_adapter`。
- runtime：`command`、`lifecycle`、`viewport`、`observation`、`dispatch`、`block_sound`、`light_armor`、`transport`、`stamped_identity`。

## 可见性升格

- 测试专用 `tests::production_support::{ImmediateBlockObservationReader, helper fn...}`：
  `pub(super)`，仅供兄弟测试套件复用；reader 两字段同范围可见。
- 测试专用 `tests::observation::valid_observation_payload` 与
  `tests::raw_reducer::reducer_respawn_packet`：`pub(super)`，由对应兄弟套件显式导入。

## 分批门禁

### 批次 T1：entity producer 内联测试

- 纯移动为 `entity_events_owner_tests/{mod,ownership,packet_adapter}.rs`。
- 文件行数：88 / 846 / 1,165；原 `runtime.rs` 减少 2,096 行。
- 可见性升格：无。
- `cargo test -p mineintent-backend --lib --locked --offline --quiet`：189/0。
- `cargo check --workspace --all-targets --locked --offline`：通过。
- `cargo fmt --all -- --check`、`git diff --check`：通过。

### 批次 T2：runtime 综合内联测试

- 纯移动为共享 fixture 加 12 个行为套件：command、lifecycle、viewport、observation、
  dispatch、block_sound、armor_formula、light_armor、raw_reducer、transport、
  stamped_identity；production fixture 独立成文件。
- 最大测试文件 1,086 行；其余均低于 1,100 行，`runtime.rs` 再减少约 6,900 行。
- 三个跨套件 helper 收到父测试模块或以 `pub(super)` 显式共享；无生产可见性变化。
- `cargo test -p mineintent-backend --lib --locked --offline --quiet`：189/0。
- `cargo check --workspace --all-targets --locked --offline`：通过。
- 默认与 stable 的 `cargo fmt --all -- --check`、`git diff --check`：通过。
- 完整 `cargo test --workspace --all-targets --locked --offline --quiet`：592/0。

### 批次 P1：config / transport policy

- `RunConfig`、reconnect schedule、transport/stable/watchdog token 移至 `runtime/config.rs`；
  `runtime::RunConfig` 公共路径由根模块重导保持不变。
- `RESPAWN_SETTLE_DELAY` 留在 runtime 根，后续随 motor 移动，避免把 actuator 行为伪装成配置。
- token 与 schedule 内部字段只提升到 `pub(super)`；`retry_at_with_delay` 仍为模块私有。
- 为公开 `RunConfig` 补充职责 rustdoc。
- `cargo test -p mineintent-backend --lib --locked --offline --quiet`：189/0。
- `cargo check --workspace --all-targets --locked --offline`、fmt 与 diff gate：通过。

### 批次 P2：contract DTO adapter

- backend-local → frozen contract 的纯转换集中到 `runtime/dto.rs`（235 行）。
- 子模块只开放调用方实际使用的 `pub(super)` 转换；内部组合 helper 不导入根模块。
- 没有合并 backend snapshot 与 contract DTO，两套类型及失败语义继续分离。
- backend 189/0；workspace check、fmt、diff gate 通过。

### 批次 P3：bounded events 与 subscription

- dispatch lane、runtime subscriber broker、loss segment、callback lease、同步 broadcast、
  subscription RAII 全部集中到 `runtime/events.rs`（966 行）。
- 保持 `runtime::RuntimeEventReceiver`、`runtime::RuntimeObservationSubscription` 公共路径；
  facade 测试所需容量常量仍为原 `pub(crate)` 路径。
- 状态类型和父级驱动方法只为 `pub(super)`；测试直接观测的 lane sequence/queue 字段
  也只在 runtime 范围开放，没有生产 API 扩张。
- backend 189/0；workspace check、默认/stable fmt、diff gate 通过。

### 批次 P4：frame facts 与 entity reducer

- light cache、armor/light reducer、原子 frame capture 与 world read 集中到
  `runtime/frame.rs`（685 行）；实体 post-state、residual authority 与 protocol snapshot
  合并集中到 `runtime/entity.rs`（427 行）。
- 保留 packet source admission 与 observation write boundary 两次校验；没有把 residual
  与 ECS snapshot 合并成第二套实体状态机。
- 跨 reducer 的类型和方法只提升为 `pub(super)`；backend 189/0、all-target check 通过。

### 批次 P5：attempt ownership 与 lifecycle

- attempt token↔epoch、一致性 fence、canonical source admission 与 reconnect owner 迁至
  `runtime/ownership.rs`（835 行）。
- transport phase/stable timer、death/respawn/close reducer、故障分类和重连调度迁至
  `runtime/lifecycle.rs`（944 行）。
- `CloseEvidence` 只在 runtime 范围可见；生命周期锁顺序与断线前清理注释随实现迁移。
- backend 189/0、all-target check 通过，无新增警告。

### 批次 P6：shutdown、handle、observation 与 command

- command admission、active movement 物理释放、stop watchdog 和 stopped finalization 迁至
  `runtime/shutdown.rs`（492 行）。
- 稳定公共句柄与 frame facts 迁至 `runtime/handle.rs`（275 行），原公共路径由根模块重导。
- epoch-bound observation/viewport worker 迁至 `runtime/observation.rs`（580 行）。
- command validation、completion ownership 与 Azalea motor actuator 迁至
  `runtime/command.rs`（590 行）；`RESPAWN_SETTLE_DELAY` 在此批随 actuator 移入。
- 新增可见性均止于 runtime 父模块；backend 189/0、all-target check 通过。

### 批次 P7：producer、driver、state 与目录化收口

- canonical packet/ECS producer 迁至 `runtime/producers.rs`（860 行）；现有 backend
  source metadata 暂按纯移动保留，等待下一阶段直接修改 Azalea fork 后删除。
- Azalea app composition、client/swarm driver 与 run entrypoint 迁至
  `runtime/driver.rs`（434 行）；`run`/`run_with_handle` 公共路径由根模块重导。
- runtime-owned mutable state 和 reducer 共用 primitive 迁至 `runtime/state.rs`（698 行）；
  所有字段/方法可见性限制为 `pub(super)`，没有扩张 crate 外 API。
- `runtime.rs` 目录化为 `runtime/mod.rs`（146 行），只保留 imports、模块声明、窄重导和
  测试模块声明；生产与测试 Rust 文件最大 1,104 行，全部低于 1,500 行目标。
- `cargo check --workspace --all-targets --locked --offline`：通过。
- `cargo test --workspace --all-targets --locked --offline --quiet`：592/0。
- `cargo fmt --all -- --check`、`git diff --check`：通过。
