# 中期更新 15（2026-08-03）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-14，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。
> 本次裁定对应仓库根三份 A7 草稿（需要决策-A7-source-provenance / frame-facts-seam / transport-connect-cancel）；管理进程重启后请把三条并入台账（编号 NEW-13/14/15）并移除草稿文件。

## NEW-13 裁定：A——vendor 源头加不可变 connection generation（第二档）

维护者已有既定原则「受控依赖的问题修在源头：fork 在手就直接改，不在自己层绕行」。三个选项里 B（fail-closed 永久禁用重连）与被排除的伪解法正是"在自己层绕行"的降级形态，A 是源头修复，原则直接适用：

1. **由 A7 直接修改 supplies/vendor**，不等外部提供；「本轮禁止改 vendor」的派发约束就此条所需的最小 seam 解除。
2. 每次 `RawConnection`/join attempt 在**消息产生处**盖不可变 generation/token，覆盖草稿列的最小面（ReceiveGamePacket/Disconnect/ConnectionFailed/WorldLoaded、StartJoinServer/reconnect handoff、Init/Login/SwarmDisconnect 的有限 handoff）；backend 与自身 epoch 一对一绑定，仅收匹配项。
3. **纪律**：补丁最小化、差异可列举；在 vendor 目录旁维护一份补丁清单（文件+目的+行位），迁回主仓库时随 vendor 一起审。
4. 当前 fail-closed `EntitySourceFence` 保留为 token 缺席时的兜底语义，不作为完成态。

## NEW-14 裁定：A——非 wire 的原子 frame-facts 捕获 seam（第二档）

不给 `MinecraftSnapshotV1` 加字段（同名 wire 静默漂移，`deny_unknown_fields` 与 fixture 回放都反对）；不铸 snapshot v2（工作面远超两个事实的需求）。按草稿 A 实施：

- contracts 增加进程内值类型：`snapshot + armor: Option<u8> + light: Option<u8>`；
- `MinecraftBackendApi` 增加带默认实现的方法，旧 fake 默认 armor/light = `None`，**绝不发明 0 或 15**；
- 真实 facade 在同一 observation generation 下原子捕获三者，杜绝跨 epoch/dimension 混合开场帧；
- Participant 对缺 light 维持 fail closed，armor 缺失或 0 均省键（更新-12 语义不变）；armor 输出前夹到 0-20。

草稿引用的 26.1.2 字节码语义（`getRawBrightness(pos,0)` = max(sky, block)、`getArmorValue` 的属性叠加顺序）作为 light/armor 的权威口径登记；这同时把更新-08 遗留的「盔甲韧性 HUD 待核」推进了一步——韧性面仍按默认 tooltip-only 处理，不进帧。

## NEW-15 裁定：transport pre-Init 取消与 NEW-13 共用同一 vendor seam（第二档）

草稿证据成立：Azalea 0.16 公共 API 不返回在途 `add_with_opts` 的 handle，`CreateConnectionTask` 不在 Disconnect 清理 bundle 内，仅丢 future 不构成旧 socket 不迟到绑定的证明。裁定：

1. **不另造第二套机制**——`StartJoinServerEvent` 处铸的 attempt token（NEW-13 §2）同时作为 pre-Init 取消的定位标识：vendor 暴露按 token/entity 取消 `CreateConnectionTask` 的最小 hook，旧 attempt 返回的 Client 只能走 stale/disconnect 路径。
2. connect deadline 在 `command_admission` 下校验 entity/epoch/retry ordinal/phase generation 的设计照草稿 §2。
3. 验收照草稿 §3：runtime-shaped test 证明 A 超时后 task/socket 被取消、B 的 `ConnectionRequested/Init` 只认 B token；Paper 真实断网/黑洞 socket/慢登录时序保留为 gate b 实测项，不许用事件发布伪装完成。
4. 草稿「当前可继续的范围」（backoff、retry ordinal、login/spawn deadline、generation 门控、stop watchdog）不受本裁定阻塞，照常收口。

## 台账

NEW-13/14/15 已裁定；后续新增待决从 NEW-16 起。维护者保留否决权（第二档），异议走下期更新翻案。
