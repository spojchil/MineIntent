# 收尾任务进度日志（执行方：验收侧，codex 额度耗尽后接手）

## 2026-08-04：任务一 app composition root

### 交付

新增 `crates/app`（workspace 第四成员，`mineintent` 可执行）：

- `config.rs`：env 配置等价迁移 oracle `src/app/config.ts`——同名变量、同默认值
  （world local-world / 127.0.0.1:25565 / MineIntentBot / 数据目录 `.mineintent`
  CWD 解析=NEW-02）；差异均有裁定锚点：agent-service/tool-bridge 变量删除
  （更新-01）、版本钉 26.1.2、auth 仅 offline（2026-08-01 发布范围裁定，
  microsoft 显式拒绝）。timeouts/reconnect 默认值逐字对照 config.ts:32-41。
- `lib.rs`：`MineIntentApp::start` 装配顺序 = backend start→Ready →
  journal(events.jsonl,worldId,sessionUuid)/memory/speech/viewport reader →
  B4 生产 registry（五 capability 单源）→ AppAgentFactory →
  ProductionParticipantFrameSource → ParticipantRuntime::try_new →
  factory.bind_runtime → start_worker。停机 = runtime.stop() → frame source
  dispose → backend stop，best-effort 聚合首错（oracle closeBestEffort 同义）。
  prompt 显式选 participant-system/v2（NEW-04 纪律）。
- `factory.rs`：每 wake 新建 assembler+dispatcher+runner；`RuntimeScopeGuard`
  以 Weak<runtime> 对照 wake 快照的 scope/generation，runtime 已灭 fail closed；
  生产 action id = UUID v4，UTC 口径复用 SystemUtcClock。
- `model.rs`：`ScriptedModelProvider`（gate b 假模型），脚本耗尽固定 closing
  保证有界收束；默认纵向脚本 view(full)+say 一轮。

### 门禁（自跑）

- `cargo test --workspace --all-targets --offline`：**594 通过 / 0 失败**
  （新 crate 并入后与基线一致）；
- `cargo check -p mineintent-app --offline` 0 error 0 unused warning；
- `cargo fmt -p mineintent-app -- --check` 通过。

### 诚实边界

- app 自身尚无 runtime-shaped 测试：装配正确性由编译期类型 + 既有组件测试 +
  Paper 纵向（任务四）承担；纵向即 app 的实测。
- `RUN_DEADLINE=120s` 与默认纵向脚本的工具参数在 Paper 实测时校准登记。
- debug HTTP 服务面未迁（DebugStateStore 已接）；正版登录未做（裁定内）。
- BackendReady 的连接细节暂未打印；入口只消费成功信号。
