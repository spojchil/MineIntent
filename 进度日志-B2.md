# 进度日志-B2

## 2026-08-02｜NEW-07 设计审计

### 约束与基线

- 基线为 `ae13ce6`，分支为 `port/b2-round-frame`；开始审计时工作树干净，没有 commit、push、子代理或其他 worktree 操作。
- 已完整阅读 `移植任务书.md`、`中期更新-01.md` 至 `中期更新-06.md`、`需要决策的新问题.md`，以及总进度、B 线、A 线、A2 线日志。Update 06 是本批最高优先级。
- A3 所有的 backend viewport kernel、minecraft viewport DTO、Information viewport provider 与 view 输入 schema 不进入本批；现有 `ViewportRead`/`ViewportProjection` 只作为采样器的泛型 payload 入口，不复制几何或 `BlockInfo`。

### 现有状态机证据

- `crates/middle/src/agent/run.rs` 的 `AgentRun` 在 `model_response` 中先追加 assistant replay，在 `tool_results` 中按 pending ID/数组顺序严格追加全部 `role=tool` 消息，然后进入 `NeedModel`；`AgentRun` 没有 I/O 或采样依赖。
- `crates/middle/src/agent/driver.rs` 的 `AgentLoopDriver::dispatch_in_order` 是唯一逐调用执行边界：本地 invalid call 不进入 dispatcher；实际 dispatcher 调用在同步/异步 panic 围栏内，普通失败转为配对 failed result，取消/deadline 维持整轮控制优先级。
- `crates/middle/src/agent/runner.rs` 只负责 prompt 组装、driver 装配和 transcript fail-open 接线；`crates/middle/src/agent/transcript.rs` 序列化 `run.messages()`，因此只要 AgentRun 追加 frame，轮末帧自然同时进入下一次 model request 与 transcript。
- opening `AgentFrame` 仍只由 `prompt.rs::initial_messages` 追加一次；不能复用或扩展它承载 NEW-07 轮末帧。

### 本批设计

1. 在 `crates/contracts/src/agent` 增加独立、严格的 `mineintent.viewport-frame.v1` wire envelope。成功只序列化 `protocol/at/viewport`；失败只序列化 `protocol/at/viewport:null/unavailable`，拒绝未知字段、缺失字段、成功 null viewport、失败空 unavailable，并且不含 `tool_call_id`。
2. 在 `ToolDispatcher` 增加默认返回 `Option<ExecutionResource>` 的分类 seam。driver 只对已解析且实际交给 dispatcher 的 invocation 查询该 seam；因此不会按工具名或 JSON 猜 body，`Body` 的成功、普通失败、panic、resource busy 都在 dispatch 前计入；local invalid 永不计入。
3. 在 middle agent 增加 `RoundViewportSampler` seam。采样器提供显式注入的 UTC 字符串时间戳和可序列化 payload，采样 future 接收同一个 `ExecutionControl`；driver 在所有 tool messages 已追加后、下一次 model request 前最多采样一次。普通读取/采样 panic/序列化失败变为显式 unavailable frame；取消/deadline 仍按既有优先级终止整轮。
4. 保留逐工具 `result/observationAfter` 原顺序和内容，不由 Agent 层过滤非 viewport observation；轮末 frame 作为独立 `role=user` message 的 JSON content 追加到 AgentRun replay，transcript 不另造旁路。
5. 保持 v1 prompt 文件不改；新增 `participant-system/v2.txt` 并让 catalog 只按显式 key/version 选择。v2 只替换观察机制文字，明确身体工具效果/非视觉 observationAfter、身体批次一帧、view full/directed 语义与 full 截断提示。

### 分阶段计划

- 实现 contracts envelope 与 ToolDispatcher 分类 seam；先补严格 wire/分类的最小测试。
- 实现 AgentRun 追加入口、driver sampler/资源批次状态，并补成功/失败/顺序/控制优先级回归。
- 接线 runner/transcript 与 v2 prompt，锁定 v1 字节、v2 exact、显式选择。
- 临时把 frame 放到 tool messages 前或排除 body 失败，运行新增具名测试确认失败，恢复后复跑；最后执行所有门禁。

### 待决审计结论

- 未发现维护者第三档仍未唯一的问题：NEW-07 已由 Update 06 完整裁定；采样器泛型形状、默认实现与 panic 围栏属于可逆内部工程 API，按 Update 03 第二档/“写法可改”自行选择并在本日志记录。
- 暂不实现 directed capability 输出生产者；本批只承接既有 contracts payload，避免越过 A3 与生产 dispatcher 的所有权。

## 2026-08-02｜第一次状态机测试

- 在 `crates/middle/tests/agent_run_loop.rs` 新增 `constructed_round_frame_is_appended_after_every_tool_message`，先完成一批严格配对的 tool messages，再追加已构造的独立 user frame，最后才发出下一次 `CallModel`。
- 定向命令 `cargo test -p mineintent-middle --test agent_run_loop constructed_round_frame_is_appended_after_every_tool_message --locked --offline`：1/1 通过。

## 2026-08-02｜Prompt 测试

- 新增 `participant-system/v2.txt`，catalog 仅接受显式 `participant-system@v2`；v1 文件与 v1 exact 字符串未改。
- `cargo test -p mineintent-middle --test agent_prompt --locked --offline`：6/6 通过，覆盖 v1 字节冻结、v2 exact、显式选择和未知版本失败关闭。

## 2026-08-02｜轮末 frame 实现与定向回归

- contracts 新增独立 `ViewportFrameMessage`；成功/失败序列化严格保持 `protocol/at/viewport[/unavailable]` 字段顺序，failure 的 `unavailable` 非空，未知字段、success null、failure 缺失/空原因和 `tool_call_id` 均拒绝。
- `ToolDispatcher::resource` 是默认可覆写的分类 seam；driver 只对真正进入 dispatcher 的 parsed invocation 分类，不按名称或 JSON 猜资源。Body 在 dispatch 前计数，所以 success、普通失败、resource busy、同步/异步 panic 均触发同批一次采样。
- `RoundViewportSampler` 由装配层提供显式 timestamp；采样接收同一 `ExecutionControl`，普通失败、panic、payload 序列化失败形成 unavailable frame，取消/deadline 仍优先终止。AgentRun 不做采样，只追加 driver 已构造的 user message。
- 定向 `cargo test -p mineintent-middle --test agent_round_frame --locked --offline`：13/13 通过；覆盖多 body 一帧、tool 顺序、observationAfter 保留、view/say/memory、local invalid、混合 view+body、失败/panic/busy、采样失败/panic/序列化、共享 deadline、取消/期限和 runner/transcript。

## 2026-08-02｜变异验证

- 临时把 `driver.rs` 中 `run.tool_results(results)` 移到轮末 frame 追加之后；具名测试 `multiple_body_calls_sample_once_after_all_tool_messages` 按预期失败，错误为 `agent_run_not_ready_for_user_message`，说明状态机与顺序回归能捕获该变异。
- 已恢复 tool messages → frame 的正确顺序；同一具名测试恢复后 1/1 通过，当前源码无该变异残留。

## 2026-08-02｜最终验证

- 精确 agent targets（driver/prompt/round-frame/run-loop/runner/transcript）：48/48 通过；contracts Agent/capability targets：29/29 通过。
- `cargo test --workspace --all-targets --locked --offline`：316/316 通过；`cargo check --workspace --all-targets --locked --offline`：通过。
- `cargo fmt --all -- --check`、`cargo +stable fmt --all -- --check`、`git diff --check`：全部通过。
- Deferred：生产 `ToolDispatcher` 装配与真实 viewport sampler I/O、A3 负责的 backend viewport kernel/minecraft viewport DTO/Information provider/view schema、directed capability producer 与实际 app/Paper 端到端接线；本批只提供可装配 seam 和 agent 状态机行为。
- 未 commit、未 push；没有修改 backend、minecraft viewport DTO、Information viewport provider 或 view input schema。

## 2026-08-02｜管理侧复审返修

- v2 prompt 改为不得虚构世界坐标；directed 只能复用观察中已有或玩家明确给出的坐标，实体 id 与目标 ref 仍不得生成或管理；v1 文件与 v1 exact 结果未改。新增 v2 语义断言防止旧矛盾句回归。
- `ViewportFrameMessage` 的 success/unavailable 构造与 strict deserialize 现在拒绝空、全空白或含控制字符的 `at`；没有额外收窄 UTC 格式，assembly seam 继续要求提供 UTC。
- `RoundViewportSampler::timestamp` 与 `ToolDispatcher::resource` 均在 driver panic 围栏内；timestamp panic 使用 transcript 共享 formatter 形成真实当前 UTC 的 `viewport_sampler_panicked` frame，分类 panic 配对 `tool_dispatch_panicked` 且不 dispatch、不采样。
- `NoRoundViewportSampler` 改用 transcript 共享真实当前 UTC，未装配 sampler 仍发送显式 `viewport_sampler_not_configured`，不再发送 epoch 占位。
- 新增 timestamp panic、未装配 sampler、resource 分类 panic 与 `at` strict 负例回归；精确复审 targets：round-frame 16/16、prompt 6/6、contracts 29/29。

## 2026-08-02｜本次返修变异验证

- 临时将 `NoRoundViewportSampler::timestamp` 恢复为 `1970-01-01T00:00:00Z`；具名测试 `missing_sampler_uses_real_utc_for_explicit_unavailable_frame` 按预期失败（epoch 不应出现在 frame `at`）。
- 已恢复真实 UTC 实现；该具名测试与 `sampler_timestamp_panic_is_isolated_with_a_real_utc_frame_at` 恢复后均 1/1 通过，源码无该变异残留。

## 2026-08-02｜复审最终门禁

- 复审精确 targets：`agent_round_frame` 16/16、`agent_prompt` 6/6、contracts Agent/capability 29/29；workspace 全量 319/319。
- `cargo check --workspace --all-targets --locked --offline`、`cargo fmt --all -- --check`、`cargo +stable fmt --all -- --check`、`git diff --check` 全部通过。
- 本次仍未修改 backend viewport kernel、minecraft viewport DTO、Information viewport provider 或 view 输入 schema；未 commit、未 push。
