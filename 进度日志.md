# 进度日志

> 规则（任务书「工作纪律」）：每次工作写一条——日期、做了什么、证据锚点、下一步。
> 凡写「完成」必须同一行给出验证证据；测试通过不等于行为验证。
> 遇到 产品.md 未覆盖的问题：登记在「待维护者裁决」，选最保守实现继续，不得静默定案。

## 待维护者裁决

（无）

## 补给申请

- [ ] minecraft.wiki 协议页离线快照（Java_Edition_protocol/Packets、Protocol_version、Data_version）：
  Cloudflare 拦截了脚本抓取，请用浏览器「另存为」放入 `supplies/wiki/`。
  非阻塞：反编译源码（supplies/decompiled/）与 azalea 源码是主参考。

## 日志

### 2026-07-31 至 08-01（物料准备，由维护者一侧的 Claude 会话完成）

- 仓库初始化、任务书入库（`任务书.md`）。
- 钉版决定：**协议号 775（MC 26.1.x），azalea 0.16.0+mc26.1（crates.io 发布版）**。
  理由：crates.io 最新发布版目标 26.1；26.2 仅在 azalea 未发布的 git 主线。
  依据任务书「≥775 内选库支持最好的」条款，此为记录。26.2 物料保留作备选（supplies/mojang、supplies/paper 顶层）。
- 物料全部落地并验证，见 `supplies/MANIFEST.md` 与 `supplies/SHA256SUMS`：
  - 三 jar（官方 client/server、Paper build 74）经官方 sha1/sha256 校验一致；
  - `cargo build --offline` 于 nightly 1.99 全量编译通过（375 crate，4m11s）——离线构建自足已证明；
  - Paper 26.1.2 用便携 JDK 25 完整首启（世界已生成），cache/libraries/versions 留存，离线重启无需下载；
  - 官方 server 内层 jar 反编译完成：4789 个 .java、全真名（oracle 就位）。
- 事故记录：首次反编译把输出目录与输入写反，Vineflower 将 server-inner.jar 清空；
  外层 jar sha1 校验完好后重新抽取（16461 条目）并带硬断言重跑。教训：管道中的完好性检查要用断言中止，不要只打印。
- 下一步（离线代理开工即 M0）：server list ping 实测协议号 775、确认 DataVersion、azalea 行为清单、EULA 摘录。

### 2026-08-01（M0，当前会话）

- **完成 M0 版本与选型**：Paper 26.1.2 build 74 在 Java 25 下启动并监听 25565；原始 status ping 返回 `version.protocol=775`；官方去混淆源码 `DetectedVersion.java`/`SharedConstants.java` 与首启 `world/level.dat` 均确认 DataVersion `4790`；azalea 提交 `a253702e` 的 workspace 版本为 `0.16.0+mc26.1`，并审计出默认自动重生、自动重连、资源包自动接受、传送确认、客户端物理预测和乐观方块更新。证据与源码锚点见 `M0-版本与选型.md`。
- 下一步：实现 M1 的显式插件组装和连接/登录事件流；先验证 keep-alive、spawn、断线事件，再接 MineIntent 语义适配。

### 2026-08-01（M1/M2/M3/M4 第一轮实现，当前会话）

- **M1 短时连接验收完成**：offline Paper 26.1.2 上收到 `transport_initialized`、`logged_in`、`ready`，连续收到并记录 KeepAlive 的服务端接收与 Azalea 应答；`--duration-secs 8` 后产生 `stopping`/`shutdown_requested` 且进程退出。证据：`server-run/m2-m3-test/combined.log`（运行产物已被 `.gitignore` 排除）。首次 30 分钟保持尝试 PID 19396 在约 13 分钟被 Paper 的 Spider 杀死；第二次 PID 20052 在约 8 分钟被原世界 `level.dat` 中的 Skeleton 杀死，虽配置文件写成 peaceful，但持久化世界难度未改变，因此两轮均判为**未通过**。第二次还验证了死亡事件只报一次、禁用自动重生后保持连接但不继续更新活体快照；证据：`server-run/m1-30m-safe/events.log` 与 `server-run/paper-restart.log`。代码已加入死亡事件去重、dead 状态冻结、断线清空旧观察缓存。
- **M1 30 分钟连接验收完成**：全新 `server-run/m1-clean/` Paper 26.1.2 世界使用 `difficulty=peaceful`、`spawn-monsters=false`、端口 25566；PID 22388 的 `MineM130Fresh` 于 02:22:02 登录并在 02:52:02 由 `duration_elapsed` 自主停止，保持约 1800 秒。后端事件中 KeepAlive 服务端接收/客户端应答各 1753 次，`died`/`connection_closed`/`error` 均为 0，`stopping` 与 `shutdown_requested` 各 1 次；Paper `java.stdout.log` 同时记录登录和断开。该目录及世界均被 `.gitignore` 排除。
- **M2 第一轮完成**：加入严格 `BackendCommandEnvelope` JSONL stdin、`RuntimeHandle::send_chat`、命令/服务端聊天事件；Paper 实测 CLI 初始聊天与 stdin 聊天均收到服务端回显，事件带发送者、`plain_text`、`raw_text`、`received_at`。证据：`server-run/m2-m3-test/combined.log`、`server-run/m2-m3-test/stdin-move-2.log`。
- **M3 第一轮完成**：加入 `mineintent.minecraft.snapshot.v1`，快照包含世界维度/协议/DataVersion 对应版本标签、边界高度、self 位置/速度/视角/生命/饥饿/经验、背包非空槽、在线玩家列表；运行时句柄提供快照、玩家列表、ECS 已知实体列表、块读取与订阅的只读观察源，已在 Paper 实测快照含 `minecraft:overworld`、`minY=-64`、`height=384`、self 和在线玩家。实体 DTO 已补齐尺寸、姿态、有效性等 viewport 所需字段；块读取已补齐状态属性、碰撞几何、透明提示与空碰撞盒标志，并明确不把“已加载”冒充“可见”。证据：`server-run/m2-m3-test/combined.log` 中 `snapshot_changed`；`cargo test --offline` 的实体/块 DTO 单测，现场块/实体抽样待下一轮新二进制。
- **M4 第一轮完成**：`move`/`look_relative`/`release_all` 以 `commanded` 记录；`move` 已覆盖前后左右、sprint、jump、crouch；运动期间按采样记录 `client_predicted` 姿态，Spawn/Death 边界快照为 `server_observed`，Tick 快照改标为 `client_predicted`，不把本地物理预测值伪装成服务端事实。相对视角方向和 50–1500ms/±90° 输入边界与主仓库 motor 对齐。证据：`server-run/m2-m3-test/stdin-move-3.log` 中同时出现两类来源及不同时间的坐标轨迹；命令边界单测由 `cargo test --offline` 覆盖。
- **M4 单一固定序列专项验收完成**：在全新 RCON Paper 世界铺设确定地面，以 yaw=0 执行 `forward/250ms`；RCON `/data get entity` 输入前为 `[4.5,115,-8.5]`，输入后约 2 秒为 `[4.5,115,-7.6428]`，后端 `client_predicted` 采样为 `[4.5,115,-7.8731]`，两条轨迹相差小于 1 方块且来源明确。证据：`server-run/m4-rcon/m4-sequence-evidence.txt`；该证据只覆盖这一固定序列，跳跃/潜行/斜向和更多落点组合仍未宣称完成。
- M4 物理 oracle 记录：官方去混淆源码 `supplies/decompiled/26.1.2-server/net/minecraft/server/network/ServerGamePacketListenerImpl.java` 的 `handleMovePlayer` 负责校验客户端移动、碰撞后 `absSnapTo` 服务端位置并在失败时发送 teleport；客户端执行层对应 vendor `azalea-client/src/plugins/movement.rs` 的 `send_position` 与 vendor `azalea-physics/src/local_player.rs` 的按键方向枚举。最新运行还启用 `packet-event`，把服务端 `ClientboundPlayerPosition` 校正包记录为 `server_observed`。
- 下一步：用启用最新协议事件的二进制补 M3/M5 块状态与实体细节抽样；再按任务书固定输入序列核对 Paper 服务端落点误差，并审计重连/死亡行为。

### 2026-08-01（协议边界与来源审计，当前会话）

- **完成协议对齐修正**：`src/snapshot.rs` 的块、物品、实体类型使用 MineIntent 主项目契约的本地注册名（`air`/`stone`/`oak_log`），可选字段省略而不写入 `null`；`RuntimeHandle::snapshot_source()` 与观察源同名方法让快照缓存的 `server_observed`/`client_predicted` 来源可检查。证据：`cargo +stable fmt -- --check`、`cargo test --offline`（9 个测试通过）。
- **补充服务端运动事实入口**：启用 Azalea `packet-event` feature，仅把 `ClientboundPlayerPosition` 位置校正包转换为 `self_state` 的 `server_observed` 事件；未重新启用自动重生、自动重连或资源包自动接受。证据：`src/runtime.rs` 的 `Event::Packet` 分支；最新二进制在 `server-run/m4-rcon/client/chat-events.log` event-5 实际抽样到该包。
- **M4 证据已补充，下一步**：最新二进制已完成一次服务端 `/data get entity` 独立真值和固定移动落点抽样；M1 重连事件也已在同一临时服完成验证，接下来补 M3/M5 块状态与实体细节抽样。M6 死亡/重生、维度切换和对称性场景仍未开始。
- **M1 断线重连专项完成**：RCON 踢出 `MineM4ReV2` 后，最新二进制事件出现 `connection_closed=1`、`reconnect_scheduled=1`、`reconnect_requested=1`、`ready=2`；首次连接为 `connectionEpoch=1/attempt-1`，重连为 `connectionEpoch=2/attempt-2`，且第二次服务端快照未复用旧观察缓存。该实测同时修复了 Azalea 复用 `LocalPlayerEvents` 时不再发送 `Init` 的 epoch/断线事件漏洞。证据：`server-run/m4-rcon/reconnect-v3-evidence.txt` 与 `reconnect-v3-events.log`。
- **M2/M3 最新二进制冒烟完成**：`MineM4Chat` 的服务端聊天回显 event-22 同时包含 `senderUsername=MineM4Chat`、`plainText`、`rawText`、`receivedAt`；event-17 的 Spawn 快照为 `server_observed`，包含协议 775、`minecraft:overworld`、`minY=-64`、`height=384`、self 和在线玩家。证据：`server-run/m4-rcon/client/chat-events.log`；原始服务端位置校正也在 event-5 记录。
