# 中期更新 16（2026-08-03）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-15，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## 增量视口内核入库（维护者裁定并入，验收侧执行；取代更新-05「代码侧视为增量不存在」的表述）

维护者的增量研究分支 `experiment/rust-viewport-incremental` 已合并进施工 main。状态与边界：

### 1. 入库了什么

- `crates/contracts/src/agent/viewport_incremental.rs`（v3 契约类型 + 测试）；
- `crates/middle/src/agent/viewport_mirror.rs` / `viewport_reducer.rs`（两阶段提交镜像 + 严格接收端）与全部测试；
- 仓库根 `增量视口实验.md`（设计文档，迁回主仓库时并入 docs 或留档案分支，import 预备期再定）。

合并后 contracts + middle 全部套件绿（验收侧实测）。

### 2. 休眠边界（对所有车道生效）

- **入库 ≠ 接线**。`ViewCapability` 与 `RoundViewportSampler` 继续输出冻结的 v2 全量 payload；`AgentLoopDriver` 继续追加冻结的 `viewport-frame.v2`；view 工具维持 full/directed 两模式。
- **v3 不是生产 wire**。`mineintent.agent-context.v5` / `viewport-frame.v2` / `viewport.v2` 三锚点不变；任何把增量帧暴露给模型的改动都是模型可见行为 = **第三档**，须维护者裁定并铸新版本，不在 gate b 范围内。
- 各车道**只需保持它编译与测试通过**（workspace 门禁自然覆盖）；若某车道演进 `BlockInfo`/`ViewportFullV2` 形状导致 mirror 适配层需要跟改，跟改属于该车道的正常收尾，不算越界。
- 已知留待毕业时处理的缺口（设计文档已诚实登记，勿在移植期"顺手补"）：`confirmedRemoved` 需要 backend 逐块权威复核或方块事件；实体 delta 因模型可见身份缺席暂不做；并行工具批次的 viewport 准入与 NEW-05 移植后一并裁。

### 3. 表述更新

更新-05 的「增量整线搁置、代码侧视为增量不存在」修订为：**增量内核在库、编译常绿、不接线；接线与暴露是移植后第三档议题**。观察三分层结论（推送帧/全量拉取/directed 手术刀，增量为驻留负载终局）不变。

## 台账

无新增待决；后续从 NEW-16 起。
