# A2 进度日志

## 2026-08-02｜SEAM-15 首次连接与重连 attempt 身份预分配

### 范围与处理

- 开始基线：`a08c7cd`，worktree clean。本批只修改 `crates/backend/src/runtime.rs` 与本日志；`protocol.rs`、I01 v2 wire、重连参数、观测生产者及其他底层缺口均未提前扩张。
- 外部执行代理的草稿曾越过 SEAM-15 边界并产生重复 `new_attempt` 路径；维护侧停止该草稿、恢复越界文件后，从干净基线直接实现最小 seam。
- 新增唯一连接入口 `begin_connection_attempt`：先在 writer 锁内递增 epoch/生成 `attempt-{epoch}`，再以同一身份发 `connection_requested`；writer 锁覆盖事件编号及订阅广播，避免其他生产者插进身份分配与请求事件之间。
- 初次 `run_with_handle` 在任何网络启动前调用该入口；重连延迟结束、调用 `swarm.add_with_opts` 前也调用同一入口。原 `reconnect_requested` 被统一为 I01 已冻结的 `connection_requested { attempt }`。
- `Event::Init` 只消费预留身份并清空旧观测，不再为同一握手创建第二个 epoch。防御性未预留路径也会先经统一入口补发 `connection_requested`，随后才允许 transport 生命周期事件。

### 测试、变异与门禁

- 新增 runtime 回归，固定首次请求为 epoch 1/`attempt-1`、Init 沿用同一身份、第二次请求为 epoch 2/`attempt-2`，且 process session 不变。
- mutation：临时跳过 writer 的 `new_attempt`；新增测试按预期以实际 epoch 0 对期望 epoch 1 失败，随后恢复实现并复跑通过。
- `cargo test --workspace --all-targets --locked --offline`：通过，共 175 条测试；backend 14/14。
- `cargo check --workspace --all-targets --locked --offline`、`cargo fmt --all -- --check`、`git diff --check`：通过。
- 本批是纯连接状态机顺序 seam，不需要独立 Paper 实服探针；完整重连策略及结构化 close 的实服证据留到对应 A2 小批。

### 明确延后

- `mineintent.minecraft.backend-event.v2` 生产 wire、dimension 信封、结构化 close/failure、完整重连退避、epoch 绑定 observation、观测生产者、复合移动、原子 viewport 与 offline auth 枚举，继续按 A2 缺口清单逐项小提交完成。

## 2026-08-02｜SEAM-12 事件发生时 dimension 信封

### 范围、纠偏与版本裁定

- 开始基线：`03ce6eb`，worktree clean。本批最终只修改 `crates/backend/src/protocol.rs`、`runtime.rs` 与本日志；不改 contracts、manifest/lock、middle、supplies/vendor/移植计划。
- 外部执行代理两次把本批扩张为 RuntimeState、结构化 failure、lifecycle payload 与 event-kind 重写；维护侧先中断、恢复同一 session 纠偏，确认其反向继续后再次中断并停止复用，随后直接从 `03ce6eb` 差异中剔除全部越界实现。
- SEAM-07 **未在本批伪装完成**：生产 backend 仍会发 `keep_alive/self_state/motor/error`，且 lifecycle 仍有 `transport_initialized`、连接请求附加字段及非结构化 close/failure；冻结 I01 v2 不接受这些 kind/payload。此时只改 discriminator 会制造 SEAM-07 明令禁止的“同名不同形”。因此生产 wire 保持 v1；待结构化生命周期、kind 映射与其测试同时收敛后再原子切到 v2 并加拒绝 v1/fixture 对齐测试。

### 实现

- `EventWriter` 持有当前事件时 dimension；所有信封都在 writer 锁内复制这一事实，不再由消费者用稍后 snapshot 回填。协议构造器新增显式 dimension 路径，原无 dimension 构造保持兼容。
- 唯一底层更新入口 `set_dimension` 同时更新 writer 事件事实与现有 `reported_dimension` snapshot 状态；`observe_dimension` 先更新，再在值实际变化时发 `dimension_changed`，所以变化事件自身携带新 dimension。
- 初次进入世界时，Spawn 从同一次 snapshot 得到 dimension，并在 `ready`、`respawned`、`snapshot_changed` 等后续事件前写入 writer。`WorldLoadedEvent` 同样走 `observe_dimension`。
- `new_attempt` 清空 writer dimension；`begin_connection_attempt` 原有 SEAM-15 身份预分配顺序不变，因此新 attempt 的 `connection_requested` 必为 `None`，不会泄漏上一 epoch。

### 测试、变异与门禁

- 新增 3 条 backend 测试：显式 dimension 构造仍保持 v1 discriminator；事件在 emit 时捕获 dimension 且新 attempt 清空；`dimension_changed` payload 与信封都使用新值。原 SEAM-15 测试也增加首次/重连请求均无 dimension 的断言。
- mutation 1：临时删除 `new_attempt` 的 dimension 清空，回归按预期在第二次 `connection_requested` 仍为 `minecraft:overworld` 时失败；已恢复。
- mutation 2：临时把 `observe_dimension` 改为先发变化事件、后更新 writer，回归按预期以信封旧值 `minecraft:overworld` 对新值 `minecraft:the_nether` 失败；已恢复。
- `cargo test --workspace --all-targets --locked --offline`：通过，共 189 条测试；backend 17/17。
- `cargo check --workspace --all-targets --locked --offline`、nightly/stable `cargo fmt --all -- --check`、`git diff --check`：全部通过。
- 本批验证的是确定性的 writer/runtime 顺序；跨维度 Paper 实服证据与 lifecycle v2 端到端 fixture 留到相关底层缺口合批验收。

## 2026-08-02｜SEAM-11 observation source epoch 绑定与 stale 订阅隔离

### 决策锚点与范围

- 开始基线：`37d5a79`，worktree clean。本批只修改 `crates/backend/Cargo.toml`、其本地锁边 `Cargo.lock`、`crates/backend/src/runtime.rs` 与本日志；不改 `crates/contracts` 源码、middle、protocol v1 discriminator、supplies/vendor、移植计划或其他代理日志。
- 第一档自决依据：任务书 §3 `SEAM-11` 与中期更新-03「能由 oracle 唯一决定就直接做」；TS oracle `supplies/mineintent-main/src/minecraft/minecraft-backend.ts:209-242` 在 source 创建时捕获 `active.epoch`，每次读取先拒绝 stale，订阅按 `connectionEpoch` 过滤且只登记 entity/block/sound。对应行为回归为 `supplies/mineintent-main/src/minecraft/minecraft-backend.test.ts:217-308`；缺口表 `移植计划/能力面差距表.md:79-83` 与测试归类表 `移植计划/测试归类表.md:98-100` 指向同一 seam。
- 架构纠偏：不复制冻结错误类型。backend 新增 `mineintent-contracts = { path = "../contracts" }`，所有 stale 结果直接使用 canonical `mineintent_contracts::minecraft::BackendError::StaleEpoch { bound_epoch, current_epoch }`（冻结定义：`crates/contracts/src/minecraft/error.rs`），`Cargo.lock` 仅产生该本地依赖边的机械变化；未改 contracts 源码。

### 实现

- `RuntimeHandle::observation_source()` 在创建时读取并保存 `bound_epoch`；source 的 `epoch()` 永远返回该固定值，不再读取共享 runtime 当前 epoch。
- pose、snapshot source、tracked players/entities、block、viewport concrete read 与 subscribe 均经过 canonical `BackendError::StaleEpoch` 检查；读前与读后都检查，旧 source 不会以空数据、当前世界数据或字符串错误掩盖 stale。当前 concrete source 仍保留没有 snapshot 时的 `Option` 语义；viewport 参数错误最小映射到现有 `BackendError::InvalidCommand { field: "viewport", ... }`，没有新增 `InvalidViewport`。
- observation 订阅拥有独立 registry：只向同一 bound epoch 的 `Entity`/`Block`/`Sound` 事件发送，lifecycle/chat/self/world/player-list/snapshot/motor/error/keep_alive 均过滤；重连后旧 source 不收新 epoch。`RuntimeObservationSubscription` 没有后台任务，显式 `unsubscribe` 与 Drop 都立即移除 registry sender，receiver 随后不再收件。
- `begin_connection_attempt` 与 `EventWriter::new_attempt` 的首次 attempt 预分配、dimension 清空顺序未改，也没有新增第二条 `new_attempt` 路径；生产 discriminator 继续保持 v1。

### 测试、mutation 与门禁

- 新增确定性同步测试（无 wall-clock sleep）：
  - `observation_source_binds_epoch_and_returns_structured_stale_errors`：当前 source pose/entity/block 可读，重连后旧 source 的所有读取返回 canonical `StaleEpoch`，且 `epoch()` 仍为旧值。
  - `observation_subscription_filters_kind_and_epoch_without_background_tasks`：三类 observation facts 可收，全部其他 backend kind 与新 epoch 均不可收，新 source 可收新 epoch。
  - `observation_subscription_unsubscribe_and_drop_release_owned_sender`：unsubscribe、receiver Drop 都停止交付并立即从 registry 移除 owned sender。
- mutation 1：临时把 stale preflight 改为无条件 `Ok(())`；旧 source 回归按预期失败，实测为 `left: Ok(None)`、`right: Err(StaleEpoch { bound_epoch: 1, current_epoch: 2 })`；已恢复。
- mutation 2：临时移除 observation 的 epoch/kind 过滤；kind/epoch 回归按预期失败，receiver 在三条 observation facts 后收到 lifecycle 泄漏；已恢复。
- `cargo test -p mineintent-backend --all-targets --locked --offline`：通过，backend 20/20。
- `cargo test --workspace --all-targets --locked --offline`、`cargo check --workspace --all-targets --locked --offline`、`cargo +nightly fmt --all -- --check`、`cargo +stable fmt --all -- --check`、`git diff --check`：全部通过。

### 明确 deferred / 不能宣称的范围

- VIEW-02 的完整原子 `ViewportRead { projection, source, revision }` 仍 deferred；本批没有实现完整 `ProtocolObservationSource` trait，也没有用旧 viewport 路径伪造 `read_viewport`。
- observation channel 仍使用 tokio `unbounded_channel`，`RuntimeObservationSubscription` 只是 backend concrete seam；它不代表完成有界/overflow 的 SEAM-10，也不代表已实现冻结 contracts `Subscription`/完整 `ProtocolObservationSource` trait。entity/block/sound 生产者补齐、结构化 lifecycle、v2 wire、完整重连与 Paper 实服证据仍留在对应缺口批次。

## 2026-08-02｜SEAM-11 独立审查返修：关闭时丢弃已排队 observation

### 问题与修复

- 审查确认 tokio `UnboundedReceiver::close()` 只阻止新发送，不会丢弃已排队消息；原 concrete subscription 在 `unsubscribe()` 后仍可能由 `recv/try_recv` 读出旧队列，违反“关闭后不得再交付”。
- `RuntimeObservationSubscription::unsubscribe()` 现在在移除 registry sender、调用 receiver close 后同步排空 receiver 队列；`recv()` 在 `closed` 时立即返回 `None`，`try_recv()` 在 `closed` 时立即返回 `TryRecvError::Disconnected`。未改变 unbounded concrete seam 的范围声明。

### 回归、mutation 与门禁

- 新增无 wall-clock 的 `observation_subscription_unsubscribe_discards_queued_events`：先 enqueue entity，再 unsubscribe，确认 `try_recv()` 立即为 `Disconnected`、异步 `recv()` 立即为 `None`，已排队事件不可见。
- mutation A2-3：临时移除 closed 快速返回和 unsubscribe 队列排空；该回归按预期失败，`try_recv()` 仍看到了已 enqueue 的 entity；已恢复。
- backend 定向测试：21/21 通过；workspace `cargo test --workspace --all-targets --locked --offline`、workspace `cargo check --workspace --all-targets --locked --offline`、nightly/stable rustfmt check、`git diff --check`：全部通过。

### 范围声明

- observation channel 仍是 unbounded backend concrete seam，不能宣称完成 SEAM-10 有界/overflow 语义，也不能宣称完成冻结 contracts `Subscription` 或完整 `ProtocolObservationSource` trait。
- VIEW-02 原子 `ViewportRead` 继续 deferred；本返修没有修改 contracts、v1 discriminator 或其他缺口。

## 2026-08-02｜SEAM-11 独立审查返修：read_block unloaded 早退后检

### 问题与修复

- 审查发现 `RuntimeObservationSource::read_block` 在 `world == None` 时只做了读取前 stale 检查，直接返回 `Ok(Unloaded)`，未覆盖 reconnect 清空 world 窗口中的后置 epoch 变化。
- `read_block` 现在在该早退返回前调用 canonical `BackendError::StaleEpoch` 后检；已保留此前已完成的 loaded 路径前后检查、canonical contracts 依赖和 unsubscribe 队列清空修复。
- 为不改变公开 API，生产方法通过私有 `read_block_with_post_read_hook` 调用；该 hook 仅用于同步测试在早退返回前确定性推进 attempt，不引入后台任务或新的连接路径。

### 回归、mutation 与门禁

- 新增 `read_block_unloaded_early_return_rechecks_epoch`：初始 epoch 的 world 为空，早退前同步触发 `begin_connection_attempt()`，结果必须是 `Err(StaleEpoch { boundEpoch: 1, currentEpoch: 2 })` 而非 `Ok(Unloaded)`。
- mutation A2-4：临时移除 unloaded 早退后检；该回归按预期失败，实测为 `left: Ok(Unloaded)`、`right: Err(StaleEpoch { bound_epoch: 1, current_epoch: 2 })`；已恢复。
- backend 定向测试：22/22 通过；workspace `cargo test --workspace --all-targets --locked --offline`、workspace `cargo check --workspace --all-targets --locked --offline`、nightly/stable rustfmt check、`git diff --check`：全部通过。

### 范围声明

- 本返修只处理 unloaded 早退后 stale 防线；unbounded observation channel/SEAM-10、VIEW-02、完整 trait 及 canonical `BackendError` 依赖状态均保持此前裁定与 deferred 范围。

## 2026-08-02｜VIEW-02 原子 ViewportRead 与取消边界

### 决策与实现

- 在不修改 contracts shape/discriminator 的前提下，`RuntimeObservationSource::read_viewport(OperationControl)` 成为 concrete atomic seam；默认参数固定使用 backend 唯一 `viewport` kernel。旧 `viewport(&ViewportOptions)` 保留为明确的非 atomic seam，不能再与 `snapshot_source()` 拼接为 `ViewportRead`。
- `SharedRuntime` 将 world handle、snapshot、FactSource、tracked entities 收进同一 `ObservationState` `RwLock`，每次发布/清空/替换都推进 generation；viewport worker 先取得 world 自身读锁，再在 generation 下复制 pose/entities/source。投影期间不持有 observation/writer 等全局 mutex，只保留 world 自身读锁；返回前在短 observation read + writer epoch 检查下验证 generation、world identity、source 与 ready。
- 创建时 `bound_epoch`、capture 前、worker capture 后、投影完成后和返回前均检查 epoch。generation 变化最多重试 3 次，仍不稳定结构化返回 `NotReady { state: "viewport_capture_changed" }`；epoch 变化返回 canonical `StaleEpoch`，绝不返回混合 projection/source。
- `viewport_revision` 是独立的全局 `AtomicU64` 成功读取序号：只在最终一致性检查通过后 `fetch_add`，每次成功 `read_viewport` 唯一且并发安全；不复用 snapshot/lifecycle revision，取消、期限、stale、缺数据和不稳定 capture 不会把 revision 作为成功结果返回。
- `OperationControl::preflight` 在扫描前按 cancellation 优先、deadline 其次返回 `Cancelled { operation: "read_viewport" }` / `DeadlineExceeded { operation: "read_viewport" }`。投影在线程池 worker 执行，外层 select 监听取消/期限 future；kernel 在几何阶段、方块候选、实体样本和每条射线步进前执行可传播 checkpoint。

### 测试、mutation 与门禁

- 新增 7 个 runtime tests，覆盖成功 projection/source/revision、4 路并发 revision 唯一性、pre-cancel/pre-deadline 不扫描、投影中 cancellation/deadline、capture generation 变化重试、关键窗口 epoch 变化 stale、ready/snapshot/source/world 缺失结构化拒绝；backend lib 29/29 通过。
- mutation A2-5：临时禁用返回前 generation/world/source 校验；具名 `read_viewport_retries_when_capture_generation_changes` 按预期失败，实测返回旧 `[1.0, 64.0, 2.0]`，期望新 capture `[9.0, 64.0, 10.0]`；已恢复，恢复后该测试通过。
- `cargo test -p mineintent-backend --lib --locked --offline`：29/29 通过；`cargo test -p mineintent-backend --all-targets --locked --offline`：29/29 通过，main 0；`cargo check -p mineintent-backend --all-targets --locked --offline`、`cargo fmt --all -- --check`、`git diff --check`：全部通过。

### 范围声明

- 本批只修改 backend runtime/viewport 与本日志；未修改冻结 contracts、middle、A/B 路径、supplies/vendor、移植计划或其他日志，也未生成 TS/JS。
- 本批没有实现完整 `ProtocolObservationSource` trait；既有 unbounded concrete subscription 仍不代表冻结 `Subscription` 或 SEAM-10 有界/overflow 语义。
