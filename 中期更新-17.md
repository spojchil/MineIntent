# 中期更新 17（2026-08-04）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-16，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## 文件规模纪律（维护者定标）

维护者原话口径：一两千行可以接受；上万行的文件在任何语言里都不可接受。落为三档：

### 1. 标准

- **≤2000 行**：正常，无需处理（Rust 内联单测撑大文件属惯例）。
- **2000-5000 行**：容忍存量；实质触碰该文件的切片**借道拆分**（顺带拆到 2000 以下），不专项立项。当前存量：middle/tests/participant_runtime.rs（3,933）、backend/src/facade.rs（3,577）、middle/src/participant/runtime.rs（3,270）、backend/src/viewport.rs（2,051）。
- **>5000 行**：不可接受，专项拆分。当前唯一：**backend/src/runtime.rs（17,541 行）**——拆为 `runtime/` 模块目录。
- 新增代码不得把任何文件推入更高档位。

### 2. 执行规则（行为中立是硬约束）

- **纯移动，零逻辑变化**：不重命名公开符号、不改 API 形状、不顺手优化；`#[cfg(test)]` 单测随其所测的私有项进入对应子模块文件。
- **分批提交**，每批之后完整门禁必须与拆分前完全一致（当前基线：workspace 全套 **592 通过 / 0 失败**、双工具链 fmt、`git diff --check`）；测试数量不得减少。
- **单车道独占**：runtime.rs 拆分进行中不得有其他车道并行改动 crates/backend；管理进程重启时先确认拆分车道状态。
- 模块边界按现有代码内的注释分区与职责划分推导，属一档自决，落进度日志即可，不新增 NEW 决策。

## 台账

无新增待决；后续从 NEW-16 起。
