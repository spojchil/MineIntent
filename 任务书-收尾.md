# 任务书：迁移收尾（app 装配 → NEW-15 接线 → 传输收口 → gate b 预备）

> 在 runtime.rs 拆分（任务书-runtime拆分.md）落地之后按序执行；
> 各项均受既有裁定约束（更新 01-17），不夹带新功能、不改冻结 wire。

## 一、app composition root（最后一块工程主体）

新建 app crate（workspace member），职责按 进度日志-B7.md 已冻结的边界：

1. 数据目录寻址按 NEW-02 裁定（调用方给定目录；默认 `.mineintent`、CWD 解析）。
2. 启动顺序：backend `start(OperationControl)` → Ready 后构造
   `ProductionParticipantFrameSource`（含 sound history 注入）→
   `ParticipantRuntime::try_new` → `start_worker`。
3. 每 wake 以当前 runtime scope/generation/trigger 构造 registry/dispatcher 并注入
   `with_observation_after(ParticipantObservationAfterSource)`；registry 与 factory
   的 identity 校验沿 B6 既有 seam。
4. 装配真实 memory/journal/speech/debug 实例；模型 endpoint 走进程内
   `AgentRunner`（gate b 用假模型 provider，真实模型是加分项）。
5. 停机顺序：runtime `stop` 完成 → dispose production frame source → backend stop；
   A7 lifecycle/ConnectionClosed 的 attempt 身份映射接入事件。
6. light 缺失必须结构化失败（fail closed），不得默认 0/15；armor 0..=20 夹取沿
   NEW-14 裁定。

## 二、NEW-15 pre-Init connect deadline 接线

vendor hook 已随补丁清单落地（`CancelConnectionTaskEvent(entity, token)`）。backend 侧：

- connect deadline 在 `command_admission` 下校验 entity/epoch/retry ordinal/
  phase generation，锁外取消对应 `CreateConnectionTask`；
- 旧 attempt 迟到返回的 Client 只能走 stale/disconnect 路径；
- runtime-shaped test：A 超时后 task 被取消、B 的 `ConnectionRequested/Init`
  只认 B token（更新-15 §3 的验收原文）。

## 三、传输策略收口（先审计再补）

先核对 2034fc0 已落地内容，列出缺口清单再动手；候补项（以实际审计为准）：
reconnect multiplier/max/jitter/stable-reset、connect/login/spawn/stop 分阶段
timeout。全部有具名确定性回归。

## 四、gate b 预备（MIG-04，验收由验收侧执行）

1. **137 测试映射总表**：把散在各车道日志的映射记录并成单一对照表
   （体例照 进度日志-B6.md 的 21 条台账），逐条给 Rust 测试锚点或标注
   「由 X 组件测试覆盖」；验收侧将逐条抽查。
2. **假模型纵向脚本**：可一键起 Paper 26.1.2（supplies/paper，offline 模式）+
   Rust 全栈 + 假 provider 的运行入口与判定标准（进入世界、指名聊天唤醒、
   五工具往返、v5 帧、正常停机）。
3. 实服运行数据不入库（CLAUDE.md 纪律）；server-run 目录维持 gitignore。

## 通用约束

- 文件规模纪律照更新-17；新待决照常走台账 NEW-16 起。
- 每切片进度日志 + 门禁数字 + 可恢复变异证据的既有纪律不变。
- 完成定义：以上四项全部落地且验收侧独立复跑通过后，进入 gate b 正式验收。
