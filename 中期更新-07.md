# 中期更新 07（2026-08-03）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-06，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## NEW-08 裁定：A（维护者 2026-08-03 批，第三档）

directed 的 `why` 闭合枚举增补第五个值 **`out_of_world`**：坐标低于当前维度 `min_y` 或不低于 `min_y + height` 时使用。

- 该行只返回 `at` 与 `why`；**绝不返回目标内容**（与其他 unseen 行同律）。
- 若几何上同时满足 `outside_fov`/`too_far`，按既定稳定顺序叠加。
- 不因单个越界坐标拒绝整批调用（保持逐坐标应答设计）；输入校验层不拦截越界 Y——它是合法问题，答案是诚实的 `out_of_world`。
- 内核 `OutOfWorld` 分支直接序列化为此值，禁止映射到 `chunk_not_loaded`（更新-06 的语义诚实纪律）。
- 更新-06 §4 的 `why` 枚举定义随本条修订为五值；directed 发布解除 NEW-08 阻塞，回归按台账「裁定前的安全处理」所列补齐。

## 台账

NEW-08 已裁定；后续新增待决从 NEW-09 起。
