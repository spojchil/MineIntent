# 中期更新 03（2026-08-02）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01、02，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## 决策权分档（维护者 2026-08-02 批）

任务书 §5「拿不准就登记待决」的总纪律细化为三档。下放改变的是**谁先决定**，不是**是否记录**：每个下放决定都必须带理由和锚点落盘，没写理由的选择仍算待决。

### 第一档：代理自决（决定 + 落盘，不停灯）

凡答案能被 oracle（TS 代码/测试）、任务书、既有裁定或前序中期更新**唯一推出**的，不再登记等批——直接决定，并在进度日志记下：结论、唯一决定它的锚点（文件:行 / 裁定编号）、一句理由。

判据一句话：**能引用唯一决定它的锚点就做；有两种读法都活着，就升第二档。**

判例（更新 02 那批）：NEW-02（TS `config.ts:12,36` 唯一确定寻址）、NEW-05（更新 01 §1 已裁）属于本档——当时停下等批是纯延迟。

### 第二档：验收侧裁定（登记后继续，不等维护者）

内部工程契约选择——同时满足：不改产品语义、模型不可见、未发版可逆——登记进「需要决策的新问题.md」（NEW-06 起编号）后**继续做不依赖它的工作**，由验收侧核证 oracle 后裁定，裁定写进中期更新即生效。维护者通过读中期更新保留否决权；翻案也走中期更新。

判例：NEW-01（v4 字段名）、NEW-03（事件 v2 收敛）、NEW-04（模板制品）属于本档。

### 第三档：维护者保留（登记，绕开，等批）

以下任何一条沾边即入本档，登记进「需要决策的新问题.md」并绕开继续其他工作：

- 产品语义（产品.md 领地的任何增改）；
- 模型可见的行为变化（提示词内容、模型可见 schema、工具面语义）；
- 发布范围与完成门槛；
- 数据不可逆操作（迁移格式，即使有 M09 备份）；
- 对外发布、账号、密钥。

### 冲突规则

档位判断本身拿不准 → 按更高档处理。第一档决定被后续裁定推翻时不算过错，改掉即可——这正是落盘锚点的用途。
