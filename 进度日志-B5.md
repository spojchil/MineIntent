# B5：backend → Information 生产 adapters

日期：2026-08-03
分支：`port/b5-information-adapters`
基线：`main 825ac19`

## 范围与所有权

- 只新增/修改 `crates/middle/**` 与本日志；测试放在 `crates/middle/tests/information_adapters.rs`，没有改 contracts、backend、root manifest/lock、总进度、决策台账、既有日志、`supplies/**` 或 `移植计划/**`。
- 新增 `middle::participant::information_adapters`，公开 `BackendSelfVitalsPort`、`BackendInventoryPort`、`BackendPerceptionPort`、`SoundHistory`、`BackendInformationScopeSource` 和 `BackendInformationAdapterBundle`。
- adapters 只持有冻结的 `Arc<dyn MinecraftBackendApi>`；没有访问 `RuntimeHandle`、Azalea 或 backend 内部类型；没有实现 Participant Runtime、wake policy、light/effects producer、真实 producer 或 app wiring。

## TS oracle 对账与 v2 字段映射

已完整读取 `supplies/mineintent-main/src/participant/information-adapters.ts` 与 `.test.ts`。TS 测试的四组可观察语义均以 strict v2 Rust contracts 为输入重新对账：

| oracle 语义 | Rust v2 → Information 映射 |
| --- | --- |
| vitals 直接读 snapshot | `self_snapshot.health/food/food_saturation/oxygen/experience/effects` 逐字段映射；`ExperienceSnapshot` 的整数 level/total 转 source-port `f64`，progress 原值；effect name/amplifier/duration 原字段保留 |
| inventory 直接读 snapshot | `selected_hotbar_slot` 和 slots 逐字段映射；slot/count/metadata/durability_used 转既有 source-port `f64`，不造空槽、不 clamp、不 wrap |
| perception pose/revision | 每次方法调用重新取得 `observation_source()`；pose 的 position/yaw/pitch 保持弧度；revision 来自同次当前 snapshot 的 `snapshot_revision` |
| block mapping | Loaded name 原样；`air`/`cave_air`/`void_air` 为 `visible=false`；`occludes=visible && !transparent_hint`；Unloaded/OutOfWorld 都是无身份 `Unloaded` |
| nearby entities | 读取当前 observation source 的 entity snapshots；snapshot 可读时按 `self_snapshot.entity_key` 排除 self，保留 type/name/username/position/width/height，不按 username 或数组位置猜测；snapshot 不可读时沿 TS try/catch fallback，不凭未知 key 排除任何 entity |
| sound record | 只接受 v2 `kind=Sound` 且 payload 为 `BackendEventPayload::Sound`；事件 envelope 的 process session/epoch/world/dimension/occurred_at 原样入 history；self pose 取回调期间的 snapshot，计算 distance/relative bearing；可选 name/category、volume/pitch 保真 |
| sound query | 最多 20 条；成功 record 后 revision 才递增；live snapshot 过滤 process session、epoch、world；事件 dimension 缺席为通配，存在则必须相等；结果新→旧并遵守 limit |
| scope capture | ready/dead→Play，connecting/logging_in/spawning→Connecting，其余→Disconnected；只有上述五种状态保留 epoch，其余为 0；可读 snapshot 时带 world/dimension；process session 来自构造参数，ui revision 为 0 |

TS 旧 oracle 的版本/旧命名没有扩展 wire：输入严格采用现有 v2 `BackendEventEnvelope`/`ProtocolSoundPayload`/`MinecraftSnapshotV1`，保留 TS 可观察行为。

## 错误与数值映射

- source-port trait 没有 `Result`，snapshot/observation backend 错误映射为稳定 panic 文本 `information adapter backend snapshot unavailable` 或 `information adapter backend observation unavailable`，由 Information runtime/provider 的 panic boundary 隔离；不会用默认健康、库存、实体或世界事实代替 not-ready。
- `SoundHistory` 在 callback 读取 snapshot 失败时直接忽略该事件，revision 不变；scope snapshot 失败时只省略 world/dimension，连接状态/构造的 process session 仍可捕获。
- source-port DTO 要求 finite 数值：非 finite pose/vitals/entity/sound 数值以稳定 numeric panic 拒绝，不静默传播 NaN/Infinity；可精确表达的整数才转换到 `f64`，不可精确表达的 `u64` 以稳定 panic 拒绝，避免无声精度损失。
- `nearby_entities` 在 snapshot 不可用时保留 TS try/catch fallback：self key 视为未知但仍读取 observation source；observation source 读取失败仍以稳定 observation panic 失败。snapshot 可用时严格按 self `entity_key` 排除。
- 注入的 `InformationClock` 使用既有 UTC formatter；越界时稳定失败，正常生产路径使用 `SystemInformationClock`，测试不依赖 sleep。

## 并发所有权

- `SoundHistory` 内部为 `Arc<SoundHistoryInner>`；backend listener 只持有 `Weak`，避免 backend subscription/listener/history 强引用环。
- history 状态由 `Mutex` 保护。callback 只在锁内检查 disposed、最终提交 entry/revision；backend snapshot、subscription unsubscribe 和所有可能重入的代码均在锁外。
- callback 在 snapshot 前和提交前都检查 disposed，dispose 先在线性化点标记 disposed，再锁外取出并退订；因此在途 callback 只能在 dispose 线性化前提交成功。
- listener 的整个处理和退订调用均有 `catch_unwind` 隔离；重入 callback、callback panic、dispose 与在途 callback 不会把锁带入 backend 或形成死锁。

## 测试与变异证据

新增机械 oracle 与补充测试：

- `oracle_vitals_and_inventory_are_snapshot_lossless_and_not_ready_is_not_defaulted`
- `oracle_perception_maps_current_pose_revision_blocks_and_self_entity_key`
- `oracle_scope_maps_status_epoch_world_and_deterministic_utc`
- `oracle_sound_history_maps_pose_order_capacity_and_dimension_wildcard`
- `sound_history_ignores_non_sound_and_snapshot_unavailable_without_revision`
- `sound_history_dispose_drop_and_callback_panic_are_safe`
- `sound_history_reentrant_snapshot_and_dispose_race_have_completion_signals`
- `perception_snapshot_unavailable_keeps_ts_nearby_fallback_and_rejects_nonfinite_inputs`
- `bundle_constructs_all_adapters_and_uses_injected_clock`

覆盖了 20 条容量与第 21 条淘汰、limit/order、跨 process session/epoch/world/dimension 过滤、live snapshot scope 切换、dimension 缺席通配、snapshot 不可用不记录、nearby snapshot-not-ready 的 TS fallback、dispose/Drop/重复 dispose、unsubscribe panic、callback panic、锁外 snapshot 重入及其顺序、在途 callback 竞态、OutOfWorld 无内容泄露、air/cave_air/void_air、transparent block、数值边界和确定性 UTC。并发测试均用 channel completion signal 与 250ms `recv_timeout`，无 sleep。

Mutation：临时把 dimension 缺席通配的 `||` 改成错误的 `&&`，运行具名测试 `oracle_sound_history_maps_pose_order_capacity_and_dimension_wildcard`，实际失败（`left: 0, right: 3`）；随后恢复 `||`，同名测试恢复通过（1 passed）。

定向结果：`cargo test -p mineintent-middle --test information_adapters --locked --offline -- --nocapture` 为 9/9，通过；删除 production 内嵌套件后 `cargo test -p mineintent-middle --lib --locked --offline` 为 5/5，通过。`--nocapture` 中可见的 panic 文本来自被测试的 catch boundary，测试结果仍为全绿。

nearby snapshot-not-ready 结论：本次没有发现比任务书“按 snapshot self key 排除”更高优先级、要求 fail-closed 的证据；该要求在 key 可取得时继续满足，而 TS oracle 明确规定 key 取得失败仍读 observation source。因此恢复并保留 TS 可观察 fallback，并用 `perception_snapshot_unavailable_keeps_ts_nearby_fallback_and_rejects_nonfinite_inputs` 固化。此前短暂的 fail-closed 实现及其 panic 测试已删除，不再在日志宣称相反语义。

重复声明核验：用 `rg -n` 检查 `pub mod participant`、`format_utc_millis` re-export，以及本批六个 public adapter/bundle 类型和其 public constructor/getter/dispose/recent/revision/capture；每项均只出现一个定义，两个管理侧发现的重复声明均已各删除一行。production adapter 中的第二套 `#[cfg(test)]` FakeBackend/行为套件已删除，仅保留 `crates/middle/tests/information_adapters.rs` integration 权威套件。`git diff --check` 同时通过。

## 门禁与环境说明

worktree 的 `.cargo/config.toml` 使用相对 `vendor`；最终验证时该路径为指向主工作区只读 vendor 的 junction，root 配置、manifest、lock 均未改动。此前命令级绝对 vendor 覆盖也只作为定向编译排障手段，未写入仓库配置。最终精确门禁结果：

- `cargo test --workspace --all-targets --locked --offline`：通过，exit 0。
- `cargo check --workspace --all-targets --locked --offline`：通过，exit 0。
- `cargo fmt --all -- --check`：通过，exit 0。
- `cargo +stable fmt --all -- --check`：通过，exit 0。
- `git diff --check`：通过，exit 0。

## 明确 deferred

- Participant Runtime、Participant 装配/唤醒策略、effects/light 与真实 sound/observation producers、backend/Azalea wiring、app composition root 均 deferred。
- 本批不扩大现有 wire/DTO，不重做 viewport kernel，也不把 scope/history 逻辑接入尚未冻结的 Participant Runtime。
