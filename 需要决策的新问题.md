# 需要决策的新问题

> **状态（2026-08-02）：NEW-01～NEW-05 已裁定，见 `中期更新-02.md` §1；NEW-06 已裁定（A + 排水循环细化），见 `中期更新-04.md`；NEW-07 已裁定（1A+2A、BlockInfo 统一表示、DTO 冻结、prompt v2），见 `中期更新-06.md`；NEW-08 已裁定（A：枚举增补 out_of_world），见 `中期更新-07.md`。**
> 本文件保留为升级台账，后续新增待决从 NEW-09 起继续登记。
>
> 更新日期：2026-08-02  
> 性质：相对 `移植计划/待决问题.md` 的增量清单；原文件保持不动。  
> 原则：这里只收录当前代码与上游任务仍无法唯一推出、且不同选择会改变公开契约或运行行为的问题。

## 优先级概览

| 编号 | 问题 | 优先级 | 当前安全状态 | 阻塞范围 |
| --- | --- | --- | --- | --- |
| NEW-01 | Agent Context v4 的精确线格式 | P0 | 暂不发布 v4 | Context v4 契约、fixture、最终提示词组合器 |
| NEW-02 | 单文本记忆文件的最终寻址规则 | P1 | `MemoryStore` 仅接收调用方给定目录 | 应用装配、旧数据迁移入口 |
| NEW-03 | Backend Event v1 到严格 v2 的原子收敛方式 | P0 | 生产端继续发 v1 | 生产端切换 v2、事件边界验收 |
| NEW-04 | 提示词模板的制品与版本选择规则 | P1 | 尚未接最终组合器 | 最终提示词可复现性、部署配置 |
| NEW-05 | 是否启用同轮工具并行与资源预占 | P2 | 当前严格顺序执行 | 迁移后性能优化及行为兼容性 |
| NEW-06 | 同步 observation callback 的事件重入顺序与死锁 | P1 | A 已实现并通过组合门禁 | 无（已收口） |
| NEW-07 | 轮末视口帧与 directed 结果的精确模型 wire | P0 | frame、BlockInfo、directed DTO/kernel 与 prompt v2 已实现；生产装配未接 | 生产 ToolDispatcher/view capability/真实 sampler/app、模型行为验收 |
| NEW-08 | directed 坐标处于维度高度范围外时的 unseen reason | P0 | kernel 保留独立 `OutOfWorld` 错误且绝不伪装；生产 directed capability 未发布 | 对外 directed capability、批量结果 |

---

## NEW-01：Agent Context v4 的精确线格式

### 已知事实

- Issue #127 要求把记忆改为单一纯文本，并把上下文协议从 v3 升到 v4。
- 当前 v3 使用：

  ```json
  {
    "protocol": "mineintent.agent-context.v3",
    "stable": { "memories": {} },
    "frame": {}
  }
  ```

- Issue 没有规定 v4 中单文本字段的准确名称和必填/null 语义。
- 这属于公开线契约，不能仅凭实现偏好决定。

### 需要决定

1. v4 是否固定为 `protocol = "mineintent.agent-context.v4"`。
2. `stable` 中采用哪种结构：
   - **A（建议）**：`"stable": { "memory": "..." }`
   - B：`"stable": { "memories": "..." }`
   - C：`"stable": "..."`
3. 建议同时确认字段语义：字段必填、只接受字符串、允许空字符串、不接受显式 `null`、未知字段继续拒绝。

### 当前建议

选择 A。它保留 `stable/frame` 分层，同时避免用复数名表示单个字符串。

### 未决定前的处理

不新增或发布猜测性的 v4 类型；通用 Agent 循环和驱动不依赖此选择，可以保留。

---

## NEW-02：单文本记忆文件的最终寻址规则

### 已知事实

- Issue #127 建议路径为 `.mineintent/memory.md`，但没有明确这是固定的 CWD 相对路径，还是默认数据目录展开后的结果。
- 当前 `MemoryStore` 已使用文件名 `memory.md`，由上层传入目录。
- 旧 `memories.json` 的一次性迁移也需要与新文件共享明确的数据目录。

### 需要决定

- **A（建议）**：`<MINEINTENT_DATA_DIR>/memory.md`；数据目录默认 `.mineintent`，因此默认结果正好是 `.mineintent/memory.md`。旧文件对应 `<MINEINTENT_DATA_DIR>/memories.json`。
- B：始终使用 `<cwd>/.mineintent/memory.md`，忽略已配置的数据目录。
- C：再增加一个记忆专用路径配置，并规定它与数据目录的优先级。

还需确认相对 `MINEINTENT_DATA_DIR` 是相对进程 CWD，还是启动配置文件所在目录解析。

### 当前建议

选择 A，并规定相对数据目录按进程启动 CWD 解析。这样默认路径符合 Issue，同时不会绕过现有数据目录配置。

### 未决定前的处理

保持 `MemoryStore` 为“调用方传目录”的中层组件，不在应用装配层硬编码路径。

---

## NEW-03：Backend Event v1 到严格 v2 的原子收敛方式

### 已知事实

- 合约层已经定义严格的 `mineintent.minecraft.backend-event.v2`。
- 生产 backend 仍发送 v1，而且 v1 还包含 `keep_alive`、`motor`、`error` 等 v2 当前没有的 kind。
- v2 对 lifecycle payload 也有严格结构；仅替换 discriminator 会形成“名称是 v2、内容仍是 v1”的伪升级。
- 当前保留 v1 是安全状态，不代表 v2 迁移已经完成。

### 需要决定

- **A**：把 `keep_alive`、`motor`、`error` 纳入 v2，并为它们冻结严格 payload。
- **B（建议）**：v2 只承载产品事实事件；`keep_alive`、命令执行/电机反馈和内部错误进入独立诊断或命令结果通道。生产 lifecycle/self/world 等事件一次性适配到严格 v2。
- C：继续保留 v1，直到将来另立任务完成完整适配；不得提前宣称 v2 已接通。

选择 A 或 B 时还需一起冻结：

1. 生产 `self_state` 对应 v2 的 `self` 时，准确 kind 名称是否改为 `self`。
2. 每个 lifecycle 分支的准确 payload，尤其 reconnect、closed、faulted、respawn 和 dimension change。
3. v1/v2 是否允许短暂双发；若允许，去重键和终止窗口是什么。

### 当前建议

选择 B，并以单个原子适配提交切换；不要只改版本字符串，也不建议长期双发。

### 未决定前的处理

生产端继续明确发送 v1；可以完善 v2 类型和适配测试，但不能切换 discriminator。

---

## NEW-04：提示词模板的制品与版本选择规则

### 已知事实

- 迁移要求先以当前单一提示词为 oracle，并通过显式 key/version 选择模板。
- 合约已有 `PromptTemplateRef { key, version }`，但文件位置、格式、加载时机与未知版本行为尚未冻结。
- 这不是提示词文案评审，而是模板制品的运行契约。

### 需要决定

1. 模板位于随程序发布的仓库制品，还是位于可运行时编辑的数据目录。
2. 模板是纯文本，还是带 metadata 的结构化文件。
3. 每次 run 读取、进程启动时读取，还是编译期嵌入。
4. 未知 key/version 时是否一律失败关闭，以及默认 key/version 的唯一来源。

可选方案：

- **A（建议）**：仓库内版本化纯文本制品，随二进制嵌入；按 key/version 精确查找，未知项失败关闭。每轮只动态拼入最新 memory/context。
- B：数据目录中的运行时可编辑文件；需额外规定热重载、原子更新、审计和回退。
- C：编译内字符串常量；实现简单，但不满足“外部文件制品”的可审查目标。

### 未决定前的处理

保留模板引用契约，不接最终 Prompt Composer，也不自行发明默认版本回退。

---

## NEW-05：是否启用同轮工具并行与资源预占

### 已知事实

- 当前迁移 oracle 和已实现的 Agent driver 都按模型返回顺序逐个执行工具，严格保持 N-in/N-out。
- 补给实现展示了“按数组顺序预占资源、无冲突调用并行”的可行方向。
- 该优化会改变可观察行为。例如同一轮两个 `move`，顺序执行可能都成功；预占模型可能让后一个立即返回 busy。

### 需要决定

- **A（建议，迁移阶段）**：迁移验收完成前保持严格顺序执行。
- B：现在启用确定性预占和无冲突并行，并接受行为变化。
- C：迁移后通过显式协议/功能开关引入，默认仍顺序执行。

### 当前建议

迁移阶段选择 A；若之后确有延迟压力，再以 C 的方式单独设计、基准测试和发布。该问题目前不阻塞主迁移。

---

## NEW-06：同步 observation callback 的事件重入顺序与死锁

### 新发现的证据

- `SharedRuntime::emit` 与 `begin_connection_attempt` 为保证事件创建及交付顺序，会取得同一个 `event_dispatch: Mutex<()>`，并在持锁期间同步执行 `broadcast_event`（`crates/backend/src/runtime.rs:391-483`）。
- typed listener 的 `on_event` 已确认不在 observation registry 锁内执行，读取 source、读取方块和二次订阅均有回归测试；但执行 callback 时仍持有上述 dispatch 锁（`crates/backend/src/runtime.rs:403-452`）。
- 公开 `RuntimeHandle::stop()` 会同步调用 `initiate_stop()`，后者立即再次进入 `emit()`（`crates/backend/src/runtime.rs:700-715,820-822`）。因此 listener 若持有 handle 并在 `on_event` 中调用 `stop()`，同一线程会再次获取非重入 dispatch mutex，形成确定性自死锁。
- 冻结 `ObservationEventListener` 契约没有声明“callback 内禁止调用公开 runtime 操作”（`crates/contracts/src/minecraft/api.rs:108-110`）。当前 `send_chat`、move、respawn 只是入队，暂不触发该死锁；问题仍会影响 `stop` 及未来新增的任何同步 emit 路径。

这不是已登记的 SEAM-10 背压问题：即使事件量为 1、没有队列溢出，也会发生。它也不同于本轮已经修复的“A listener 退订已预留 B”死锁。

### 决策档位

按 `中期更新-03.md` 属于**第二档（验收侧裁定）**：它是模型不可见、未发版且可逆的内部工程契约，但不同方案会改变嵌套事件的交付顺序，不能静默选择。

### 需要决定

- **A（建议）**：把 dispatch 改为单一 drainer + FIFO 队列。事件创建与 enqueue 在短锁内线性化；callback 在 dispatch/registry 锁外执行。callback 内产生的新事件排到当前事件之后，由同一 drainer 继续交付。这样保留全局 FIFO、避免递归栈和自死锁，但嵌套 `emit` 返回时，新事件可能尚未完成 callback 交付。
- B：允许递归立即 dispatch（重入锁或显式递归）。嵌套事件可在调用返回前交付，但可能让后注册 listener 先收到嵌套事件、后收到触发它的原事件，并引入无界递归深度。
- C：在 contract 中明确禁止 callback 内触发同步 emit，并让 `stop` 等入口检测 callback 上下文后返回结构化错误或强制外部转发。实现较小，但把内部锁限制升级为调用方必须长期遵守的公开约束。

### 当前建议

选择 A。它与已经要求的“callback 不在 registry 锁内”“单 listener panic 隔离”“同 epoch 事件按 emit 顺序交付”最一致，也不会把当前实现细节泄漏成永久 API 禁令。

### 实施状态（2026-08-02）

- 已由 `9005d21` 实现单 drainer + FIFO 排水循环，主线合并提交为 `526104f`。stdout、channel 与 typed callback 均由唯一 drainer 按入队顺序交付，callback 期间不持 dispatch/registry 锁。
- callback 内调用 `RuntimeHandle::stop()`、嵌套两次 emit、40 次 attempt 与 8×32 并发生产者均有精确回归；所有并发完成检查均为有界 channel timeout，不会在旧死锁复发时无限挂测。
- LIFO 变异得到 `event-2,event-4,event-3` 并被嵌套 FIFO 测试捕获，恢复后组合 workspace 301/301 通过；既有 unsubscribe、pending reservation、panic 隔离及 epoch 过滤断言未弱化。

---

## NEW-07：轮末视口帧与 directed 结果的精确模型 wire

### 新发现的证据

- `中期更新-05.md` 已正式裁定：同轮执行过身体工具后，在全部 `function_call_output` 之后追加一份全量视口帧；`view` 改为 `full | directed`。但更新没有冻结追加消息的 role/envelope、采样失败 wire、directed 返回 DTO 或不可见原因的闭合分类。
- 当前 Rust `AgentRun` 只追加 assistant replay 与逐调用 tool message；每个 tool output 固定为 `{result, observationAfter}`（`crates/middle/src/agent/run.rs`）。`AgentFrame` 当前没有 viewport 字段（`crates/contracts/src/agent/context.rs`），因此“追加一帧”不能仅靠搬动现有字段得到唯一实现。
- 上游 Issue #94 明确写着“本 issue 不冻结完整 DTO”；它只给出不可见结果的语义示例。PR #93 的实盘证据只证明 tool message 后紧跟 user message 能被两家模型接受，没有把该 user message 的稳定 wire 冻结成契约。
- 现有 `participant-system/v1.txt` 仍告诉模型“之后每个工具结果都有 result 和 observationAfter”以及“每次模型响应最多调用一个动作类工具，等它返回的新视野再判断”。若身体结果不再逐个带视口，这段模型可见文字需要版本化替换；按 NEW-04，不能静默覆写既有 v1 制品。

该缺口不能由“写法可改”豁免：消息形状、工具结果内容、directed 事实表达和 prompt 文案都会直接改变模型可见行为，按 `中期更新-03.md` 属于**第三档（维护者保留）**。

### 需要决定

1. **轮末帧的消息契约**：
   - **A（建议）**：新增有 discriminator 的 viewport-frame v1 user message，例如 `{protocol, at, viewport}`；它只在全部 tool messages 后出现，不复用 opening `AgentFrame`，也不伪装成某个 tool call 的结果。
   - B：扩展 `AgentFrame` 增加 optional viewport，并在轮末追加一份完整 AgentFrame user message；需同时决定是否重复 status/inventory/sound/events，以及这一帧是否消费 pending events。
   - C：追加无 envelope 的裸 `ViewportProjection`；形状最小，但没有 `at` 与版本锚点，和更新要求模型按时间区分新旧不相容，当前不建议。
2. **逐工具 `observationAfter` 的保留边界**：
   - **A（建议）**：身体工具仍返回动作效果及不含 viewport 的 `observationAfter`（保留伤害、声音等非视觉中断事实）；轮末独立帧只承担 full viewport。显式 `view` 的结果自身返回所选模式数据，纯 say/view 轮不再自动追加。
   - B：身体工具的 `observationAfter` 一律为 null，所有事实都并入轮末帧；这会改变事件 drain 与失败路径语义，需要另行冻结。
3. **directed 输出 DTO**：需冻结坐标类型、批量上限初值、`visible/not_visible` 结果形状、可见空气/方块状态的表达、reason 枚举（至少视锥外/超距/遮挡/未加载）及遮挡物字段。Issue #94 的示例不足以作为严格 serde/schema 契约。
4. **prompt 制品**：确认新增 `participant-system/v2` 并由装配层显式选择，还是另给版本名；同时给出与“轮级一帧 + full/directed”一致的准确文案。v1 保持字节不变用于旧契约回放。

### 当时建议（已由更新 06 采纳）

选择 1A + 2A，并新增 prompt v2；directed DTO 在同一批维护者裁定中给出严格 JSON 示例后冻结。这样不会把独立 resource frame 重新塞回某个工具结果，也能保持非 viewport 中断事实的既有到达路径。

### 裁定前的安全处理（历史）

- 可以先把 `ViewArguments` 的平铺 `mode + positions?` 输入 schema 和跨字段校验落地；不得在生产工具目录发布尚无输出契约的 directed capability。
- 不修改 AgentRun/ToolDispatcher/prompt v1 来猜轮末帧 wire；participant/app 装配继续 deferred。
- A2 的 NEW-06 排水循环及其他 backend 内部任务不依赖本问题，可继续。

参考上游：[MineIntent Issue #94](https://github.com/spojchil/MineIntent/issues/94)、[PR #93](https://github.com/spojchil/MineIntent/pull/93)。

### 实施状态（2026-08-02，组合收口）

- `4f8e7b4` 实现独立 `mineintent.viewport-frame.v1` user message、身体工具批次结束后至多一帧、显式失败帧、资源分类与采样器 seam，并新增显式选择的 `participant-system/v2`；v1 prompt 字节未改。纯 say/view 不采样，取消/deadline 不伪造失败帧。
- `5f17cc7` 实现唯一 `BlockInfo` string/object 表示、初版视觉属性白名单与 presenter hook，升级 full 三类方块槽位，并在 backend 唯一 kernel/runtime 原子 capture 中实现 strict directed DTO、几何零读取短路、exposed-face 可见判定、首遮挡表面及取消/deadline/stale/revision 边界。
- 管理侧复审补齐了 extreme i32 零读取、中心射线假阴性、worker wake operation、空属性对象/重复键、合计 16 项预算及 `Clear + Unloaded` 只能返回 `chunk_not_loaded` 等回归；真实变异均先失败后恢复。
- B2+A3 组合门禁为 workspace **336/336**；all-targets check、默认/+stable rustfmt 与 `git diff --check` 全部通过。
- 当前仓库仍没有生产 `ToolDispatcher`、view capability、真实 `RoundViewportSampler` 适配器或 participant/app composition root；默认 no-sampler 只会显式报告 unavailable。它们属于后续生产装配，不得把本次数据线与 Agent seam 冒称为端到端发布；directed 发布还受 NEW-08 阻塞。

---

## NEW-08：directed 坐标处于维度高度范围外时的 unseen reason

### 新发现的证据

- `中期更新-06.md` 将 `why` 冻结为 `outside_fov | too_far | occluded | chunk_not_loaded`，但 backend 的 `BlockReadResult` 还存在独立且明确的 `OutOfWorld`。
- `OutOfWorld` 表示坐标低于当前维度 `min_y` 或不低于 `min_y + height`；它既不是 chunk 尚未加载，也不必然超出 32 格，不能诚实地映射到四个既有 reason。
- directed 输入是任意 `i32` 世界体素坐标且按坐标拆分结果；当前契约没有允许忽略该坐标、返回无 reason 的 unseen 行，或让单个坏坐标使整批失败。

这会直接改变模型可见的闭合枚举，属于第三档协议补充，不能由实现层把 `OutOfWorld` 偷换成 `chunk_not_loaded`。

### 需要决定

1. **A（建议）**：向闭合枚举增加 `out_of_world`。该行只返回 `at` 与 `why:["out_of_world"]`（若几何上还同时满足 `outside_fov`/`too_far`，按既定稳定顺序允许叠加），绝不返回目标 block 内容。
2. **B**：任一坐标处于世界高度外时，将整个 `view(directed)` 调用作为 invalid request 拒绝。优点是不扩 enum；缺点是一个坐标会使同批其他合法坐标全部失去结果。
3. **C**：映射为 `chunk_not_loaded`。不扩 wire，但语义不真实，当前不建议。

### 裁定前的安全处理

- 继续实现 BlockInfo、full projection 与其他四类 directed 判定。
- kernel 保留 `OutOfWorld` 的独立内部分支；不得序列化成 `chunk_not_loaded`，也不得泄漏目标内容。
- 在生产 directed capability 发布前补上本项裁定及精确回归。

---

## 已冻结、无需重新决策的事项

以下项目已有任务书或代码级 oracle，不应混入上述决策：

- 每次 run 最多 16 次模型请求、每个模型响应最多 8 个工具调用、整轮最多 32 个工具调用。
- 同轮工具结果严格按请求顺序回填，且每个调用恰好产生一个结果。
- 默认 180 秒 deadline；取消优先于同时可见的 deadline。
- provider/tool 的同步及异步 panic 必须被边界拦截，不能击穿 runtime。
- 记忆是单一纯文本，支持追加、唯一 anchor 替换、显式整篇重写及滚动备份。
- Agent 核心直接落在 Rust middle，不增加 TypeScript 中间实现。

## 建议回复格式

可以只回复选项和必要补充，例如：

```text
NEW-01: A；必填，空串允许，null 拒绝
NEW-02: A；相对路径按进程 CWD
NEW-03: B；不双发
NEW-04: A
NEW-05: A，迁移后另议
NEW-06: A
NEW-07: 1A + 2A；directed DTO 按补充 JSON 冻结；prompt 新增 v2
```

参考上游：[MineIntent Issue #127](https://github.com/spojchil/MineIntent/issues/127)
