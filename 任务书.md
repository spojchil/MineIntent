# 离线长程任务书：MineIntent 自有 Minecraft 协议后端（Rust，目标 26.1）

> 临时文档，不随 PR 提交。写给一个长时间自治工作、不实时回复的 AI 处理源。
> 产品权威只有一个：仓库根 `产品.md`。本任务书只描述工程目标，不含任何产品裁决权。
> 起草：2026-07-31，基于当日检索证据（见「已核实事实」）。

## 一句话

在独立代码库中，用 Rust 实现一个面向 Minecraft Java 26.1 的最小客户端后端，
语义对齐 MineIntent 现有的 Backend 契约（`src/minecraft/contracts.ts` 的
`MinecraftBackendApi`：snapshot / subscribe / motor / observationSource），
最终作为主线摆脱 TypeScript/Mineflayer 依赖、迁移到 26.1 的载体。

## 背景（为什么做）

- MineIntent 是原版 Minecraft 的具身 AI 参与者。当前后端用 Mineflayer（钉 1.21.1），
  它的高层聚合替产品做了不该做的决定（自动重生、乐观本地更新等），审计与 Issue #118 有记录。
- Minecraft 从 26.1 起完全取消代码混淆——第一方源码可读，协议语义和客户端物理
  可以直接对照官方实现，而不是二手逆向。这改变了自研后端的风险结构。
- 项目产品判断（已确认）要求：动作以实际游戏结果判定成败（W03）；工具/运行时不得
  向 AI 虚报世界状态（W04c）；感知与身体的每个决定要有理由和记录（W09）。
  自有薄后端的意义就是把这些边界握在自己手里。

## 已核实事实（2026-07-31 检索，动手前在 M0 重新核实）

- 26.1「Tiny Takeover」2026-03-24 发布，首个完全去混淆的正式版，版本格式 year.drop.hotfix；
  已有 hotfix 至 26.1.2。26.2 已发布。
  （minecraft.wiki/w/Java_Edition_26.1；minecraft.net/en-us/article/removing-obfuscation-in-java-edition）
- 26.1 协议号 775（go-mclib/protocol 以 @775 提供 26.1 绑定）。
- Paper 已支持 26.1.x（存在 26.1.2 构建线；注意：世界升到 26.1 后不可降级，实验世界要备份）。
  （papermc.io/news/26-1）
- Rust 库 azalea（azalea-rs/azalea）：活跃、带客户端物理与寻路，单版本支持。
  **crates.io 发布版为 0.16.0+mc26.1（目标 26.1，仓库提交 a253702e）**；
  26.2（协议号 776）只在未发布的 git 主线上——这是默认钉 26.1 的主要理由。
- Go 侧 go-mc 慢一个版本、走 master；go-mclib/protocol 只有裸协议。综合看 Rust+azalea
  起点最好，但 M0 需给出带证据的最终选型（包括「直接用 azalea 分层」还是
  「只用其协议/物理 crate、上层自写」）。

## 目标

1. 一个独立 Rust 代码库（不放进 MineIntent 主仓库；主仓库只加集成点），实现最小后端：
   连接/登录 → 聊天收发 → 世界快照与实体跟踪 → 按键式移动输入与物理 → 视口观察。
2. 语义对齐 `MinecraftBackendApi`（对齐指行为语义，不要求逐字段照抄 TS 类型）；
   跨进程边界用版本化协议（参考主仓库 tool-bridge 的信封风格：protocol 字段 + 严格键校验）。
3. 明确区分三类事实并在数据上可检查：`commanded`（我们发出的指令）/
   `client_predicted`（客户端物理预测）/ `server_observed`（服务端确认）。
   预测永远不得被标成世界事实（对应 W04c）。
4. 对齐目标的准确表述是**钉协议号**。默认钉 **775（26.1.x）**。维护者的真实约束是
   「去混淆时代」（≥26.1，协议号 ≥775），在此范围内选库支持最好的——2026-07-31 核实：
   azalea 的 crates.io 发布版是 0.16.0+mc26.1，26.2 只在未发布 git 主线；对无人盯守的
   长任务，已发布、有版本号的依赖优于主线快照。若实测发现 0.16.0 有硬缺陷，可切到
   本地 azalea 克隆的主线（26.2/776，全套物料已备）并记录理由；
   要跳出 ≥775 范围才需交维护者裁决。
   显示版本「26.x」只是人类标签，客户端在握手包里报的、服务器校验的都是协议号；
   hotfix 通常共享协议号。M0 用 server list ping 向 Paper 实测确认
   （返回 JSON 的 `version.protocol` 字段即权威值）。快照版本协议号带高位标志，一律拒连。
   注意协议号只管线上格式：存档与 `/data` 输出按 **DataVersion**，1.20.5 起大量注册表在
   configuration 阶段运行时同步——M3/M4 解析服务端真值时按这两者来，不要都归给协议号。

## 非目标（越界即停）

- 不做多版本兼容、不做完整客户端（渲染/GUI/容器/制作/挖掘）、不做通用协议框架。
- 不改 MineIntent 主仓库的 `src/participant/`、agent-service、产品.md、docs（集成点除外，且集成走 PR）。
- 不替产品做任何未决决定：遇到 产品.md 未覆盖的问题（例如某个协议行为要不要暴露给 AI），
  在进度日志里登记「待维护者裁决」，选择最保守实现继续，不得静默定案（G05）。
- 不重写认证/加密/压缩/编解码基础设施——能用 azalea 现成 crate 的就用。

## 里程碑（每级一个可交付物，逐级验收，不许跳级）

- **M0 钉版本与选型**：锁定协议号（默认 775 / 26.1.x，用 server list ping 向
  supplies/paper/26.1.2 实测确认）与对应 DataVersion；确认 Paper 对应构建可跑通；
  锁定 azalea 的对应 release（或替代选型），列出它替你处理与自动执行的行为清单
  （特别排查：自动重生、自动确认传送、乐观方块更新、keep-alive 之外的任何主动行为）；
  获取官方去混淆 jar 并确认可反编译对照；读一遍 EULA 关于源码阅读/衍生的措辞并摘录。
  交付：`M0-版本与选型.md`，所有结论带链接或文件锚点。
- **M1 连接登录**：offline 模式进入本地 Paper 26.1 服务器，稳定在线 30 分钟，
  正确响应 keep-alive；断开重连有事件。交付：可运行示例 + 服务端日志证据。
- **M2 聊天**：收发公屏聊天；收到的每条消息带发送者、时间、原文。
- **M3 快照与实体**：位置/生命/饥饿/在线玩家列表/被跟踪实体的快照，
  与服务端控制台查询（`/data get`、`/tp` 查询等）抽样一致。
- **M4 移动与物理**：按键式输入（前后左右/跳跃/潜行，与主仓库 motor 语义对齐），
  物理对照官方源码移植；验收：固定输入序列的落点与服务端实测坐标误差在方块级一致，
  并显式区分 predicted 与 server_observed 两条轨迹。
- **M5 视口**：可见方块/实体的观察读取，语义对齐主仓库 viewport（绝对坐标、不穿墙）。
- **M6 场景验收**：把主仓库 `docs/guides/validation.md` 与 paper-ci 场景在 26.1 服务器上
  重演：死亡/重生、维度切换、断线重连、双人对称（互换玩家名结果不变，对应 R01 对称性测试）。

## 验收机制（三路证据，主仓库 scripts/live-observer.mjs 是先例）

1. 服务端控制台/数据命令 = 独立真值（坐标、方块、实体）；
2. 后端自身事件流 = 它声称发生了什么；
3. 观察者视角（可另起一个最小客户端或用主仓库 live-observer 模式）= 玩家能看到什么。
   三路互相对不上就是缺陷，修到对上为止。官方去混淆源码作为语义 oracle：
   凡与源码行为不一致处，以源码为准并在日志里记录出处（类/方法名）。

## 停止条件（到了就停，停下来写报告；停止不是失败，是证据）

- 在 M2 之前就发现必须实现容器/制作/完整客户端才能推进；
- 必须大段复制而非引用 azalea 的聚合层才能达到里程碑；
- 官方源码对照发现钉定版本存在无法绕过的协议级阻碍；
- 任何一级里程碑连续两周无可验证进展。

## 网络策略：离线为常态

本任务在**无网络**环境执行。所有外部物料由维护者按下方清单预置；任务中缺料时，
在进度日志的「补给申请」节登记（写明物件名、精确版本、来源 URL、用途），
由维护者在窗口期人工取回放入 `supplies/` 目录，不因缺料而擅自降低验收标准。

### 开工前物料（已由维护者一侧预置完毕）

物料的权威清单与校验方式见 **`supplies/MANIFEST.md`**——以它为准，此处只列类别：
工具链（Rust stable+nightly、JDK 21、git）、Rust 依赖 vendor（`vendor/` + `.cargo/config.toml`，
azalea 0.16.0+mc26.1）、Paper 26.1.2 与官方 26.1.2 双 jar（26.2 整套为备选）、
Paper 首启缓存、Vineflower 反编译器与预反编译源码树、azalea 全历史克隆、
MineIntent 主仓库只读克隆、EULA 存档。
已知缺料：minecraft.wiki 协议页快照（Cloudflare 拦截脚本抓取），已登记在
进度日志「补给申请」，非阻塞——反编译源码是主 oracle。

## 工作纪律

- **独立 git 仓库**（例如与主仓库平级的 `MineIntent-backend-rs/` 目录），不是 MineIntent
  的 worktree——你对 MineIntent 主仓库只读，任何提交都发生在你自己的仓库里。
- 离线无远端：交付形态 = 本地里程碑分支 + tag + 进度日志。维护者验收时从你的目录
  `git fetch` 审阅；合入 MineIntent 的集成点由维护者一侧另行走主仓库 PR 流程。
- 维护 `进度日志.md`：日期、做了什么、证据锚点、待裁决登记、下一步。
  凡写「完成」必须同一行给出验证证据；测试通过不等于行为验证（主仓库 CLAUDE.md 同款纪律）。
- 注释与文档一律中文（技术名词保留原文）。
- 不提交任何密钥、令牌、世界存档、未脱敏日志。
