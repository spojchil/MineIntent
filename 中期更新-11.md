# 中期更新 11（2026-08-03）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-10，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## 唤醒路径统一裁定（维护者 2026-08-03 批，第三档）

1. **取消独立的聊天唤醒路径**。participant runtime 的 run 触发架构统一为**事件驱动**：所有事件（backend 九 kind 及内部事件）进同一条唤醒评估管线，聊天是其中一种事件。TS oracle 的「唯一模型路径 = 指名玩家聊天」（`src/participant/runtime.ts:74`）不再作为架构，改为**初始唤醒政策**。
2. **初始政策表恰一条：指名玩家聊天唤醒**。可观察行为与 oracle 等价（不夹带新唤醒源）；架构按豁免规则直达新形态，裁定锚点即本更新。相关 TS 行为测试在归类表登记「架构豁免、行为等价保留」。
3. **唤醒政策是显式注册点**（同帧段注册表、BlockInfo 呈现钩子的模式）：条目 =（事件类型 → 唤醒判定），增删条目走登记。移植后 W08a 关注批次扩展（伤害/呼名/靠近等）时只加条目、不动架构。
4. **触发与呈现分离**：本裁定只统一触发管线。聊天内容呈现仍走 v5 帧 chat 段（NEW-10 冻结稿）；事件呈现仍走开场帧 `events[]` 与轮内 `observationAfter`（NEW-07 2A）。v5 帧对唤醒源天然中立：非聊天唤醒的 run，chat 段缺席、事件在 events[] ——无需 wire 改动。
5. **已移植的 speech 调度逻辑重新定位**：作为唤醒政策「聊天条目」的实现挪位，不重写；其行为测试（指名判定、调度语义、waitUntil 纪律）继续有效。
6. **运行中注入与强制打断不在本批**：运行中事件走既裁的边界注入形态（轮末帧/observationAfter）；打断政策与打断后重入语义留 W08a 批次另裁（机制先例已由维护者查证归档：Codex `Op::UserInput` 队列→下次请求前排为独立 user message；interrupt 留部分痕迹接续）。
