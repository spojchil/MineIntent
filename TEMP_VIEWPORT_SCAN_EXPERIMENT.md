# 临时实验记录：候选方块扫描与屏幕 DDA 射线

> 临时工作记录，创建于 2026-07-25。不要把本文件作为已接受的架构设计，也不要随 PR 提交；完成讨论后删除。

## 1. 实验问题

比较下面两种生成 `visibleBlocks` 的方法，在当前 `65 × 65 × 41`、最远 32 格视野范围内的计算规模和观察差异：

1. **当前方法：候选方块驱动。**遍历整个候选长方体，对通过距离与视锥筛选的方块读取类型、检查暴露邻面，再从眼睛向方块中心进行遮挡验证。当前射线每隔 0.25 格调用一次 `blockAt`。
2. **备选方法：屏幕射线驱动。**在一个低分辨率虚拟屏幕上生成透视射线，每条射线使用 3D DDA（Amanatides–Woo 风格）依次穿过体素；记录沿途可见但不遮挡的方块，遇到第一个遮挡方块后停止。

这里同时加入一个控制组：保留当前候选方块算法，只把内部的 0.25 格射线替换成 DDA。这样可以区分“DDA 遍历优化”和“改成屏幕采样”分别带来了什么。

## 2. 计算口径

计算使用 `refactor/agent-architecture` 工作树在本次记录时的参数：

- 扫描半径：水平 32，垂直 20；
- 候选长方体：`65 × 65 × 41 = 173,225` 个体素；
- 最远观察距离：32 格；
- 垂直 FOV：70°；
- 假定宽高比：16:9，对应水平 FOV 约 102.45°；
- 相机位置：玩家位置上方 1.62 格；
- 相机姿态：水平朝前（pitch 0）；
- 方块只使用当前实现已有的 `visible`、`occludes` 两个布尔光学属性；
- 不计算实体 AABB、GUI 遮挡、部分方块 shape、半透明衰减和未加载区块；
- 屏幕射线在遇到遮挡方块前继续穿过透明方块，因此不是“只保留每条射线的最终一个方块”；
- 表中的“原始读取”是不加缓存时的 `blockAt` 调用数；“唯一体素”是增加一次 Read 内部体素缓存后，理论上需要进行的不同坐标读取数。

这是确定性的合成计算，不是真实 Mineflayer backend 的耗时基准。实际耗时仍会受到区块查找、方块对象构造、registry 查询和 JavaScript 调度影响。

## 3. 基础规模

当前姿态下，在 173,225 个候选体素中心中：

- 通过 32 格球形距离和矩形视锥筛选的中心约有 **20,280** 个；
- 占整个候选长方体的 **11.71%**；
- 但是当前算法仍然会对全部 173,225 项执行坐标构造、距离和视锥计算。

当前 0.25 格采样射线走满 32 格时约执行 128 次 `blockAt`。同一条 32 格射线使用 DDA 时，在当前视锥中的平均体素访问量约为 **49.8**，理论上大约不超过 59。单看射线遍历，DDA 将走满的单条射线减少到当前读取量的约 39%。

## 4. 合成场景

- **空气视野**：范围内全是空气，用来测量没有任何候选进入邻面和遮挡阶段时的成本。
- **开阔平地**：`y <= 63` 全是完整不透明方块，玩家站在 `y = 64`。
- **前方 8 格墙**：`z = -8` 是一面覆盖视锥的不透明墙，其余为空气。
- **全透明体**：范围内每个体素都 `visible=true, occludes=false`。这不是常见世界，但会迫使当前算法为每个候选把射线走到终点，可用作路径性最坏夹具。
- **全实心夹具**：包括相机所在体素在内全部不透明，只用于压力计算，不代表合法玩家场景。

## 5. 主要数据

每个数据为“原始 `blockAt` 调用 / 本轮不同坐标数”。输出上限 256 不会降低当前候选扫描的这些成本，因为裁剪发生在全部候选处理完成以后。

| 方法 | 固定工作量 | 空气视野 | 开阔平地 | 前方 8 格墙 | 全透明体 |
|---|---:|---:|---:|---:|---:|
| 当前候选扫描 + 0.25 采样 | 173,225 次候选循环 | 20,280 / 20,280 | 129,534 / 21,962 | 28,436 / 20,490 | 1,997,348 / 22,058 |
| 屏幕 DDA 32×18 | 576 条射线 | 28,702 / 14,721 | 18,878 / 8,531 | 8,788 / 779 | 28,702 / 14,721 |
| 屏幕 DDA 48×27 | 1,296 条射线 | 64,494 / 20,523 | 42,528 / 11,915 | 19,800 / 779 | 64,494 / 20,523 |
| 屏幕 DDA 64×36 | 2,304 条射线 | 114,628 / 22,261 | 75,464 / 12,879 | 35,360 / 793 | 114,628 / 22,261 |
| 屏幕 DDA 80×45 | 3,600 条射线 | 179,286 / 22,623 | 118,174 / 13,123 | 55,060 / 793 | 179,286 / 22,623 |
| 屏幕 DDA 96×54 | 5,184 条射线 | 258,014 / 22,775 | 169,820 / 13,239 | 79,296 / 793 | 258,014 / 22,775 |

全实心压力夹具中，当前候选算法执行 141,960 次 `blockAt`，因为每个通过视锥的实心候选都继续检查六个实心邻居。屏幕 DDA 会在每条射线的第一个体素停止，所以调用数分别等于射线数；但相机位于实心方块内并不是合法游戏状态，这组数字只能说明早期遮挡对射线算法的影响。

### 5.1 不能只看 `blockAt` 数

空气场景中，当前算法的原始 `blockAt` 数低于 64×36 和 80×45 屏幕射线，因为距离/视锥可以在读取世界前淘汰大部分候选。但当前算法仍然执行 173,225 次外层几何循环，这部分没有出现在 `blockAt` 列中。

前方近墙场景也有类似情况：高分辨率屏幕网格会让许多射线重复命中同一面墙。加入一次 Read 内部缓存后，64×36 的 35,360 次调用只对应 793 个不同体素。这说明屏幕射线法是否划算，很大程度上取决于是否有绑定同一世界 revision 的短时体素缓存。

### 5.2 时间才是性能决策的有效口径

上面的循环数、DDA 步数和 `blockAt` 次数只能解释成本来自哪里，**不能单独证明值得改造**。项目关心的是同伴一次感知—决策—动作闭环的端到端时间，而不是让某个局部函数的基准数字尽可能小。

一个重要的反例是：假设屏幕 DDA 能把一次 viewport Read 从 100ms 降到 1ms，但同一轮模型网络调用需要数秒，那么玩家几乎感受不到这 99ms；为了它引入新的采样盲区、光学语义和维护成本，很可能得不偿失。模型调用、网络往返和多轮 tool loop 才更可能是当前闭环的主要时间来源。

不过，“单次只占 100ms”也不能自动判定无需处理。以下情况会让局部时间重新变得重要：

1. 一次玩家消息触发多轮工具调用，每轮都重新读取 viewport，100ms 会按轮数累积；
2. 扫描的 p50 较低，但复杂世界中的 p95/p99 膨胀到数百毫秒或数秒；
3. 同步计算长期占用事件循环，使聊天、取消、受伤和危险反应不能及时进入调度；
4. 主动同伴在空闲期持续刷新感知，单次不明显的成本变成长期 CPU 占用；
5. 读取时间接近 Provider timeout，造成偶发失败而不只是稍慢。

因此，真实性能判断至少要在真实 backend 上记录：

| 指标 | 要回答的问题 |
|---|---|
| viewport Read 的 p50、p95、p99 和最大值 | 常态与尾延迟分别是多少？ |
| CPU 时间与 wall-clock 时间 | 时间花在本地计算、backend 读取还是等待调度？ |
| 单次决策和一次完整玩家交互中的 Read 次数 | 局部成本会累计多少次？ |
| 最大连续事件循环占用及取消响应时间 | 是否影响聊天、中断和危险反应？ |
| viewport 占完整模型闭环的时间比例 | 优化后玩家实际能感知多少收益？ |
| 空闲主动运行时每分钟调用数与 CPU 占用 | 长期陪伴是否付出持续成本？ |

在这些数据产生前，本实验只能说明不同算法的**工作量、最坏形状和扩展趋势**，不能声称屏幕 DDA 在产品时间上更快，更不能仅凭“百万次调用”决定重构。

即使最终实测只是 `100ms → 1ms`，屏幕射线仍可能因“方块中心不等于玩家看到的表面”而值得研究；那时改造理由属于**观察正确性**，而不是关键路径性能。反过来，如果两种方法观察质量相当、当前实现尾延迟稳定且不阻塞调度，就应保留更简单的实现，即使微基准明显更慢。

## 6. 只替换 DDA 的控制组

保留全部候选、暴露邻面和方块中心语义，只把当前 0.25 格遮挡射线改成 DDA，得到：

| 场景 | 当前 0.25 采样 | 候选扫描 + DDA | 变化 |
|---|---:|---:|---:|
| 空气视野 | 20,280 | 20,280 | 没有候选进入射线阶段 |
| 开阔平地 | 129,534 | 90,780 | -29.9% |
| 前方 8 格墙 | 28,436 | 24,120 | -15.2% |
| 全透明体 | 1,997,348 | 764,966 | -61.7% |
| 全实心夹具 | 141,960 | 141,960 | 没有候选进入射线阶段 |

因此，DDA 本身已有独立价值，但它没有消除固定的 173,225 次候选循环，也没有改变“只验证方块中心”的观察语义。

在墙夹具中，简化 DDA 原型与当前 0.25 采样分别判断出 176 和 190 个候选表面。差异可能来自格线/角点穿越和离散采样跳过遮挡体素，不能直接把其中任何一个数字当作正确答案；正式实现前需要冻结起始体素、目标体素、边/角相交和 shape 命中规则。

## 7. 屏幕分辨率的含义

当前 FOV 在正前方深度 32 的平面上约覆盖：

- 水平 79.7 格；
- 垂直 44.8 格。

由此可粗略理解各网格的远处采样间距：

| 网格 | 射线数 | 深度 32 处每条射线约覆盖 | 初步含义 |
|---|---:|---:|---|
| 32×18 | 576 | 2.49 × 2.49 格 | 明显偏粗，容易漏掉孤立方块和窄缝 |
| 64×36 | 2,304 | 1.24 × 1.24 格 | 性价比较好，但一个远处完整方块仍可能因对齐而漏采 |
| 80×45 | 3,600 | 约 1 × 1 格 | 远处完整、正对相机方块的近似一像素基准 |
| 160×90 | 14,400 | 约 0.5 × 0.5 格 | 空场约 716,726 次 DDA 步进，常规每轮读取成本偏高 |

任何有限网格都无法保证看到只露出极小部分的方块、细方块或斜向表面。80×45 只意味着“完整、正对、未遮挡的一格表面”大致达到一个采样间距，不等于像素级完备可见性。

## 8. 输出差异与新发现

### 8.1 方块中心并不等于可见表面

在水平看向无限平地的夹具中：

- 当前候选中心方法返回 **0 个地面表面**；
- 屏幕 DDA 32×18 返回 100 个不同地面命中；
- 屏幕 DDA 64×36 返回 208 个；
- 屏幕 DDA 80×45 返回 244 个；
- 屏幕 DDA 96×54 返回 280 个，已经超过当前公开输出上限 256。

原因是远处地面方块的中心位于地表以下。从眼睛指向方块中心时，射线会先进入更近的地面方块，因此目标中心被判为遮挡；但该方块的顶面可能确实出现在玩家画面中。

这不是 DDA 与 0.25 采样之间的性能差异，而是“目标中心射线能否代表表面可见性”的正确性问题。平台边缘、台阶、半砖和部分遮挡表面会出现类似问题。

### 8.2 近墙会迅速达到有效信息饱和

前方 8 格墙场景中：

- 当前中心候选方法得到 190 个表面；
- 32×18 及更高屏幕网格约得到 198 个不同墙面体素；
- 从 32×18 增加到 96×54，原始 DDA 步进持续增长，但不同墙面命中几乎不再增加。

这说明固定提高全屏分辨率会产生大量重复信息。缓存、去重、跨帧错位采样或只在深度/材质边界附近局部加密，可能比每轮全屏超采样更有价值。

## 9. 暂定判断

1. **用 3D DDA 替换 0.25 格射线采样，值得作为独立实验，但调用数下降本身不足以批准实现。**它减少重复读取，也能明确逐体素穿越顺序；仍需同时验证真实端到端时间、边/角、起点、shape 和透明材质规则。
2. **屏幕射线不是在所有场景都天然更少读取，也不应被当成一次纯性能优化。**空场或高分辨率近墙时，它的原始调用可能多于视锥筛选后的候选读取；它真正可能解决的是固定体积枚举的扩展趋势和“只看方块中心”的观察语义问题。
3. **一次 Read 内部的 revision-bound 体素缓存很重要。**在本实验里，64×36 空场的 114,628 次访问只有 22,261 个不同坐标，近墙只有 793 个。没有缓存会反复承担 backend 方块构造成本。
4. **64×36 适合作为第一组观察质量/时间实验，80×45 适合作为质量对照。**32×18 可以作为极低成本组，但不应提前假定其空间信息足够。
5. **不能仅凭本次合成数字决定正式方案。**下一步应在同一真实世界、同一姿态和同一 backend snapshot 上记录候选循环数、DDA 步数、不同体素读取数、公开表面数、遗漏表面、真实 p50/p95/p99、取消响应时间及完整模型闭环占比。

## 10. 尚未回答的问题

- 屏幕射线网格是固定、由模型选择近/中/远模式，还是由 Runtime 自适应？
- 是否通过跨帧改变采样偏移，让静止观察逐步补齐，而不是单帧提高分辨率？
- 透明、半透明、流体、门、栅栏、作物、火把等 shape 如何进入 DDA 命中？
- 同一条射线应返回多少层透明表面，怎样限制全透明场景的输出和内部预算？
- 方块结果按距离选前 256 个是否仍然合理，还是应该按屏幕覆盖、材质边界和空间分区选取？
- 实体继续使用候选 AABB 可见性，还是与屏幕射线共享深度证据？
- 如何把 per-read cache 绑定到世界/连接 revision，避免一次读取混合两个世界状态？
- 真实 Mineflayer `blockAt` 的耗时分布是多少，DDA 数学、Map 缓存和对象构造各占多少？

在这些问题得到真实实验数据前，本文件只支持“值得做屏幕 DDA 原型并比较”，不支持直接宣布它替代当前实现。
