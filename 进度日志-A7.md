# A7 阶段日志

## 阶段一：SEAM-10 有界生产桥（已完成）

### 生产事实与线性化不变量

- public callback 仍由单一 dispatcher 串行调用；listener registry 的 reserve、
  callback 锁外执行、panic isolation、unsubscribe linearization，以及 runtime
  lifecycle state 先于 public callback 的顺序均保留。
- facade event bridge 的固定容量是 ordinary entity/block/sound=256、control
  lifecycle/chat/其他不可重建事实=512、overflow segments=64、terminal=1。
  不再有 `lossless VecDeque` 或每 listener 队列；control/marker 槽满时是有限的、
  可取消背压。
- runtime `RuntimeHandle::subscribe` 已从 unbounded Tokio sender/receiver 改为
  `RuntimeEventQueue`/`RuntimeEventReceiver`。上游 ordinary=256、control=512、
  overflow segments=64、terminal=1，同样没有慢 public callback 导致的无界
  receiver backlog。测试还直接让上游 receiver 不消费并检查 queued count 上限。
- facade `route_event` 在 bridge admission 锁内取得全局 sequence ticket；ordinary
  满时只丢 entity/block/sound。连续 loss 共用一个 marker；任何成功 ordinary 或
  control admission、以及 marker 被 dispatcher 取出，都会封口 loss segment。之后
  的 loss 新建 marker，因此 marker 的 `droppedCount` 不会越过中间 lifecycle/chat。
- overflow 是 `BackendOverflowPayload`/`kind=overflow` 基础设施事件，使用首次丢失
  事实的 epoch/attempt/world/dimension/source 快照，拥有独立 `overflow-N` id；不
  伪装成九类领域事实，也不会因为自身再次触发 ordinary overflow。runtime upstream
  marker 使用独立 `runtime-overflow-N` id，同样走不可丢控制类。
- Stopped 不依赖 callback 消费完成：runtime stop admission 先设置 authoritative
  state、写入 terminal reserve 并唤醒 broker；facade stop completion 只等待
  runtime owner 的资源收束与 worker join。runtime worker 记录 event 分支先消费的
  Stopped，避免 run future 随后返回时再次等待不存在的 terminal event。

### 阶段一测试矩阵

| 回归 | 结果 | 证据 |
| --- | --- | --- |
| `event_bridge_closes_each_loss_segment_at_loss_position` | 通过 | 填满 256 后执行 drop → lifecycle → drop，得到两个 count=1 marker，顺序严格为 marker/lifecycle/marker |
| `event_bridge_sustained_high_frequency_is_finite_and_control_is_retained` | 通过 | 20,000 次高频 admission 中 ordinary/control/marker/terminal 始终不超过声明容量，chat 最终交付 |
| `runtime_subscriber_queue_stays_bounded_under_unconsumed_high_frequency` | 通过 | 真实 `RuntimeHandle` subscriber 未消费时高频输入仍受上游有限 queue 限制，admitted chat 可取回 |
| `facade_real_runtime_worker_capacity_plus_one_stop_is_bounded` | 通过 | 非 scripted `RuntimeSession`/runtime worker 经过真实 subscribe→route→dispatcher 链路提交逻辑容量+1；callback 内同步 stop 返回，worker joined，overflow 恰一条，Stopped 恰一条，lifecycle/chat 保留 |
| `cargo test -p mineintent-backend --lib --locked --offline` | 通过 | 116 passed, 0 failed |

该 production-shaped 回归使用 deterministic synthetic producer 作为输入门，
但启动的是非-scripted RuntimeSession、真实 runtime worker 线程、bounded runtime
subscriber、facade bridge 和 public callback；它不声称已经验证 Azalea/Paper 网络。

### 第三轮并发复审收口证据

- 三层有限队列（`EventDispatchState`、`RuntimeEventQueueState`、
  `EventBridgeState`）都用 `open_loss_segment: Option<sequence>`，不再用共享
  `bool`。只有出队 marker 正是当前开放 segment 时才关闭；成功 admission 仍在
  原 loss-position 封口。对应回归为
  `runtime_dispatch_old_marker_pop_does_not_close_new_loss_segment`、
  `runtime_broker_old_marker_pop_does_not_close_new_loss_segment`、
  `event_bridge_old_marker_pop_does_not_close_new_loss_segment`。
- runtime owner 的 internal dispatch admission 新增 sequence ticket/turn：先到
  的 control waiter 未完成或取消前，后到 ordinary 不能越过；取消会推进
  `next_admission` 并唤醒全部 waiter。`runtime_dispatch_waiting_ticket_cannot_be_overtaken_by_later_ordinary`
  覆盖释放槽位后的 control→ordinary 顺序，
  `runtime_dispatch_stop_cancels_waiting_ticket_and_advances_turn` 覆盖 gate 化的
  stop cancellation；生产 `enqueue_dispatch_locked` 在 admission/取消推进 turn
  后也唤醒后续 waiter，避免 drainer 已在 broker 阻塞时丢失 turn wakeup。
- stop 的取消位单调分离：`stop_requested` 先于 `command_admission` 设置，
  `dispatch_cancelled` 唤醒 internal dispatch 与 subscriber backpressure；
  `begin_connection_attempt_locked` 在 stop 已发出后不重置取消位，最终 terminal
  才设置 `shutdown_requested`。精确竞态回归为
  `runtime_stop_cancellation_is_not_reset_while_connection_admission_waits`；它让
  broker control 与 internal control 同时饱和，连接尝试持有 command lock 卡在
  dispatch admission，再由 stop 唤醒并断言唯一 `Stopped`。
- `facade_real_worker_finite_internal_admission_stops_under_broker_backpressure`
  现在先 gate 满 control、确认下一条 control 在等，再并发提交大量 ordinary；
  ordinary 不越序，stop 取消后两类 producer/真实 runtime worker 均收束。旧的
  `facade_scripted_callback_sync_stop_completes_with_quiescent_bounded_dispatch_saturation`
  按 control=512 的 lane 语义验证，ordinary capacity+1 仍由
  `facade_real_runtime_worker_capacity_plus_one_stop_is_bounded` 覆盖。
- 做过一次可恢复 mutation：临时把 runtime dispatch marker 出队的 identity
  判断改为无条件清除，`runtime_dispatch_old_marker_pop_does_not_close_new_loss_segment`
  按预期失败；已恢复 identity 判断，最终全量 116/116 通过。

阶段一当前的 item boundary 已到达；不把上述 synthetic/gate 测试写成真实
Paper/Azalea 网络验证，也不在本日志之后继续进入阶段二 producers。

## 其他阶段实现/测试矩阵

| 阶段 | 生产实现 | deterministic test | 状态 |
| --- | --- | --- | --- |
| entity 六类增量 | 六类 mapper/cache seam 已有；真实 schedule 仅接入 Spawned/Moved/Removed，Updated/Animation/Hurt 未接入 | 15 个 entity 聚焦测试覆盖 mapper、packet batch fidelity、scope/token、current-owner epoch 绑定与 publish linearization | 部分完成 |
| block updated/chunk_unloaded（保留 chunk_loaded） | 尚未接入 | 尚未新增 mask/scope/reset 矩阵 | 未完成 |
| sound heard 完整 payload | 尚未接入 | 尚未新增 NamedSoundEffect/SoundEffect 矩阵 | 未完成 |
| self light | 尚未接入 `ClientboundLevelChunkWithLight`/`ClientboundLightUpdate` cache | 尚未测试 nibble/mask/负 section Y/unavailable | 未完成 |
| armor | 尚未消费 self `ClientboundUpdateAttributes` 与真实 modifiers | 尚未测试 base/三种 operation/equipment/unknown | 未完成 |
| reconnect/timeout policy | `RunConfig` 仍未真正消费 multiplier/max/jitter/stable reset 和四阶段 deadline | 尚未新增注入 clock/RNG policy 矩阵 | 未完成 |

### A7 stage2a1 entity 纵切（Spawned/Moved/Removed）

- AddEntity 的动态字段直接取 packet；三类 move packet 按 message 顺序 patch producer shadow，避免同一 PreUpdate 批次被 Update 阶段最终 ECS 状态折叠。RemoveEntities 只消费 shadow，因此 Add→Remove 同批、查询侧已无 ECS entity 时仍保留 `Removed.last`。
- entity key 固定为 `epoch:protocol-id`；relative delta 使用协议 1/4096 量化，compact rotation 使用 `i8*360/256`。未知 move 不从批次最终 ECS 倒推。
- `SharedRuntime` 只持有一个 current `(Azalea client Entity, epoch)` owner。Init 绑定并 reset；Login/Respawn 在 ordered `ReceiveGamePacketEvent` stream 的确切位置按 owner reset，已移除 entity producer 的独立 WorldLoaded pre-drain；Disconnect/ConnectionFailed 仅对 current owner deactivate，清空 cache/token/active epoch。迟到旧 owner lifecycle 或 packet 不能污染新连接。
- entity publish 在 queue insertion 前于 `command_admission` 下复核 owner，并持有 owner guard 直到 EventWriter 验证并写入同一 bound epoch 的 metadata；若连接已切换则丢弃旧 payload。Init 的 reservation 消费、writer epoch 读取、owner/cache 绑定也在同一 admission 事务内，attempt transition 无法插入其间；drain/callback 保持锁外。
- `SwarmEvent::Disconnect` fallback 在 reconnect claim 的同一 lifecycle admission 下失活 current owner；无 entity 的 fallback 与有 entity 的旧 disconnect 分开处理。
- entity-specific Disconnect/ConnectionFailed 现在在同一 owner-aware lifecycle admission 中完成 owner/epoch 验证、producer deactivate 与 global close/failure mark；late A 在 B bound 后不会清 observation、取消 commands、触发 reconnect 或 bot.disconnect。pre-Init ConnectionFailed 仅允许 current unbound attempt。
- `cargo test -p mineintent-backend entity_events --lib --locked --offline`：19 passed，0 failed；`cargo test -p mineintent-backend --lib --locked --offline`：135 passed，0 failed；workspace all-target check 通过。Updated/Animation/Hurt 的真实来源仍记录在 `.codex-tmp/A7-stage2-需要决策.md`，本切片未宣称完成。

## contracts/API 兼容性

阶段一没有修改 `crates/contracts/**`，没有新增 wire/API 字段；event-v2 的现有
`Overflow` DTO 被按冻结 schema 使用。light/armor 仍待最小 API seam 裁定与 serde
fixtures，不能把当前 fake/snapshot 缺失状态写成已完成。

## Azalea 源码证据与真实验证边界

审计已确认 vendor 内 Azalea 0.16.0 packet-game 入口包含
`ClientboundLevelChunkWithLight`、`ClientboundLightUpdate`、
`ClientboundUpdateAttributes` 以及 entity movement/removal/equipment packet；
现有 `light_update` 和 `update_attributes` plugin handler 是 stub，故 A7 后续
必须由 backend 自持 cache/权威状态。阶段一未修改 vendor/supplies、未联网、未
启动 Paper，尚未做真实服务器或 app 验证。

## 约束与裁定

未提交 commit；仅修改 backend 与 `.codex-tmp`。永久阻塞 callback 下“所有通道
有限容量、任意长 lifecycle/chat 永不丢”在有限内存中数学上不可同时满足。严格
反例、推荐的有限 admission + 可取消背压 + stop owner 解耦语义写于
`.codex-tmp/A7-需要决策.md`；实现不再使用无界 spill lane，也不把容量内保证
扩写成永久阻塞下的全局保证。

## 下一步（暂停在 item boundary）

等待派发方/主审在干净边界继续。恢复后才按任务书顺序进入真实
entity/block/sound producers；必须保留上述 FIFO/linearization/stop 不变量，先做
目标 Azalea symbols 的窄读和每类 deterministic 输入到冻结 DTO 测试，再进入
light/armor 与 transport policy。本轮明确禁止把阶段二标为完成。

## A7 stage2c 实体 slice（当前返修记录）

本轮保留未提交状态，仅修改 backend 与本地 `.codex-tmp` 记录；未修改 vendor、supplies、contracts、root、middle、app，未启动实服/Paper 验证。

- 同一个 Bevy entity 的 A/B reconnect 已由 reservation epoch、reconnect token、canonical join-started source 与 reconnect-return bind 串成单一 admission。Init/return 任意先后及重复调用最多消费一次 reservation、最多一个 writer epoch、最多一次 `transport_connected`；return bind 在 `finish_reconnect_attempt` 前完成。旧 A 的 source epoch lifecycle、packet、world boundary 与 producer input 对 B 均零副作用。
- production entity packet seam 现在按 Add、三类 relative Move、TeleportEntity、EntityPositionSync、RotateHead、SetEntityMotion residual、RemoveEntities 顺序处理。Teleport 先按旧 look 对旧 velocity 做 vendor `Vec3::x_rot(...).y_rot(...)`，再应用 relative/absolute delta；RotateHead 发布 Moved；SetEntityMotion 不发布 Moved/Updated，但 public observation 立即更新 velocity。
- `capture_tracked_entities` 与 packet event 使用同一 `${epoch}:${protocol_entity_id}` key，UUID 保持独立字段；Add 与 ECS observation 共用 `canonical_entity_type`。真实 packet-shaped 测试覆盖多词 `dark_oak_chest_boat` 的 event/observation 字节一致。
- refresh 不再把旧 packet shadow 的 position/type/pose 等永久覆盖 ECS；只合并明确的 headYaw 与 PositionSync/SetMotion velocity residual。Remove、world boundary、scope reset 清掉 residual。finite admission 对 position/velocity 按有限 f64，yaw/pitch/headYaw/width/height 才要求 f32 可表示；无效输入不消费 token，Teleport、SetMotion、unknown RotateHead 可同 token retry。LocalEntity/MinecraftEntityId 证据缺失时 packet adapter fail-closed。
- 中期更新 14 已裁定 NEW-12 为 `1B + 2B + 3A`：Updated 后续按 packet post-state、`changed: []`；Animation 保留但不生产；Hurt 只接 canonical DamageEvent cause，cause 未命中同 epoch shadow 时 source 缺席，绝不 direct fallback；旧 entity_status 1/2 不采用。本轮不扩大到 Updated/Hurt。

### fresh deterministic evidence

- `cargo test -p mineintent-backend entity_events --lib --locked --offline`: **37 passed, 0 failed**。
- `cargo test -p mineintent-backend --lib --locked --offline`: **156 passed, 0 failed**。
- 测试包含同 entity reconnect-return 无 Init 的 Spawn→Move→Remove、pre-bind/bound late-A、精确 close reason/error once、Init/return 幂等、callback 内 observation post-state、真实 packet adapter 逐包 post-state、Teleport mixed-relative、PositionSync、RotateHead、SetMotion 无 envelope、finite/unknown retry、refresh residual/ECS 双向边界、local/self fail-closed、key/type 多词一致性。
- final gates：`cargo check --workspace --all-targets --locked --offline` 通过；`cargo fmt --all -- --check` 通过；`cargo +stable fmt --all -- --check` 通过；`git diff --check` 通过。source attribution 的完整运行 schedule residual 仍需父级审阅，不能宣称 A7 entity slice 已被实服接受。

## A7 parent-fix fresh continuation 4（收尾）

- 审查返修已收口：新增真实 `ReceiveGamePacketEvent -> produce_entity_packet_events`
  批次测试，在一次 `app.update()` 前投递
  `Add -> MovePos -> Teleport -> PositionSync -> SetMotion -> RotateHead -> Remove`；
  同步 listener 每次 callback 读取 post-state，确认事件顺序、Remove 空 observation，
  以及 SetMotion 无 envelope 但下一 callback 可见量化后的速度。
- 新增单次 Update 的初始 `Login -> Add` 与 `Respawn(the_nether) -> Add` 测试：raw
  dimension 在包序边界生效，scope reset 后旧实体不残留，Respawn 的
  `DimensionChanged` 先于 `Spawned`，两次 Spawned envelope 均带非 `None` dimension。
  `admit_unstamped_client_source` 没有实际调用路径，已删除，不留 warning；compact
  pitch 增加 `x_rot=100/-100 -> +/-pi/2` 极值回归。
- 本轮 focused gate：`cargo test -p mineintent-backend entity_events --lib --locked --offline`
  **37 passed, 0 failed**；backend 全量 gate：`cargo test -p mineintent-backend --lib
  --locked --offline` **156 passed, 0 failed**。
- capture 证据层级如实限定：`capture_membership_excludes_deferred_remove_and_cross_client_entities`
  覆盖 `LoadedBy + WorldName` owner predicate；runtime
  `remove_then_refresh_with_membership_excluded_capture_does_not_revive` 覆盖 Remove
  后空 capture refresh 不复活且清 residual。未构造完整 `Client` 的真实 Azalea
  deferred-despawn/cross-client capture integration，不把 predicate/runtime 证据写成
  实服或完整 ECS capture 验证。
- provenance 仍 **BLOCKED**：vendor event 没有 source token；同一 Bevy entity
  reconnect 的无歧义 provenance fixture 仍受 fail-closed fence 限制，未放宽 fence。
  Paper/真实服务器仍 **未验证**。
- equipment 风险登记给后续 Updated/Hurt：equipment `slot: u32 -> u8`、`count: u32 -> i32`
  的 representability/后续转换 panic 风险本轮不扩展处理；当前路径保持 fail-closed，
  不把 Updated/Hurt 提前接入。

## A7 stage2d 父审续修：同一 raw batch 的跨 scope 顺序

- 父审反例已复现并修复：旧实现由两个 `MessageReader` 分别遍历同一批
  `ReceiveGamePacketEvent`，第二 reader 会在 entity reader 完成 Respawn 后用最终
  scope 处理旧 Sound/Forget。现在 `produce_entity_packet_events` 是唯一按 raw message
  顺序推进的 canonical reducer：Sound/Forget 在其原始位置直接处理；Login/Respawn
  只影响后续 raw item；不再保留 block/sound 的第二 raw reader。
- Block token 在 raw 位置写入一个与 Azalea `QueuedServerBlockUpdates.list` item 对齐的
  immutable `CanonicalSourceAdmission` metadata；不复制 block payload、不形成跨 tick
  spill。Update 消费并清空 metadata；长度/entity 不匹配时所有对应 observation
  fail-closed，但仍逐项执行 vendor prediction/update/world apply。旧 token 即使跨
  Respawn 也不会错配到新 token，A→B→C 的逐项 post-state callback 保持。
- LevelChunk raw item 同样保存 source stamp 与 `(chunk_x, chunk_z)`，Update 仅在
  `ReceiveChunkEvent` 坐标和整个本批 metadata 序列匹配、且 vendor 已完成 world load 后
  发布 `ChunkLoaded`。缺失、额外或坐标错位会整批不发布并清空 metadata，禁止向后错配。
  publication 仍二次复核 owner、epoch、scope generation、source fence，callback 外不
  持有 command/producer/world/dispatch lock。

### cross-scope deterministic evidence

- `production_block_sound_raw_reducer_preserves_sound_scope_at_packet_position` 覆盖
  `Sound(old) -> Respawn(nether) -> Sound(new)` 与 `Respawn -> Sound(new)`：old 仍是
  old scope，new 恰一次且带 nether dimension。
- `production_block_sound_raw_reducer_stamps_block_and_chunk_items_across_respawn` 覆盖
  old/new block 与 LevelChunk token 穿过同一 raw batch scope reset：旧 observation 不发，
  新 block/chunk 各一次；旧、新 block world state 均仍应用。
- `production_block_sound_*` 既有 ordinary batch、负坐标、Section 顺序、oldBlock null、
  sound direct/reference、chunk once 与 callback post-state 回归继续通过。
- `cargo test -p mineintent-backend block_sound --lib --locked --offline`：**7 passed，0 failed**；
  `cargo test -p mineintent-backend production_ --lib --locked --offline`：**10 passed，0 failed**；
  `cargo test -p mineintent-backend entity_events --lib --locked --offline`：**37 passed，0 failed**；
  `cargo test -p mineintent-backend --lib --locked --offline`：**163 passed，0 failed**。
- `cargo check --workspace --all-targets --locked --offline`、默认/stable fmt check 与
  `git diff --check` 均通过。source provenance 仍 **BLOCKED**，SoundEffect 仍只是冻结
  compatibility variant；SoundEntity、Paper/真实服务器、light/armor/transport 仍未接入
  或验证。

## A7 stage2d block / chunk / sound 窄纵切（本轮收口）

- 修改仅落在 `crates/backend/src/runtime.rs`（并保留本轮已有的 entity 变更）；未修改
  vendor、supplies、contracts、Cargo.lock、manifest 或主 worktree。新增
  `BlockSoundProducerPlugin`：raw packet admission 位于 `read_packets` 之后，完成的
  `ReceiveChunkEvent` 与 Azalea ordered block queue 在 Update 中按 vendor 时序接线。
- `QueuedServerBlockUpdates.list` 被逐项 take/drain，按原顺序执行
  `BlockStatePredictionHandler::update_known_server_state`，未消费项写入 world，并在每项
  应用后立即发布 `ProtocolBlockEvent::Updated`。old/new 是完整冻结 block DTO；未加载 old
  明确为 `null`；不新增 block shadow/spill。连续 A→B→C 的第二个 old 是 B，callback
  读取到对应 post-state；SectionBlocksUpdate 展平保持 packet 顺序和负 section 坐标。
- `ChunkLoaded` 只从完成的 canonical `ReceiveChunkEvent` 发布一次；原高层
  `Event::ReceiveChunk` 分支保留其他高层行为但不再重复生产。`ChunkUnloaded` 来自已应用
  的 raw `ForgetLevelChunk`，负 chunk 坐标原样保留。block/sound 新事件都在 queue
  insertion 前复核 owner、epoch、scope generation 与 source fence；drain/callback 不持有
  command、producer 或 world lock；late scope publication fail-closed。
- `ClientboundSound` 的 direct custom 与已解析 known reference 都生产
  `ProtocolSoundPayload` heard / `named_sound_effect`：名称、`x/y/z / 8`、volume、pitch
  和唯一非空 soundKey 精确映射，soundId/category 缺席。volume 必须 finite 且 >=0，pitch
  必须 finite；NaN/Infinity/负 volume 不发。
- 按 Mineflayer oracle 的边界，Azalea typed holder 当前没有可证明的未知 numeric registry
  producer seam，因此本轮不伪造 `ProtocolSoundSource::SoundEffect`；该 variant 仅保留为
  冻结兼容面。`ClientboundSoundEntity` 没有对应 oracle listener，故不接入。Paper/真实
  服务器未启动验证，synthetic packet-shaped Bevy 测试不宣称实服证据。

### fresh deterministic evidence

- `cargo test -p mineintent-backend block_sound --lib --locked --offline`：**1 passed，0 failed**；
  该过滤名只命中既有 typed conversion test。
- 新增 production-shaped Bevy schedule 过滤 `production_`：**8 passed，0 failed**，覆盖
  block A→B→C 后态与 unloaded old=null、SectionBlocksUpdate 负坐标/顺序、direct/reference
  sound 与无效数值 fail-closed、chunk loaded/unloaded 各一次/负坐标，以及 entity 的既有
  production tests。
- `cargo test -p mineintent-backend entity_events --lib --locked --offline`：**37 passed，0 failed**。
- `cargo test -p mineintent-backend --lib --locked --offline`：**161 passed，0 failed**。
- `cargo check --workspace --all-targets --locked --offline`、`cargo fmt --all -- --check`、
  `cargo +stable fmt --all -- --check`、`git diff --check`：全部通过。
- NEW-13/source provenance 仍 **BLOCKED**，沿用现有 fail-closed fence；本轮不放宽、不声称
  解决，也未进入 light/armor/transport 或实体新分支。未提交 commit。

## A7 stage2e light / armor / atomic frame-facts（当前续跑收口）

### 生产实现与父审反例

- contracts 新增普通 Rust `MinecraftFrameFacts`（无 `Serialize`/`Deserialize`），只包含既有
  `MinecraftSnapshotV1`、`armor: Option<u8>`、`light: Option<u8>`；
  `MinecraftBackendApi::capture_frame_facts` 默认只调用 `snapshot()`，两个新事实均为
  `None`，既有 fake 不需要改动。`MinecraftBackendFacade` override 只把
  `RuntimeHandle` 的一次 observation capture 转换为该 DTO。
- `MinecraftSnapshotV1` 的既有 serde/protocol 名称和字段未改；fixture round-trip 明确检查
  JSON 不含 `armor`/`light`。armor 的原始 packet reducer 只接受当前 local entity、owner、epoch
  和 source fence，按最后 armor snapshot、modifier id 最后替换和三组 operation 顺序计算；
  非有限值写 unavailable，不保留伪旧值，最终 floor/clamp 到 0..20。
- light reducer 沿唯一 ordered raw packet stream 消费 `LevelChunkWithLight`、`LightUpdate`、
  `ForgetLevelChunk`，Login/Respawn 按 packet 位置 reset scope；mask/data/empty/retain、
  nibble index、负 section/minY=-64、sky/block max、unknown+15 与 unknown+<15 fail-closed
  均在 cache 中处理。有效 cache mutation/reset 和同 epoch scope reset 使用同一 observation
  generation。
- 父审首次 Spawn 反例已修复并保留回归：同一 owner/epoch/scope 中真实
  `ReceiveGamePacketEvent -> produce_entity_packet_events -> LevelChunkWithLight -> 首次
  set_world_if_running -> snapshot -> RuntimeHandle/facade facts` 不再无条件清掉已收光照；
  仅当 command-admission 下 owner/epoch/scope/dimension context 一致时保留，旧 scope attach
  不能复活 cache。same-epoch Respawn 保留 armor、清 light；新 epoch 两者均不可见。

### 本地 packet/Bevy schedule 证明

- 聚焦结果：`entity_events` **37 passed**；`block_sound` **7 passed**；`production_` **13
  passed**；`armor` **3 passed**；`light_cache` **3 passed**；`frame_facts` **2 passed**。
- 新增/保留的 production-shaped 回归包含首次 Spawn attach 保留 light、同批
  Login/Respawn scope 顺序、full replace/partial retain/Forget、registry extra 的
  `has_skylight=true/false/unknown`、实际 packet modifier duplicate id/NaN/Inf、真实 facade
  override/default fake、fixture wire 与 capture generation race。
- `frame_facts_capture_cannot_mix_with_an_epoch_reset_at_the_read_boundary` 已改为明确门控：
  before-values hook 先在 read guard 已持有后放行 reset，reset writer 报告将进入 write 边界，
  cfg(test) write-boundary hook 再允许其尝试写锁；read guard 内 `try_write` 必须失败，释放后
  writer 有界完成。临时 mutation（先 clone snapshot、释放 read guard，再读取 armor/light）
  实际使该测试失败，失败测试名就是上述 race 测试；随后恢复单一 read lock，测试通过，mutation
  未保留。
- `cargo test -p mineintent-backend --lib --locked --offline`：**173 passed, 0 failed**。
  `cargo test -p mineintent-contracts --locked --offline`：新增 2 个 contracts 单测及既有套件
  全部通过。`cargo check --workspace --all-targets --locked --offline`、两套 fmt check、
  `git diff --check` 均通过（Git 仅报告既有 LF/CRLF 工作副本提示）。

### 尚无 Paper 实服证明

- 上述均是本地 packet-shaped / Bevy schedule / runtime lock 的确定性证明；没有启动 Paper，
  没有真实服务器登录、Respawn、光照 registry 或客户端 26.1.2 实服证明。NEW-13 token
  provenance 仍是外部 blocker，继续保持 fail-closed，不用时间窗、hash 或当前 epoch 伪造
  source。
- 未进入 transport 或 app composition，未修改 vendor/supplies、Cargo manifest 或
  `Cargo.lock`，未 commit/cherry-pick/push。

## A7 stage2f transport（当前独立语义切片；pre-Init 部分阻塞）

### 已实现且有本地证据

- `RunConfig` 现在完整承载 contracts 的 `BackendTimeouts` 与 `ReconnectPolicy`；facade
  将四个 timeout、enabled、initial/multiplier/max/jitter/stableReset 全字段映射，诊断 CLI
  的 `--reconnect-delay-secs` 仍可用。wire DTO/contracts schema 没有改变。
- epoch 与 retry ordinal 已分离：epoch 只在真正 begin attempt 时单调递增；retry ordinal 从 0
  递增并用于 `Connecting/LoggingIn/Spawning` state、`ConnectionRequested`、
  `ReconnectScheduled`，stable Ready 身份复核后才归零。
- backoff 使用 TS oracle 的 exponent/base/jitter/非负 `Math.round` 语义；f64 pow/multiply
  非有限、u64 毫秒和 chrono retryAt 边界均在转入调度值前饱和，不 wrap/panic。生产 RNG 是
  无新增 manifest 依赖的本地原子 seam；单测直接注入 random 与 fixed clock。
- Init 后的 login/spawn deadline 在真实 `consume_attempt_for_transport_init_and_bind` /
  `emit_logged_in` 路径安装 Tokio task；phase generation、epoch、retry ordinal、state、
  stop/close 状态在 callback 的 command admission 下精确复核。Ready stable timer 与旧 phase
  timer 均可失效；late A 对 B/Ready/Stopped 零副作用。timeout close code 为
  `login_timeout`/`spawn_timeout`，retryable=true，关闭只走既有唯一 seal；disabled policy
  保持 `reconnect_disabled` fault 优先级。
- stop watchdog 使用独立 `timeouts.stop_ms`，正常提前 Stopped 会使 generation 失效；强制路径
  取消 pending command/movement waiter、失效 reconnect/phase/stable、清 active client/swarm，
  通过同一 `enqueue_stopped_locked` 发布唯一 Stopped，不发布 Faulted/ReconnectScheduled。

### deterministic/runtime-shaped 证据

- `transport_retry_schedule_matches_ts_oracle_boundaries`：exponent 0/1/多次、max clamp、
  initial>max、random 0/0.5/接近 1、jitter 0/1、round 边界、巨大 multiplier/attempt、
  retryAt-delay 一致，**1 passed**。
- `transport_retry_ordinal_matches_scheduled_and_next_request_while_epoch_stays_monotonic`、
  `stable_reset_requires_exact_ready_epoch_attempt_and_generation`：ordinal/epoch 与 stable
  exact identity，**各 1 passed**。
- `phase_deadlines_have_distinct_codes_unique_close_and_disabled_fault_priority`、
  `late_phase_timeout_and_disconnect_stop_races_are_admission_gated`：三 phase code、唯一 close、
  disabled fault、late A 与 timeout/disconnect/stop 竞态，**各 1 passed**。
- `normal_stop_invalidates_watchdog_and_publishes_stopped_once`、
  `stop_watchdog_forces_pending_movement_and_has_no_fault_or_reconnect`：正常 watchdog cancel、
  强制 movement/command 收束与唯一 Stopped，**各 1 passed**。
- `runtime_shaped_timer_tasks_install_progress_and_cancel_on_phase_and_stop`：LocalSet 中真实
  Tokio phase/stable/stop task 安装、推进与取消，**1 passed**。目前未宣称 Paper 连接证据。

### pre-Init connect cancellation：明确阻塞

- `需要决策-A7-transport-connect-cancel.md` 已写入工作树根。Azalea 0.16 的
  `SwarmBuilder::start_with_opts` 在 `vendor/azalea/src/swarm/builder.rs` 内部 spawn 并 await
  `Swarm::add_with_opts`；`vendor/azalea/src/swarm/mod.rs` 的 add 先 await
  `Client::start_client` 才返回 Client；公开 `Client::disconnect/exit` 不能覆盖这个阶段，
  `CreateConnectionTask` entity 也没有从 add future 暴露给 backend。故生产 begin→Init
  `connectMs` 的安全取消/旧 socket 不绑定 B 尚未完成，本轮未用假 timeout/reconnect 事件冒充。

### 尚未进行的验证与门禁

- 尚未做 Paper 真实断网、黑洞 socket、慢登录、慢 spawn；pre-Init connect deadline 仍受上述
  API seam 阻塞。
- stage2f 最终全量 backend/contracts/workspace、entity_events 37、block_sound、light/armor、
  两套 fmt 与 diff gate 尚未在本轮收尾重跑。

### mutation evidence

- 临时把 `fire_phase_deadline` 的 `phase_deadline_matches_locked(token)` admission 改为无条件
 通过；`cargo test -p mineintent-backend
  late_phase_timeout_and_disconnect_stop_races_are_admission_gated --lib --locked --offline`
  立即失败，失败测试名为 `runtime::tests::late_phase_timeout_and_disconnect_stop_races_are_admission_gated`
  （断言 `late A timeout must not close B`）。随后恢复 token 复核，变异未保留。

### stage2f 续跑门禁收口（2026-08-03）

- 依据 `任务书.md`、`移植任务书.md` 和 `.codex-tmp/checkpoint-a7-stage2f.md` 续跑；未扩大审计范围，未修改 contracts schema、vendor、manifest、`Cargo.lock`、app composition 或 supplies。
- transport 聚焦证据保持通过：`transport_retry_schedule_matches_ts_oracle_boundaries`（1）、`transport_retry_ordinal_matches_scheduled_and_next_request_while_epoch_stays_monotonic`（1）、`stable_reset_requires_exact_ready_epoch_attempt_and_generation`（1）、`phase_deadlines_have_distinct_codes_unique_close_and_disabled_fault_priority`（1）、`late_phase_timeout_and_disconnect_stop_races_are_admission_gated`（1）、`runtime_shaped_timer_tasks_install_progress_and_cancel_on_phase_and_stop`（1）、两个 stop watchdog 测试、facade 完整映射和 stop/reconnect 竞态均通过。
- `cargo test -p mineintent-backend --lib --locked --offline`：**181 passed，0 failed**。
- 聚焦回归：`entity_events` **37 passed**；`block_sound` **7 passed**；`light_cache` **3 passed**；`armor` **3 passed**。
- `cargo test -p mineintent-contracts --locked --offline`、`cargo check --workspace --all-targets --locked --offline`、`cargo fmt --all -- --check`、`cargo +stable fmt --all -- --check`、`git diff --check`：全部通过。
- mutation 证据仍为：移除 `phase_deadline_matches_locked(token)` admission 时，`runtime::tests::late_phase_timeout_and_disconnect_stop_races_are_admission_gated` 在 `late A timeout must not close B` 失败；源码已恢复，具名测试及全量 backend 均通过。

### 尚未完成 / 明确阻塞

- `需要决策-A7-transport-connect-cancel.md` 仍是唯一 transport 硬阻塞：Azalea 0.16 的 `SwarmBuilder::start_with_opts` → `Swarm::add_with_opts` → `Client::start_client` 在 pre-Init 返回前没有按 attempt 的公共 cancel handle；`Client::disconnect()` 不能安全取消在途 `CreateConnectionTask`。因此 begin→Init 的生产 `connectMs` deadline 尚未宣称完成，也没有用假 timeout/reconnect 事件掩盖。
- 尚未进行 Paper 真实断网、黑洞 socket、慢登录、慢 spawn 验证；其余 deterministic/runtime-shaped 证据不等同于实服证据。
- 未提交、未 cherry-pick、未 push；既有 A7 工作树改动均保留。

### stage2f stop watchdog 返修（2026-08-03）

- 修复前只有 `stop_watchdog_generation` 失效复核：正常 `enqueue_stopped_locked`/`finalize_stop_if_ready` 没有唤醒 `stop_watchdog_cancel`，因此只能证明迟到 callback inert，不能证明长 `stopMs` task 已退出。
- 现在唯一 `Stopped` reducer 在正常 cleanup 首次成功 seal 后调用一次 `stop_watchdog_cancel.notify_one()`；`Notify` permit 会在 watchdog task 尚未首次 poll 时保留，故不会 lost-wakeup。forced watchdog 调用同一 reducer 但传入 `cancel_watchdog=false`，不让正在执行自身路径的 task 留下 stale permit；generation 仍先失效并保持唯一 Stopped gate。
- Notify 语义审核：stop watchdog 只有一个待取消 task，使用 `notify_one`；既有 reconnect waiter 继续使用保留 permit 的 `notify_one`；phase/stable 可能有多个旧 task，继续使用 `notify_waiters`，本返修未重构它们。
- 新增 `runtime::tests::normal_stop_cancels_watchdog_task_before_large_stop_deadline`：Tokio `LocalSet` 下 `stop_ms=60_000`，在同一同步 stop 调用中让 task 尚未首次 poll，正常 stop 后等待显式 oneshot completion probe；task 先 drop `RuntimeShared` 再完成 probe，`Weak` 随后无法 upgrade。该测试不靠 sleep 猜测，**1 passed**。
- transport 聚焦回归（retry/stable/phase/late/runtime-shaped/forced watchdog）全部通过；`cargo test -p mineintent-backend --lib --locked --offline`：**182 passed，0 failed**。
- pre-Init connect cancellation blocker、Paper 真实断网/黑洞 socket/慢登录/慢 spawn 尚未改变；未修改 vendor/supplies/contracts schema/manifest/`Cargo.lock`，未提交。

## NEW-13/15 V1：Azalea vendor attempt identity 与精确取消基座（2026-08-03）

### 目标与边界

- 只实现 vendor 侧“不可变 attempt 身份 + 按 (Entity, token) 精确取消 pre-Init
  `CreateConnectionTask`”基座；backend epoch 一对一绑定、connect deadline
  admission 接线不在本切片，仍保持未宣称。
- 实际修改受控依赖 `vendor/azalea-client`、`vendor/azalea`（父级基线
  `.codex-tmp/vendor-baselines/new13-15-pre-deepseek-warm-20260803-2035`）；
  backend `runtime.rs` 仅做新增字段/枚举形状的保持编译迁移；
  `vendor-patches/azalea-0.16-attempt-identity.md` 登记全部 vendor 改动。

### 实现要点

- `AttemptToken`（u64 newtype，`AtomicU64` 进程内自增 `mint()`，`Copy`/比较/
  Component）只在 `Client::start_client` 与自动重连 `rejoin_after_delay` 铸
  造；handler 不重铸，重连必获新 token。
- 链：`StartJoinServerEvent.attempt_token` → callback `(Entity, AttemptToken)`
  → `CreateConnectionTask{task, attempt_token}` → `RawConnection.attempt_token`
  → `Client` 私有 `attempt_token` + 只读 getter。`Client::disconnect()` 用句柄
  自身 token 盖章，不从复用 Entity 读“当前 token”。
- 事件在产生处盖章：`ReceiveGamePacketEvent`/`WorldLoadedEvent`/
  `ConnectionFailedEvent` 必填 token；`DisconnectEvent` 用
  `Option<AttemptToken>`，清理仅在事件 token == 实体当前 `AttemptToken`（或
  两者皆无的 legacy 测试路径）时执行。`SwarmEvent::Disconnect` 增加
  `Option<AttemptToken>` 第三个元素，事件拷贝任务持旧 attempt 的 Client，天然
  区分 A/B；`Event::Init/Login` 与成对收到的 Client 携带同一 token。
- 取消：`CancelConnectionTaskEvent(entity, token)` 在 poll 前移除匹配
  `CreateConnectionTask`；组件内 `bevy_tasks::Task` drop 即取消/丢弃连接
  future。poll 安装 `RawConnection` 前复核 current token，迟到 A 完成既不装
  连接也不发 `ConnectionFailedEvent`。

### 验收测试与证据

- azalea-client 单测（受控 future/drop probe + ECS schedule）：
  `cancel_matching_task_drops_future_and_stale_cancel_is_noop`（A 取消实际
  drop、B 复用后 stale cancel(A) 不碰 B、matching cancel(B) 生效）、
  `stale_completed_task_cannot_install_raw_connection_or_emit_connection_failed`、
  `connection_failed_event_stamps_the_attempt_token`、
  `start_join_callback_handoff_carries_entity_and_attempt_token`：**4 passed**。
- azalea-client 集成测试 `tests/simulation/attempt_identity.rs`（packet/
  WorldLoaded 事件 token、stale Disconnect(A) 不移除 B、matching Disconnect(B)
  正常清理）：**2 passed**；全套 `--test main` **25 passed**。
- azalea 单测：`Client` getter/disconnect 盖章、legacy 构造无 token、
  `SwarmEvent::Disconnect` 携带 token：**3 新增**，全套 `--lib` **23 passed**。
- 具名回归重复：cancel/drop 测试与 stale-disconnect 测试各连续 **10 次通过**。
- Mutation：临时把 `cancel_create_connection_task` 的 token 匹配去掉（仅按
  entity 取消），`cancel_matching_task_drops_future_and_stale_cancel_is_noop`
  在“stale cancel(A) 不得移除 B 任务”断言失败；立即恢复后同一命令通过。
  变异前后已向 `.codex-tmp/progress-new13-vendor.jsonl` 上报
  `temporary_change_active`。
- 门禁：`cargo test -p mineintent-backend --lib --locked --offline`
  **182 passed**；`cargo check --workspace --all-targets --locked --offline`
  exit 0；默认/stable `cargo fmt --all -- --check`、vendor rustfmt
  `--check`、`git diff --check` 全部通过；两个 `.cargo-checksum.json` 已按
  实际改动文件机械更新并全量复核（mismatch=0）。

### 剩余风险与明确未完成

- backend 尚未把 source token 与自身 epoch 一对一绑定，也没有 pre-Init
  connect deadline 的 admission/取消接线；本切片只提供 vendor 基座与证明。
- `eyre`/`criterion`/`backtrace` 未 vendored，azalea 的 testbot example 无法
  在本环境离线完整编译（已做最小 pattern 迁移，未做完整编译门禁）。
- Paper 真实断网、黑洞 socket、慢登录、慢 spawn 仍未验证；vendor 测试均为
  本地 deterministic/runtime-shaped 证据。
- 未提交、未 cherry-pick、未 push；未修改 workspace manifest、lockfile、
  应用层协议或无关代码。

## NEW-13/15 V1 父级审查返修 1（2026-08-03）

### 返修内容

- **精确取消三条件**：`cancel_create_connection_task` 现在要求 entity、
  实体当前 `AttemptToken`、`CreateConnectionTask.attempt_token` 三者全等；
  新单测 `cancel_requires_task_owned_and_current_token_match` 构造
  `current=B, task=A`，证明 cancel(B) 不删残留 A task，cancel(A) 也不能越过
  current fence，只有全匹配才删除。
- **checked mint**：`AttemptToken::mint` 改用 `try_update` + `checked_add`，
  到 `u64::MAX` 不再推进、不 wrap、不重复；`mint_from(&AtomicU64)` 边界
  helper + 单测 `checked_mint_never_wraps_or_reissues`。
- **消费端 token fence（审计全部四个事件消费者）**：
  - `auto_reconnect`：stale A 的 Disconnect/ConnectionFailed 不安装 timer；
    `InternalReconnectAfter` 保存来源 token，到期复核，stale 则移除 timer 且
    不发 `StartJoinServerEvent`。
  - `events.rs`：`packet_listener`/`disconnect_listener`/
    `connection_failed_listener` 在消费时比较 event token 与实体当前 token，
    stale A 事件不进 B 的 `LocalPlayerEvents` channel；共享
    `attempt_matches_current`（tokenless 仅当实体也 tokenless）。
  - `container.rs`：`handle_menu_opened_event` 消费时复核，stale A packet
    不得移除当前 B 的 `WaitingForInventoryOpen`。
  - `WorldLoadedEvent` 在 vendor 内无其他消费者（backend 侧受本切片范围
    限制仅做编译迁移，不做 epoch admission）。
- **`Client::new` 快照**：构造时只读快照实体当前 token 并永久保留，
  `disconnect()` 发快照 token；新单测验证 entity 换 B 后仍发 A。
- **文档**：`ReceiveGamePacketEvent` doctest pattern 补 `..`。

### 验收证据

- 新单测（azalea 6 个）：stale Disconnect/Failed 不进 B channel、不装 timer；
  A timer 到期且 entity 已切 B 时移除且不启动新 attempt；stale packet 不改 B
  组件；cancel 三条件；Client::new 快照。azalea-client 新增 2 个（cancel
  mismatch、checked mint）。
- 重复：`cancel_requires_task_owned_and_current_token_match`、
  `stale_reconnect_timer_is_removed_without_starting_attempt`、
  `stale_disconnect_and_connection_failed_are_not_forwarded_to_current_channel`
  各连续 10 次通过。
- Mutation：临时把共享 `attempt_matches_current` 恒返回 true（移除 token
  fence），`stale_disconnect_and_connection_failed_are_not_forwarded_to_current_channel`
  立即失败；恢复后同一命令通过。变异前后已在
  `.codex-tmp/progress-deepseek-new13-vendor.jsonl` 上报
  `temporary_change_active` / `temporary_change_active=false`。
- 门禁：vendor azalea-client lib 6 passed、`--test main` 25 passed、azalea lib
  29 passed；backend lib 182 passed；workspace all-targets check exit 0；
  默认/stable fmt、vendor rustfmt、`git diff --check` 通过；两个
  `.cargo-checksum.json` 全量复核 mismatch=0。
- 仍存风险：backend token↔epoch 绑定与 connect deadline 未接线；testbot
  example 因 `backtrace`/`eyre` 未 vendored 无法离线完整编译（仅 pattern
  迁移）；Paper 实服证据未做；未提交。

## NEW-13 V1 父级审查返修 2：packet source fence（2026-08-03）

### 必修项：`handle_raw_packet` 前置 source admission

- `azalea-client/src/plugins/connection.rs::handle_raw_packet` 现在在函数
  最前面做 fail-closed 检查：`ecs.get::<AttemptToken>(entity).copied() ==
  Some(attempt_token)` 才继续；entity 不存在、无 token、或 token 不匹配都
  直接安全忽略。顺序为：先 admission，再反序列化，再 process handler，再
  排队事件；stale A packet 不再可能先触发 ECS 状态修改/派生事件再被下游
  fence 拦截。
- 新增 3 个 deterministic 单测（`plugins::connection::tests`）：
  - `stale_attempt_packets_are_ignored_before_parsing`：stale A + invalid
    bytes → Ok 且零 queued；同一 bytes 用 matching B → 到达 parser 返回
    Err，证明短路发生在反序列化之前。
  - `missing_or_tokenless_entities_are_fail_closed`：无 token 组件与已
    despawn 的 entity 均安全忽略。
  - `stale_serializable_packet_does_not_mutate_ecs_and_matching_applies`：
    真实可序列化 `ClientboundSetHealth`，stale A 不改 `Health` 组件且零
    事件；matching B 正常应用并排队带 B token 的事件。
- Mutation：临时移除 source admission 后，
  `stale_attempt_packets_are_ignored_before_parsing` 在 `result.is_ok()`
  断言失败；恢复后通过。变异前后已上报 `temporary_change_active` /
  `=false`。

### 文档/计数机械修正

- checksum JSON 条目计数改为：azalea-client `files` 映射 9 个既有 hash
  修改 + 1 个新条目（共 10）；azalea 6 个既有 hash 修改；checksum 文件自身
  不在 `files` 映射内，按工作树 diff 另计。已同步 vendor-patches 清单。
- `Client::new(entity, ecs)` 迁移说明改为“构造时只读快照当前 token；entity
  无 token 才为 None”，不再写“保持 legacy token=None”。
- `AttemptToken::mint_from` 注释改为准确表述：返回本次分配的唯一值（更新前
  的 counter 值）并推进 counter；到 `u64::MAX` 返回 `None` 且不推进。

### 收尾 gates

- vendor：azalea-client lib **9 passed**、`--test main` **25 passed**、azalea
  lib **29 passed**；source-fence 具名测试连续 **10 次通过**。
- backend lib **182 passed**；workspace all-targets check exit 0；默认/stable
  fmt、vendor rustfmt、`git diff --check` 全部通过；两个 `.cargo-checksum.json`
  全量复核 mismatch=0。
- 测试边界：source fence 证据使用真实 `handle_raw_packet` + 真实反序列化/
  SetHealth handler，直接断言 queued events 与 ECS 组件效果，无网络依赖；
  不替代 Paper 实服验证。backend token↔epoch 绑定与 connect deadline 仍未
  接线；未提交、未 push。

## NEW-13 V2：backend source `AttemptToken` ↔ epoch 一对一绑定（2026-08-03）

### 实现

- `SourceTokenBindings`：token↔epoch 双向受控映射，历史 token 不可重绑、
  epoch 不可换 token、同 pair 幂等；条目随会话内 stamped attempt 数单调
  增长且不清理（见 patch 清单的增长边界说明）。
- `CanonicalSourceAdmission` 与 `AttemptAdmissionState::Reserved/Bound` 携带
  `Option<AttemptToken>`；`admit_canonical_source_with_token` /
  `canonical_source_still_valid_locked` 对 stamped source 校验
  `bindings.matches(token, epoch)` 并跳过 legacy fence；tokenless 仍走
  `EntitySourceFence`（same-entity rebind 后 fail closed）。
- 接线点：`StartJoinServerEvent` reader 绑定 reservation（epoch）；
  `Event::Init` 用 `Client::attempt_token()` 消费 reservation；
  reconnect return 的 Client token 必须与 StartJoin token 相同才绑定并
  `set_active_client_if_current`；packet/WorldLoaded/Disconnect/
  ConnectionFailed reader 用真实来源 token；`handle_client` 各副作用 arm 在
  执行前经 `client_is_current_owner`；`SwarmEvent::Disconnect` 经
  `claim_reconnect_with_token`；`take_canonical_connection_failure_followup`
  复核 token，stale A 不能作用于 B。
- 生产 `record_server_position_corrections` 不再直接 `emit_if_running`，改走
  stamped admission + `emit_canonical_observation_event`。

### hung-test 根因与修复（父级运行时纠偏）

- 根因：新增 admission→publication race 测试把 publisher 阻塞在
  `command_admission` 锁内的 `event_admission_hook`，而主线程重绑 B 需要同一
  把锁 → 永久等待（父级采样 CPU delta=0 确认死锁，非冷编译）。
- 修复：新增 `#[cfg(test)] canonical_publication_probe`，在
  `emit_canonical_observation_event` 的**锁外**调用；race 证明改为 publisher
  在锁外窗口挂起、主线程完成 B 重绑、release 后 publication 的
  `canonical_source_still_valid_locked` 拒绝 A。原有锁内
  `event_admission_hook` 测试语义未动。
- 同轮修复生产缺陷：`bind_reserved_attempt_locked` 的 init_path fence 检查
  被限定为 tokenless（`source_token.is_none()`），否则 stamped B 在
  same-entity 复用后也会被 legacy fence 误拒。

### 验收证据

- 新增 7 个 deterministic 测试：A→B 复用恢复生产且迟到 A packet 被拒、
  late A 生命周期/Client/SwarmDisconnect 全路径拒绝、一对一映射幂等、
  历史 token 不能绑后期 epoch、reconnect return token mismatch 拒绝、
  tokenless fallback 首次可用/same-entity rebind fail closed、
  admission→publication race（锁外 probe + barrier）。
- 关键竞态（race、late A、A/B 复用）各连续 10 次通过；backend lib
  **189 passed**；entity_events 37、block_sound 7、light_cache 3、armor 3；
  workspace all-targets check exit 0；默认/stable fmt、`git diff --check`
  通过。
- Mutation：`SourceTokenBindings::matches` 恒 true（移除 token↔epoch 校验）
  后 `stamped_rebind_restores_production_and_late_a_packets_are_rejected`
  在 “late A packet must be rejected at the stamped source admission” 失败；
  恢复后通过，`temporary_change_active=false` 已上报。
- 未完成：pre-Init connect deadline（NEW-15）仍未接线；Paper 实服未验证；
  未提交、未 push；未修改 vendor/contracts/middle/manifest/lock。
