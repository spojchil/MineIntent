# A3 进度日志

## 2026-08-02｜NEW-07 设计审计与实现边界冻结

### 审计结论

- 已完整核对 `移植任务书.md`、中期更新 01–06、`需要决策的新问题.md`，以及 A/A2/B 的 NEW-07、viewport、contracts、runtime 与 Information provider 进度记录；基线为 `ae13ce6`，工作树初始干净。
- 以 `中期更新-06.md` 为最高优先级：`BlockInfo` 是唯一模型可见方块表示。无初始白名单属性时序列化为名称字符串；有属性时序列化为首个 `name` 加全部白名单属性的扁平对象。full visibleBlocks、standing/looked、directed seen 与 occluder `by.block` 统一走该 DTO 的唯一 serializer/presenter。
- 初版全局视觉属性白名单冻结为：`age`、`axis`、`attached`、`attachment`、`bites`、`bottom`、`candles`、`conditional`、`delay`、`disarmed`、`east`、`east_wall`、`enabled`、`face`、`facing`、`half`、`hanging`、`hinge`、`honey_level`、`in_wall`、`instrument`、`layers`、`level`、`lit`、`locked`、`mode`、`moisture`、`north`、`north_wall`、`occupied`、`open`、`orientation`、`part`、`pickles`、`powered`、`rotation`、`shape`、`short`、`signal_fire`、`snowy`、`stage`、`triggered`、`unstable`、`up`、`vertical_direction`、`vine_end`、`wall`、`waterlogged`、`west`、`west_wall`。明确排除树叶 `distance`、`persistent` 及不在名单中的内部状态；初版按方块名 presenter registry 为空，属性原样透传。字符串 `true`/`false` 和整数/有限数字文本在 DTO 边界恢复为布尔/数字，`false` 不省略。
- full wire 仅升级方块槽位，保留 256 方块、32m、暴露面/遮挡、实体上限 8、`truncated` 与 legend 机制；directed 输入继续由 contracts 的平铺 `mode + positions?` 驱动，批量上限 16，并新增重复坐标拒绝。
- directed kernel 以当前 pose/world 原子 capture 为输入，按稳定顺序生成 `outside_fov`、`too_far`、`occluded`、`chunk_not_loaded`；可叠加但不重复。`distance`/`max` 只在 `too_far` 出现，`by` 只在 `occluded` 出现且只携带首命中的可见表面；可见 `air` 进入 seen。DTO 反序列化与 schema 同步校验闭合原因、顺序和条件字段。
- 管理侧 NEW-08（`BlockReadResult::OutOfWorld`）为维护者保留问题：不映射为 `chunk_not_loaded`，不泄漏目标内容；kernel/runtime 保留独立 `OutOfWorld` 内部错误路径，生产 directed capability/participant assembly 继续 deferred，不宣称完整发布。

### 实施顺序

1. 先落 `BlockInfo`、presenter registry、full projection DTO 与 contracts/middle fixtures/schema/tests。
2. 再补 view 输入重复坐标拒绝与 directed DTO/schema/kernel；直接调用 directed read 的 `OutOfWorld` 走独立内部错误。
3. 最后把 directed kernel 接入 runtime 的原子 capture、取消/deadline/stale/revision 边界，并补齐 backend contract adapter。

### 当前 deferred

- 不修改 agent/prompt/AgentRun/driver/runner/ToolDispatcher，也不创建生产 view capability/participant assembly。
- NEW-08 的对外 `OutOfWorld` 分类与完整 directed capability 发布等待维护者后续裁定。

## 2026-08-02｜首次定向编译

- 已完成首轮 contracts/backend/middle 定向编译触发；编译首先暴露既有 I02 fixture 仍使用旧 `{name,position}` 与字符串 visible-block tuple，说明 full DTO 升级的 fixture 同步尚未完成。
- 下一步：先同步 contracts I02 fixture 与 wire JSON，再继续 backend adapter、middle schema/test 与 runtime 接口接线。

## 2026-08-02｜BlockInfo、full DTO 与 directed kernel 定向回归

- `BlockInfo` 已进入 contracts 唯一 serializer：无白名单属性为字符串，有属性为扁平 `{name,...}`；初版白名单、类型恢复、`false` 保留、树叶 `distance`/`persistent` 排除及空 presenter registry 均有回归。
- full projection 已升级 visible block 行与 standing/looked 三处，backend 统一使用同一 `BlockInfo`；Information provider 的 definition/schema/fixture/test 已同步，保留原 full 预算、暴露面、实体上限和 truncated 语义。
- view 输入已增加 16 项上限下的重复坐标拒绝；directed kernel 已在 backend viewport 唯一路径实现 seen air、outside_fov、too_far、occluded、chunk_not_loaded、可叠加原因、条件字段、首命中 occluder 与 NEW-08 独立 OutOfWorld 错误。
- 定向测试结果：contracts capability 12/12、contracts I02 17/17、middle viewport provider 9/9、backend all-targets 45/45；runtime directed atomic 接线与取消/deadline/stale/revision 回归仍在进行。

## 2026-08-02｜runtime 原子边界与 provider wire 收口

- runtime `read_directed_viewport` 复用 full viewport 的 capture、generation/world pointer、source、epoch、取消/deadline、重试和 revision 边界；成功 directed read 与 full read 共用原子 capture，revision 按成功读递增。
- runtime 定向回归 4/4：原子 capture/revision、取消、deadline、generation 变化重试、stale epoch 拒绝；输入重复坐标在入口拒绝。`OutOfWorld` 保持 `DirectedViewportError::OutOfWorld` 独立路径，不转换为 `chunk_not_loaded`，不返回目标 block 信息。
- middle provider schema/fixtures 已锁定 rich `BlockInfo`、显式 `lit:false`、隐藏 `distance` 拒绝，并核对 standing/looked `{block,position}` 与 visibleBlocks `[BlockInfo,x,y,z]`；full 的 256/32m、暴露面、实体上限 8、`truncated` 与 legend 回归保留。

## 2026-08-02｜真实变异验证

- 临时将 `distance` 加入 `is_visible_block_property` 白名单并运行具名测试 `block_info_has_one_string_or_flat_object_wire_and_typed_whitelist_columns`；测试按预期失败，实际输出泄漏 `"distance": 3`，而断言要求隐藏该属性。
- 已恢复白名单实现；变异仅用于证明新增 BlockInfo 隐藏属性测试有效，未保留变异代码。

## 2026-08-02｜收口状态

- 初版冻结白名单仍为：`age`、`attached`、`attachment`、`axis`、`bites`、`bottom`、`candles`、`conditional`、`delay`、`disarmed`、`east`、`east_wall`、`enabled`、`face`、`facing`、`half`、`hanging`、`hinge`、`honey_level`、`in_wall`、`instrument`、`layers`、`level`、`lit`、`locked`、`mode`、`moisture`、`north`、`north_wall`、`occupied`、`open`、`orientation`、`part`、`pickles`、`powered`、`rotation`、`shape`、`short`、`signal_fire`、`snowy`、`stage`、`triggered`、`unstable`、`up`、`vertical_direction`、`vine_end`、`wall`、`waterlogged`、`west`、`west_wall`；树叶 `distance`/`persistent` 与未知内部属性排除。
- NEW-08 / `OutOfWorld` 对外四原因闭合映射和生产 directed capability/participant assembly 仍 deferred；没有声称完整生产 capability。
- directed schema 也以 15 个非空规范 reason 子集锁定 `outside_fov`、`too_far`、`occluded`、`chunk_not_loaded` 的稳定顺序；DTO、schema 与 runtime 分类保持一致。
- 最终验证（上一轮收口）：workspace `cargo test --workspace --all-targets --locked --offline` 汇总 314 passed、0 failed、0 ignored；workspace all-targets check 通过；默认与 `+stable` rustfmt check 通过；`git diff --check` 通过。
- 未 commit、未 push；最终工作树仅保留本 A3 所有权范围内的未提交改动和本日志，NEW-08 deferred 仍是唯一维护者侧未决项。

## 2026-08-02｜管理侧独立复审定点返修

- directed 先做纯几何分类：`outside_fov` 或超过 directed 32m 上限时不调用目标读取、不做射线、不生成 `by`；极端 i32 坐标零读取回归通过。近且在视锥内才读取目标，并复用 full 的 `ViewportOptions::predicate`/`is_visible_candidate`；新增中心射线受挡但 exposed-face 可达时 full/direct 差异回归通过。
- directed 结果分类修正：`Clear + target Unloaded` 仅产生 `[chunk_not_loaded]`；`Hit` 可产生 `occluded`/首命中 `by`，目标未加载时仍可叠加 chunk；`OutOfWorld` 仍是独立内部错误。
- runtime `control_wakeup_error` 现在接收当前 operation；worker wake cancellation/deadline 回归在 worker 第 4 次检查后触发，外层 timeout 有界，最终均精确报告 `read_directed_viewport`。原先第 5 次 Condvar 阻塞及测试返回后 release 的死锁已移除，gate 有超时兜底，不遗留永久阻塞线程。
- strict DTO/schema/provider 收口：BlockInfo 拒绝只有 `name` 的对象和重复视觉属性键，构造器过滤为空仍序列化字符串；BlockInfo schema/provider 要求至少一个视觉属性。`DirectedViewportProjection` 拒绝 seen+unseen 超过 16，结果 schema 两数组均 `maxItems:16`。

## 2026-08-02｜本轮真实变异与最终验证

- 临时恢复 `Clear => Occluded`，运行具名测试 `directed_clear_to_unloaded_target_reports_only_chunk_not_loaded`；按预期失败，实际 reason 为 `[Occluded, ChunkNotLoaded]`，断言要求 `[ChunkNotLoaded]`。已恢复正确分类。
- 本轮定向结果：具名 clear 回归 1/1、worker wake 回归 1/1；backend directed 6/6、runtime directed 5/5、backend all-targets 53/53、contracts capability 12/12、contracts I02 19/19、middle viewport 9/9。
- 最终 workspace `cargo test --workspace --all-targets --locked --offline` 汇总 318 passed、0 failed、0 ignored；workspace all-targets check、默认/+stable rustfmt check、`git diff --check` 全部通过。未 commit、未 push；NEW-08/deferred 与 production directed capability 范围不变。

## 2026-08-02｜管理侧最终示例与总预算收口

- contracts I02 fixture 的 legend 已从旧 `[block_name,x,y,z]` 更新为 `[BlockInfo,x,y,z]`，避免示例描述与已升级 wire 冲突。
- directed 输出预算回归新增 `seen=8 + unseen=9` 的分拆超限负例；两边各自不超过 schema 的单数组上限，但 DTO 合计 17 必须拒绝，精确锁定总量上限 16。
