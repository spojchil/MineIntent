# 中期更新 06（2026-08-02）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-05，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## NEW-07 裁定（维护者 2026-08-02 批，第三档）

1. **轮末帧消息契约 = 1A**：独立判别式 user message，只出现在全部 tool messages 之后：

   ```json
   {"protocol": "mineintent.viewport-frame.v1", "at": "<UTC>", "viewport": { ...ViewportValues }}
   ```

   仅当本轮执行过 ≥1 个身体工具（成败均计）时追加；纯 say/view 轮不追加。采样失败发显式失败帧 `{"protocol": ..., "at": ..., "viewport": null, "unavailable": "<简短原因>"}`，不静默省略——模型该知道「试过看但没看到」，而不是误以为世界没变。不复用开场 `AgentFrame`，不伪装成任何 tool call 的结果。

2. **逐工具 observationAfter = 2A**：身体工具保留**不含 viewport** 的 `observationAfter`（伤害、声音等中断事实的既有到达路径不变）；轮末帧只承载视口；显式 `view` 的结果自身返回所选模式数据。

3. **可见方块统一表示 BlockInfo**——唯一序列化器，全量行 / standingOn / lookedAt / directed seen / 遮挡物 by 共用，不造两套轮子：
   - `BlockInfo = "<name>"`（该状态无白名单属性）`| {"name": "<name>", <白名单属性全列、真实值>}`（string | object 联合，机器可无歧义区分）。
   - **全局属性名白名单**（跨方块共享）：facing/lit/open 一类视觉属性入名单；树叶 `distance`/`persistent` 一类玩家不可见属性不入。初版名单实现时冻结并登记，增删走登记。
   - **不做「省略=默认」**：属性全列真实值（`lit: false` 显式可读）。理由：省略需要每种方块的默认值表这份额外逻辑，且让模型做缺席推理是理解负担。
   - **呈现钩子（维护者增设）**：序列化器内保留按方块名注册的呈现处理接口——特殊方块在返回前可对白名单属性做映射/改写（例：小麦 `age` 数字 → 单词描述），协议原始值不一定适合原样给模型。**初版注册表为空、一律原样透传**；条目按实际使用登记增补。钩子在唯一序列化器内部，不构成第二条路径。

4. **directed 输出 DTO 冻结**：

   ```json
   {
     "seen": [
       {"at": [124,65,-37], "block": {"name": "furnace", "facing": "north", "lit": true}},
       {"at": [10,64,3],   "block": "air"}
     ],
     "unseen": [
       {"at": [9,70,2],   "why": ["outside_fov"]},
       {"at": [1,64,900], "why": ["too_far"], "distance": 47.2, "max": 32},
       {"at": [5,63,8],   "why": ["occluded"], "by": {"at": [5,63,7], "block": "stone"}},
       {"at": [0,64,200], "why": ["chunk_not_loaded"]}
     ]
   }
   ```

   - 归属即判别（`block` 在 seen、`why` 在 unseen），无冗余 visibility 字段；行自带坐标（拆组后承重，非冗余）。
   - `"air"` 合法（可见空气 = 该位置当前为空）。
   - `why` 闭合枚举：`outside_fov` / `too_far` / `occluded` / `chunk_not_loaded`，可叠加；`distance`/`max` 仅随 too_far；`by` 仅随 occluded、BlockInfo 形状、只能是视线首命中的可见表面、可省略。
   - 批量上限 **16**（登记后可调）；输入重复坐标拒绝；不可见一律不返回目标内容。

5. **prompt 制品**：新增 `participant-system/v2`，装配层显式选择；v1 字节冻结留回放（NEW-04 纪律）。v2 观察机制段按「身体工具只返回效果；执行过身体动作的轮末收到一份视野帧；主动看用 view——full 可能截断，想看的坐标没出现用 directed」重写。

## 豁免名单增补（接更新 01 §2、05 §2）

| 旧结构 | 裁定锚点 | 直达形态 |
|---|---|---|
| visibleBlocks 行 `[name, x, y, z]` | 本更新 §3 | 行首槽升级为 BlockInfo |
| standingOnBlock / lookedAtBlock `{name, position}` | 本更新 §3 | `{block: BlockInfo, position}` |

更新-05 §2「其余视口行为仍按 oracle 等价移植」相应收窄：预算 256/32m、暴露面判据、实体上限 8、truncated 标志、legend 随数据机制不变。

## 台账

NEW-07 已裁定；后续新增待决从 NEW-08 起。
