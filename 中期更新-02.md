# 中期更新 02（2026-08-02）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01，再继续各自队列。
> 本文件是任务书与更新 01 的增补；冲突处以本文件为准。

## 1. 「需要决策的新问题.md」五条全部裁定（维护者 2026-08-02 批）

- **NEW-01：A**。`protocol = "mineintent.agent-context.v4"`；`stable` 结构为 `{ "memory": "..." }`（单数字符串）。字段语义冻结：必填、仅接受字符串、允许空字符串、显式 `null` 拒绝、未知字段继续拒绝。
- **NEW-02：A**。路径 = `<MINEINTENT_DATA_DIR>/memory.md`，数据目录默认 `.mineintent`，相对路径按**进程启动 CWD** 解析；旧文件对应 `<MINEINTENT_DATA_DIR>/memories.json`。此条为 TS oracle 等价（`src/app/config.ts:12,36`；`src/app/mineintent-app.ts:26`），不是自由选择。
- **NEW-03：B**，不双发，单个原子适配提交切换。加强依据：TS oracle 的 v1 契约（`src/minecraft/contracts.ts` `BackendEventKind`，行 214-223）共九个 kind，**本来就没有** `keep_alive`/`motor`/`error`，且 kind 名本来就是 `self`（无 `self_state`）——这三个是 Rust 施工期自加的泄漏，切 v2 即回到 oracle。子冻结：
  1. v2 kind 集合 = oracle 九个：`lifecycle`/`self`/`world`/`entity`/`block`/`sound`/`chat`/`player_list`/`snapshot_changed`。
  2. lifecycle 各分支 payload 按 TS `BackendLifecycleEvent` 逐分支 1:1 冻结（含 reconnect、closed、faulted、respawn、dimension change）。
  3. motor/命令反馈走 SEAM-03 进程内完成 future；内部错误走 lifecycle `faulted`（`BackendFailure`）；`keep_alive` 属传输内部事务，不出事件流。不新建诊断通道——调试需求走 MIG-03 的 debug HTTP。
- **NEW-04：A**。仓库内版本化纯文本制品，编译期嵌入（`include_str!` 一类）；按 key/version 精确查找，未知项失败关闭；默认 key/version 的唯一来源 = 装配层显式给定，不做隐式回退。运行时可编辑模板属移植后特性，想要另立裁定。
- **NEW-05：A**（该问题实际已由更新 01 §1 裁过）：迁移验收完成前严格顺序执行；租约化轮内并行属移植后增强，行为变化（如 `[move,move]`）登记后实施。

「需要决策的新问题.md」保留为升级台账：本批五条标记已裁定，后续新增待决从 NEW-06 起继续登记。

## 2. 施工仓库 main 定位修正

main = 三代理的**集成分支**。合并由管理进程执行（既有的代理侧合并全部追认）；各自测试绿即可合入。真正的验收门只有两道：gate b 完成门槛、迁回主仓库的 PR。更新 01 §4「里程碑合 main 维持验收后合入」一条**废止**。
