# MineIntent 工作约定

## 阅读顺序

1. [产品宪法](./PRODUCT_CONSTITUTION.md)：根目的与硬边界；草案未获维护者公开确认准确版本并合入默认分支前，
   不得自行补成产品决定。
2. [README](./README.md)：当前可运行能力和已知缺口。
3. 目标代码、类型和测试：程序当前行为的产生方。
4. [来源索引](./docs/source-index.md)：历史为何形成；历史证据不自动成为当前权威。

## 产品判断

- 初始记录对恢复原始愿景很重要，但“最早”不等于“正确”或“仍有效”。
- 当前产品不保证某名玩家获得陪伴，也不强迫 AI 追求独立；产品建立选择条件，具体社会与行动结果属于 AI。
- `merged`、测试通过、代码存在和多个文档互相引用，都不能单独证明维护者接受了产品判断。
- 行为变化必须把新增的产品假设翻译成短句，让维护者可以不读实现细节也能判断。
- 会拒绝完整、合法输入的 guard，必须说明它在执行哪条已确认边界；不能用一个静默 `return` 隐藏社会政策。
- 对仓库事实作判断时给出代码、提交、Issue 或 PR 锚点。产生方一条命令可达时，优先核对产生方。

## 工程硬边界

- 世界在模型思考时不会暂停：轮次与身体操作必须有取消、超时、作用域失效和资源释放语义。
- 模型输出是提议，不是事实。成功与失败以真实游戏状态和可验证结果为准。
- 协议客户端能读取的数据不自动等于 AI 角色按玩家式边界可获得的信息；信息与动作必须遵守共享世界条件。
- 模型只使用宿主声明的能力，不执行任意生成代码。
- 在事实、安全和能力边界内，社交判断、关系解释和活动选择属于 Agent；中间层不得编码对启动者或具体结果的偏好。
- API 密钥、令牌、私人聊天、世界存档和未脱敏运行日志不得提交或回显。

## 工作方式

- 提新模块前先用 `rg` 搜索已有实现。
- 优先交付可以在真实世界中失败、观察和继续的纵向切片。
- 测试记录已经理解的契约；产品假设还需要对抗性阅读、反事实主体和真实运行验证。
- 设计理由写进 commit message 或紧贴代码的注释。未决工作放 GitHub Issue，不新增长期架构、状态或提案文档。
- `main` 只通过合并 PR 变更；本地恢复工作先留在独立分支。

## 文档边界

项目级活文档只保留产品宪法、用户运行指南、Paper 验证指南和历史来源索引。README 可以给出短的当前能力清单；
不再维护会随实现腐烂的系统快照、巨大架构说明、提案登记册或历史正文副本。旧内容由 Git、Issue 和 PR 保存。
