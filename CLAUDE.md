# MineIntent 工作约定

产品目标：原版 Minecraft Java 1.21.1 里的长期 AI **玩家**，标准是长期游玩中与一个安静的人不可区分。

**没有终止任务。**"这一轮算成功了吗"不可答，所以不要按任务完成度做取舍，也不要照搬任务型 agent
框架的假设——压缩、记忆、"我做完了"全都建立在有终止条件之上。

**不是 demo。**"在设计好的场景里成功找到一次羊、走过去、说一句话"不算成功。同伴的失效是累积的：
一次误判不要紧，一千次里三十次会让"像个人"彻底崩掉。

## 两条约束 + 两条梯度

四个定义问题，但分成两类，因为两类的处置方式完全不同。

**约束**（有外部锚点，基本是布尔的，**违反就是 bug，当场修**）：

1. **实时交互** → 锚点是 deadline 与取消：世界在你思考时不会停。
2. **什么算玩家动作 / 合法性** → 锚点是键鼠能表达什么。所以移动是"按住某键 N 毫秒"而不是"走到 X"，
   "没有寻路"因此是有原则的而不是偷懒。观察的锚点同属这条：原版玩家标准，F3 显示自身坐标和准星
   方块、其余可推导，所以绝对坐标不是特权信息，而实体 id、entity key、world id 这类**句柄**是。

**梯度**（没有锚点，只能枚举失效，而且**互相之间要交换**；退步是债，记下并权衡，不是当场修）：

3. **可信度**（像不像一个人）→ 不可区分是否定性质：验证不了，只能枚举"什么会暴露"逐条消掉。
4. **在场感**（是不是个好同伴）→ 可能没有稳定答案，它是关系性的。同样用枚举失效代替定义美德。

**该不该拆成两条轴的判据：一个改动能不能帮到一个、同时伤到另一个。**能就必须拆开——不是为了
分类学，是为了让冲突有地方显形。按这个判据，上面四条两两都能冲突：缓存感知能降延迟但等于看到
过期状态（1×2）；瞬间回话对交互是优点、对"像人"是暴露（1×3）；遗忘是"像人"的必要条件、
却是"好同伴"的禁忌（3×4）。

**顺序更像依赖而不是优先级，但这条不能说死。**约束的失效会盖住梯度的印象——同伴在对话中途卡死
三秒、或者贴地寻路瞬移过去，没人会去评价它像不像人。所以**评价**梯度确实要等约束先立住。

但**做**梯度不必等，而且一直在做。凡是行为由措辞或接口形状决定、不需要新子系统的地方，成本接近零：

- `prompt.py:49`「如实说'正前方没看到方块'即可，不要当成故障或编造一个方块」——不是功能陈述，
  是用一个具体例子塑造人味。`:55` 同类；`:41`「你与玩家共同游玩，不是任务机器人」；`:42`「简短自然」。
- **不止提示词。**`speech/chat-input.ts:31` 的 `addressedToCompanion`：只在被指名时才回应，
  人不会接每一句话——这既是可信度也是在场感，而它是机制不是措辞。同文件的 `isUnambiguousSafetyStop`
  同理。记忆里存的是"和 Alex 一起收集木材"这类共同经历而不是任务日志，也属于这一类。

所以正确的做法不是"先约束后梯度"，而是**造约束时顺手把免费的梯度收益拿走**，并明确记下没拿到的
那些——下面两份清单就是这个账本。真正必须等的只有两类：需要仪器的（衡量一段会话有多像人），
和需要新子系统的（主动行为、记忆衰减）。

遇到梯度类问题时，**先找锚点，不要直接回答**。

## 事实主张必须有锚点

对代码库或文档下任何事实判断，指出具体位置。带不了就先去读，不要凭回忆陈述。
估算数量（"大约 N 个文件受影响"）也算事实判断：要么去数，要么明说是估计。

- **对话里**用 `file.ts:123`，因为当场可核。
- **写进长期文档时**用符号名（函数/类/常量），因为行号腐烂得最快。

历次严重错误都来自未引用的回忆：引用过一句 README 里根本不存在的话、估计"30+ 个文档可删"而实际是零、
提议新建一个仓库里已经存在得更完整的模块。带了锚点的判断没有出错过。

我分不出"我记得这个"和"我正在合理地编造这个"——两者手感相同，所以内省靠不住，
必须用"能否给出锚点"这个外部检查代替"我会更小心"。

## 提新方案前先搜仓库

要写新模块、新工具、新抽象之前，先 Grep 有没有已实现。
`src/information/tool-session.ts` 是被漏掉的例子：153 行，比我打算写的更完整，且零生产调用者。

## 没写理由的选择是待决，不是已决

代码里存在但没有记录理由的设计选择，默认当成**尚未决定**并明确问用户。
不要围绕它设计，也不要用"改它风险大"替它背书——项目未发版，没有兼容负担。

例（已闭环，留作这条规则挣回成本的证据）：`perception.ts` 的 `visibleBlocks` 曾用"候选枚举 +
射线射向方块中心"判断可见性。这个选择从未记录过理由，而它是错的——**方块中心不等于玩家看见的表面**。
远处地面方块的中心埋在地表下，射线先进入更近的地面，于是判为遮挡；对闭式 oracle 实测水平看平地
召回 **0/900**。判据已换成朝向观察者的暴露面（`exposed_face`），同一夹具 **900/900**，`blockAt`
调用还更少。

要记住的不是结论而是过程：**这个选择被当成既定事实围着它设计了很久，直到有人去量它。**
量之前它看起来和一个做过的决定没有区别。

## 能推翻当前设计的发现要立刻上报，不要归档

量到一个可能让正在做的事失效的证据，当场说"这可能推翻 X"。
不要写成"记进文档待办"——那是把设计问题降级成记账。上面那个可见性缺陷就被这样降级过一次。

## 测试是棘轮，不是探测器

测试记录已经想清楚的结论，找不到 belief 层面的错误——它由写代码的同一个头脑在同一时刻写出。
真正找到错的是：**对抗性阅读、真实供应商跑一遍、变异测试、测量**。
声称某处正确时说明是哪个手段验的，不要用"测试通过"冒充"已验证"。

- 测试名里出现全称词（untouched / every / always / 全部 / 任何）时，身体必须真的枚举边界
  （小数、负数、NaN、Unicode、深嵌套），否则改名。读自己写的全称测试名会当成已证明的命题去信。
- 测试夹具的输入要来自真实契约（zod schema / 类型），不要手写。手写夹具会漂移到不具代表性的角落，
  而且没有任何东西会察觉。
- 变异测试只在"错了会静默"的地方常规化——资源仲裁、感知判据这类。全库跑变异是无脑堆测试换了件衣服。

## 理由写进 commit message 和代码注释，不写状态文档

描述现状的文档会腐烂，而**错的文档比没有文档更糟**，因为我会信它。
带证据的发现和"为什么这样选"写进 commit message 或紧贴代码的注释——它们钉在让其为真的那次改动上，
不需要维护。不新增描述系统当前样子的架构/状态文档。

## 断言"没有先例"之前，先检查我自己是不是反例

反复出现的毛病：把问题归因于生态缺失，而答案是"我们跳过了标准做法"。
每次都被"你自己就是反例"打破（后台进程、编辑配置、查询运行中的服务、外部漂移）。
先去 `research/repos/` 和上游文档里找。

## 可信度梯度：什么算暴露（append-only，只增不改）

改完约束层的东西，顺手回答一句"这有没有新增一条暴露"。**同一段代码在两个视角下可以评价相反**——
方块镜像在 token 与缓存上是干净的胜利（命中率九成），作为"像人"是退步。

- 特权信息（已由原版玩家标准约束：句柄不可见，坐标可见）
- 反应快于人类区间
- **精确复述坐标、以及绝不遗忘也绝不记错的记忆**。人记得的是"那边有堵墙"，不是 `[11,65,-25]`
- 从不打错字、从不改口
- 走位过于最优
- 边移动边瞬间回话
- 永不主动、只会应答

## 在场感梯度：什么会毁掉陪伴感（append-only，只增不改）

- 黏人、话太多；以及反过来，永不主动
- 忘掉对方明显在意的事；记住对方希望你别记的事
- 一味顺着，没有独立反应
- 跨会话人格不一致

注意方向：多数 AI 陪伴产品优化**参与度**（随时在线、总是赞同、什么都记得），
而本项目要的是**在场感**，它主动禁止上面每一条。两者是反向拉的。
工具描述就是回答这个问题的地方——`say` 可选调用所以沉默是默认，`remember` 要求"不确定就不记"。

## 两条梯度的第一个已知冲突：遗忘

上面两份清单直接矛盾：**绝不遗忘是暴露**（可信度），**忘掉对方在意的事会毁掉陪伴感**（在场感）。
这不是写错了，它就是梯度层真正的设计问题——不拆成两条轴，这个矛盾会被静默地随手解决掉。

收敛方向：**遗忘必须是人形状的选择性遗忘。**忘方块坐标、忘三天前走过哪条路；永不忘对方在意的事。
人的记忆正是这样偏的——记得情绪和关系，忘掉细节和几何。

这条给出可实现的方向而不只是原则，但**方向要落在哪一层曾经写错过一次**，值得连错一起留着：

原先写的是"方块镜像应该衰减"。这是错的。镜像记录的是**已经说进上下文的东西**，它的失效模式是与上下文
漂移；衰减恰好是人为制造漂移。后果不对称：被衰减的方块再被看到只会重报一次"新看到"（像人会犯的口误，
可接受），但它**消失时将永远无法被报告**——不在镜像里，扫描发现缺席时无话可说。用一个可接受的口误
换一个静默的感知洞，方向是反的。

正确的落点是**内容层**：上下文里还剩什么、`remember` 写下的东西如何组织与淘汰。镜像只跟随内容层——
上下文忘了它就收缩，上下文还记得它就必须还记得。区别不在存储，在**是谁决定记它**，而这一条本身没错。

## PR 与 issue 是公共空间

它们写给不在当时对话里的读者，包括几个月后的自己。以下四条都对应真实写过的东西，不是通用文风建议。

- **不用只有当事人能解码的速记。**"PR A / PR B"这类临时编号在 GitHub 上不存在，读者无法定位任何东西。
  写分支名、issue 号或提交哈希——可点击、可验证。这比人称问题严重，因为它让引用彻底失效。
- **能去主语就去主语。**"通用建议我会跳读"和"通用建议会被跳读"信息量相同，后者读起来是一条规则而不是
  一句自述。确实需要区分角色时才写「助手」或「维护者」，例如归属某个设计想法时。
- **不引用私下讨论。**"我们俩都没提过""你说的那个方案"对读者是断链。要么把内容写出来，要么给出可查的引用。
- **区分实测与推算。**数字要说清哪些量过、在什么条件下量的，哪些是按比例推的。混在一起的表格会让
  推算获得实测的可信度。

## 硬约束

- main 只通过合并的 PR 变更。
- API 密钥永不回显。只在脚本内部读取，只打印状态、长度一类信息。
- provider 前缀缓存有下限，低于它一个 token 都不缓存：Moonshot 256、智谱 512、
  DeepSeek 64-token 单位（实用约 1024）。所以稳定前缀必须足够大才有意义，
  且**易变内容只能追加、绝不能重渲染**——追加一次的时间戳免费，同一个时间戳每轮重渲染是致命的。
