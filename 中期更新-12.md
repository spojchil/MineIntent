# 中期更新 12（2026-08-03）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-11，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## NEW-10 裁定：按冻结稿（维护者 2026-08-03 批，第三档）

### 1. 开场帧精确 wire（`mineintent.agent-context.v5`，`stable` 沿 v4 不变）

```json
{
  "protocol": "mineintent.agent-context.v5",
  "stable": { "memory": "..." },
  "frame": {
    "at": "<UTC>",
    "world": { "dimension": "minecraft:overworld" },
    "pose": { "position": [0.5, 64.0, -7.5], "yawDegrees": 0, "pitchDegrees": 0 },
    "status": { "health": 20, "food": 20, "armor": 15 },
    "hotbar": { "selected": 2, "slots": { "0": ["oak_log", 12], "2": ["iron_sword", 1] }, "offHand": ["shield", 1] },
    "chat": {
      "items": [
        { "username": "bob", "text": "早", "at": "<UTC>" },
        { "username": "alice", "at": "<UTC>", "moved": "events" }
      ],
      "omitted": 0
    },
    "sound": { "...": "现行形状不变" },
    "light": 12,
    "events": [ { "type": "player_chat", "username": "alice", "text": "帮我看看农田", "at": "<UTC>" } ],
    "omissions": { "...": "现行形状不变" }
  }
}
```

字段语义冻结：

- **开场帧无视口段**（OBS-03 维持：开场不带，模型用 view 主动拉）。
- `pose` 取代旧键 `self`（v5 铸新，名实相符）。
- `status.armor` = 原版护甲点整数 0-20，**为 0 整键缺席**；health/food 常显。
- `hotbar.slots` = **稀疏槽位映射**：键为 "0"-"8"，缺席=空格；值为 `[物品名, 数量]` 紧凑元组；主手不复制（`selected` 指向即是，指向空格=空手）；`offHand` 空则缺席。
- `chat.items` 旧→新序，上限 8，`omitted` 计早于窗口的条数；无未读则整个 chat 段缺席。
- `light` = 整数 0-15（F3 口径的自身位置光照）。
- 通用：显式 null 一律拒绝；空集合以段缺席表达；未知字段拒绝。

### 2. `player` 字段整体废除

chat 段完全替代；同一消息全帧恰出现一次。

### 3. 唤醒事件呈现与去重（承更新-11 统一唤醒）

- **唤醒事件在帧内**：开场 = 一条完整帧（接地信息不浪费），触发事件在 `events[]`；「独立事件消息与帧并列」的形状保留为**运行中注入的预备队形态**（先例背书，量大或需保工具结果纯净时另裁引入）。
- **去重规则**：触发消息移入 `events[]` 并携带**结构化全文**（聊天：`{type:"player_chat", username, text, at}`；普通事件仍为 `{type, summary}`）；chat 段在原时间位留移动说明条目——**有 `moved` 无 `text`**，存在即语义。

### 4. 未实现段的处理

- `effects`（药水）**不注册直到实现**：v5 schema 不含该字段，实现时随版本铸新登记。
- `light` **随 v5 进 schema**：backend 自身位置光照读取排同阶段（更新-08/09 已列 A2 面缺口）；帧侧先以 fake-backed 测试推进，不因 light 后到而阻塞。

### 5. 版本锚点

- 开场上下文 → `mineintent.agent-context.v5`。
- 轮末帧（去 `standingOnBlock` 后）→ `mineintent.viewport-frame.v2`。
- **视口 DTO 自带 schema 锚点** `mineintent.viewport.v2`：`viewport-frame.v2` 与 `view` 工具结果内嵌**同一个**锚点；`mineintent.tool-result.v1` 外壳不变，嵌套内容有版本可查、不静默漂移。

### 6. 台账

NEW-10 已裁定；v4 与 viewport-frame.v1 的既有 fixture 冻结保留作回放；后续新增待决从 NEW-11 起。
