# C6：生产 AgentRunner / prompt 原子切换到 v5

日期：2026-08-03
分支：`port/c6-agent-runner-v5`
基线：`825ac19`

## 实现结果

本批完成生产路径的原子切换：

- `ConcreteAgentRunner` 的 contract associated `Context` 固定为
  `JsonAgentDecisionContextV5`；transcript 与 round viewport 的构造路径共用
  该生产 runner，不再以 v4 请求作为默认输入。
- 生产 `initial_messages` 只接受 v5 context。system message 继续通过显式
  prompt template key/version 查表，并以 `stable.memory` 生成；未知 key/version
  仍 fail closed，无隐式模板回退。
- opening user message 直接序列化 v5 `frame`，保持 stable system 在前、opening
  frame 在后的前缀；后续 assistant/tool 消息以及轮末
  `mineintent.viewport-frame.v2` 的追加顺序未改变。
- v4 opening 组装保留为明确命名的 `initial_messages_v4_legacy` helper，仅供
  legacy replay/test 使用。没有让生产同名函数通过泛型或联合参数静默接受两种
  版本；v3/v4 DTO、fixture、严格反序列化及回放测试仍由 contracts 保留。

## 测试覆盖

middle agent 的 prompt、runner、round-frame、transcript 测试均改用现有
`fixtures::agent_context_v5()` 构造请求。具名首请求断言确认模型第一次 request
看到的 opening JSON 与 v5 `frame` 完全相等，并逐项确认：

- `light`、hotbar slot `0` 和 `chat.items[].moved = "events"` 保留；
- 对应 `events` 中的 `player_chat`、sender 和文本保留；
- 根级旧字段 `player`、`self`、`inventory`、`timeOfDay`、
  `standingOnBlock` 均不存在；
- 第二次 request 重放同一 stable/opening 前缀，round-frame 测试仍确认轮末
  `viewport-frame.v2` 在正确位置追加，transcript 测试确认 v5 opening 原样落盘。

既有 limits、取消优先级、panic 隔离、N-in/N-out、transcript fail-open、工具
调用顺序及 16 次 model request 上限未改动，并由原测试继续覆盖。

## Mutation 证据

在 `crates/middle/src/agent/prompt.rs` 中临时让 production opening wrapper
删除 v5-only `light` 字段，然后运行：

```text
cargo test -p mineintent-middle --test agent_runner concrete_runner_composes_v5_once_and_maps_run_id_tools_usage_and_model --locked --offline
```

具名测试按预期红灯，失败差异显示实际 opening frame 缺少 `light`；立即恢复原
production 序列化调用后，同一具名测试绿灯。contracts、supplies 和 fixture
均未参与 mutation。

## 门禁结果

以下门禁均通过：

- middle agent 定向 targets：`agent_context_v5`、`agent_driver`、`agent_prompt`、
  `agent_round_frame`、`agent_run_loop`、`agent_runner`、`agent_transcript`；
- `cargo test --workspace --all-targets --locked --offline`；
- `cargo check --workspace --all-targets --locked --offline`；
- `cargo fmt --all -- --check`；
- `cargo +stable fmt --all -- --check`；
- `git diff --check`。

隔离 worktree 初始缺少 `vendor` junction，早期定向命令曾用 CLI source override
只读指向根 `vendor`；管理侧补好 junction、清理首轮残留 cargo/rustc 后，workspace
test/check 均按上面原始命令、不带 override 通过。首轮 workspace test 的外层等待
曾超时但没有代码失败诊断，最终缓存后的同一命令返回 0。

## Deferred 与边界

- Participant 装配、backend/app wiring、light producer 及任何 producer 迁移不在
  C6；后续 Participant 必须构造 v5 request，不能回退生产 runner 到 v4。
- contracts v5 DTO/fixture、backend、supplies/simple-agent、TS model/runtime
  oracle、根 manifest/lock、计划和总进度均未修改。
- 不在本批扩展 v4 compatibility API；v4 仅保留显式 legacy/replay/test 边界。
- 不执行 merge、rebase、cherry-pick、push，也不修改 main。
