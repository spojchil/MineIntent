# 移植任务书：TypeScript 中间层 → Rust（含 Agent 服务内联）

> 2026-08-01 起生效。本仓库从「backend」升格为项目本体：中间层与 Agent 服务全部迁入，TypeScript 与 Python 退役。
> 规范性附件在 [移植计划/](./移植计划/)（模块清单、测试归类表、能力面差距表、所有权划分、待决问题），由独立规划任务产出并已验收；本任务书给出附件之上的裁定与编队。

## 0. 背景与已定裁定（均为维护者 2026-08-01 拍板，不得在实现中重议）

1. **终态**：底层交互 + 中间层 + Agent 循环全部 Rust，单进程。TS 是行为 oracle（参考答案），不是要维护的代码；Python agent-service 一并移除，「分层解耦的思想保留」——层间边界保留为进程内 trait，不保留进程边界。
2. **定性**：本次是**等价迁移**。完成标准 = 与 TS oracle 行为等价；挖掘/放置/GUI 等身体能力扩展另立路线（对照清单见主仓库 docs/minecraft-client-capability-panorama.md）。
3. **登录**：只做 offline；Microsoft 正版登录记为发布范围限制，后议。
4. **完成门槛（MIG-04）**：b 档 —— 全部行为测试逐条对应通过 + backend 既有 M0–M6 + 一次「Paper 26.1.2 + Rust 全栈 + 假模型 provider」纵向场景实测。真实模型场景是加分项，不进门槛。
5. **公屏历史（DELTA-03）**：移植不夹带新结构；现行三类寻址唤醒规则就是 oracle。W08b/W08a 是移植后的第一个功能包。
6. **记忆（DELTA-01）**：A11/M04a/M11 已入 产品.md（主仓库 PR #122）。主仓库将先落「记忆重构」PR（单文本、及时落盘、组装时读文件）；**记忆模块以重构后的 TS 为 oracle，在该 PR 合并前 memory 支线只做归类表中两方案可共用的边界测试**。
7. 产品未决项（Q01–Q15 等）继续未决；发现实现绕不开某个未决项时，登记阻塞、不得替产品拍板（G05）。

## 1. 编队与所有权

- **两个移植代理并行**：代理 A =「世界与信息面」，代理 B =「决策、工具与应用运行面」。文件所有权、只读依赖、I01–I04 契约冻结件、提交与集成顺序、防冲突规则**全部以 [移植计划/所有权划分.md](./移植计划/所有权划分.md) 为准**，本文不复述。
- 身份建议（由维护者启动时决定）：A fork 自 M0–M6 后端代理（继承 azalea/工具链/证据手艺）；B fork 自规划代理（已通读全部 TS 源码与测试）。
- 各自维护 `进度日志-A.md` / `进度日志-B.md`，避免写冲突；补给申请两份日志各自登记。
- 仓库结构：本仓库改为 Cargo workspace —— `contracts`（薄契约 crate，DTO/trait/fixture）、`backend`（现 src/ 迁入）、`middle`（中间层与 Agent 循环）。根 manifest/lock/toolchain/CI 由 B（集成 owner）独占；`backend` 与 `contracts` 内 information/adapter 部分由 A 独占（对齐 所有权划分.md 的 manifest 规则）。

### 1.1 工作区机制（2026-08-01 已搭好）

- 代理 A 工作目录：`C:\Users\20695\Desktop\cc\MineIntent-backend-rs-A`（分支 `port/a-world-info`）；代理 B：`...\MineIntent-backend-rs-B`（分支 `port/b-decision-runtime`）。两者是同一仓库的 git worktree，共享对象库——`git fetch` 不需要，直接 `git merge <对方分支>` 就能取到对方已提交的冻结契约。谁也不进对方目录。
- `supplies/` 各子目录与 `vendor/` 是指向主目录的 junction（共享同一份物料）。supplies 只读纪律照旧；唯一例外是 A 按 §5.5 修 supplies/azalea——注意该修改对两个 worktree 同时可见，A 提交补丁前先在进度日志预告。
- `server-run/`、`target/` 各 worktree 独立，互不干扰。
- **P0 顺序约束**：B 先提交 workspace 骨架（根 Cargo.toml members、空 crate 目录、src/ → crates/backend/ 的移动），A 合并该提交后再动手——避免两边同时挪根路径。A 在等待期间可先做只读工作（I01 契约映射草案）。
- 里程碑合并到 `main` 由维护者侧验收后执行；两代理不直接写 main。

## 2. Oracle 基线与锚点

- TS oracle = `supplies/mineintent-main` @ `6fb3ed0`（与全部附件锚点一致）。此后主仓库已合并 #121（gitignore）、#122（产品修订 A11/M04a/M11）、#123（能力全景文档）、#125（speech 测试去竞速）——只有 #125 触及 src/，且改的是测试等待机制而非行为断言；迁移该测试时按归类表保留行为语义、用确定性时间控制，不复制真实计时竞速。
- 官方反编译源码、azalea 克隆、Go 客户端等既有 supplies 地位不变（见 supplies/MANIFEST.md）。

## 3. 接缝裁定（对 移植计划/待决问题.md 的逐条答案）

标注【维】= 维护者已裁定；【设】= 接缝设计裁定（Claude，2026-08-01）——【设】项如与实现证据冲突，登记后找维护者，不得静默改。

| 条目 | 裁定 |
|---|---|
| SEAM-01 | 【维/设】链接 crate（workspace 内直接依赖）。stdin/stdout CLI 保留为诊断与验收入口，不做生产通道。SEAM-08 随之消解：生产不走子进程查询协议。 |
| SEAM-02 | 【设】接口住在薄 `contracts` crate（对应 TS contracts.ts 的角色）：trait、最小 DTO、错误类型、fixture builder。依赖方向：backend/middle → contracts；两代理谁改公共契约按 I01–I03 归属。 |
| SEAM-03 | 【设】进程内异步完成语义对齐 TS：motor/look/move 返回可等待 future，完成/取消/失败与 TS Promise+AbortSignal 断言逐条对应；`observationAfter` 保持「时间相邻、不声称因果」。 |
| SEAM-04 | 【设】拆两层：传输级重连策略（enabled/initial/multiplier/max/jitter/stableReset，见差距表配置节）迁入 backend `RunConfig`；产品级重生/停止决策留在 middle（重生保持显式命令，A11 语义）。 |
| SEAM-05 | 【设】逐调用 CancellationToken 对齐 AbortSignal；release 带 generation 防旧 epoch 迟到执行；掉线残句/一次性等断言语义逐条保留。 |
| SEAM-06 | 【设】`source`（commanded/client_predicted/server_observed）保留在内部契约与诊断面；**模型可见 schema 移植期不变**。任何把 provenance 升格为模型可见的改动是产品级变化，移植后另行 G05 登记。 |
| SEAM-07 | 【设】形状变了就升版本：backend 事件信封含必需 `source` 等新形状 → 定名 `mineintent.minecraft.backend-event.v2`，配套兼容/拒绝测试与版本化 fixture。禁止同名不同形。 |
| SEAM-09 | 【维】offline-only；配置项保留 auth 枚举位但只实现 offline，非 offline 值显式报「暂不支持」。 |
| SEAM-10 | 【设】有界通道；lifecycle/chat 永不丢；可由新快照重建的高频事实可丢但必须发 overflow 事件。进程内链接使该项风险大幅缩小，仍须有测试。 |
| SEAM-11 | 【设】等价迁移采 TS 语义：观察句柄绑定创建时 epoch，stale 句柄拒绝读取、订阅按 epoch 过滤。backend 侧修改由 A 承担。 |
| SEAM-12 | 【设】dimension 是事实，由 backend 在事件发生时盖进信封（对齐 TS）；A 修 backend 信封构造。 |
| SEAM-13 | 【设】缺失观测事实的**生产者在 backend**（实体六类增量、块 updated/chunk_unloaded、声音 heard payload——即差距表缺口 1）；**规范化在 adapter**，字段语义以 I01 冻结契约为准，不许 adapter 静默猜测。 |
| SEAM-14 | 【设】采 TS 语义：1–4 个不重复方向逐键按下（三键保留未抵消轴）；backend 补齐组合映射（差距表缺口 10），A 承担。 |
| SEAM-15 | 【设】采 TS 语义：先分配 epoch/attemptId 再发 `connection_requested`，同一连接的生命周期信封不得横跨两个身份（差距表缺口 11），A 承担。 |
| VIEW-01 | 【维/设】方案 1：backend `viewport.rs` 是唯一生产 kernel；middle 只迁 Information wrapper（provider schema/revision/read 协议）。TS `perception.ts` 的逐条几何断言转为对该 kernel 的回归 oracle 测试；TS 的让步/取消/预算语义按 SEAM-05 机制在 kernel 或 wrapper 补齐并写明归属。 |
| VIEW-02 | 【设】backend 提供原子 `{projection, source, revision}` 一次读取，消除两次读取间 epoch/world 变化的竞态。 |
| AGENT-01–04 | 【维】随 Python 移除整体消解：Agent 循环内联为 middle 内 `AgentRunner` trait 的实现；工具「回调」变为进程内调用；提示词外置为模板文件（为 Q01/Q07 铺路）。25 条 Python 用例按归类表迁移——组装规则、循环上限（16 请求/8 工具/32 工具/180s）、幂等拒绝、transcript 格式一比一；HTTP 认证/回调 URL/loopback 绑定类随边界消亡，在日志列名单。tool-call 一次性权威 = runtime 内存级 claim（对齐 TS）。 |
| DELTA-01 | 【维】见 §0.6。 |
| DELTA-02 | 【设】现行单一提示词作第一版 oracle，加显式版本键；Q01/Q07 未决不动组织结构。 |
| DELTA-03 / DELTA-04 | 【维】见 §0.5 / §0.2。 |
| MIG-01 | 【设】除记忆外持久格式原样（journal `event.v1` 追加 JSONL、transcript v1 轮转）；任何迁移动作先备份原文件。 |
| MIG-02 | 【设】保留 `MINEINTENT_*` 变量名；无效或过时值（含旧 1.21.1 目标残留）**显式报错**并给出指引，不许静默忽略。 |
| MIG-03 | 【设】debug `/health`、`/v1/state` 端点与 schema 原样兼容（仍只绑 127.0.0.1、只读、脱敏语义逐条保留）。 |
| MIG-04 | 【维】b 档，见 §0.4。 |

## 4. 里程碑

阶段划分与依赖顺序**以 [移植计划/模块清单.md](./移植计划/模块清单.md) 第 4 节为准**（阶段 0–9 及并行工作带）。在其上叠加：

- **P0（两代理共同的第一个里程碑）**：workspace 重构 + I01/I02/I03 契约冻结提交。§3 的裁定直接落进契约；冻结提交里仍未决的新问题按「blocked 协议」（见 §5）留档，不猜答案。
- **A 的底层补课**（阶段 3 前置）：差距表「底层缺口清单」中归属 backend 的项——观测事件生产者（缺口 1）、结构化 close/failure/stopped/faulted（缺口 2）、分阶段 timeout 与完整重连策略（缺口 3）、快照字段补齐与语义对齐（缺口 4）、聊天 wire 规范化（缺口 5）、可取消命令与完成边界（缺口 6）、epoch 绑定（缺口 7）、命令可用期与同步释放（缺口 8）、dimension 信封（缺口 9）、复合移动（缺口 10）、首次 attempt（缺口 11）。每项修完带对应测试与（需要实服的）证据。
- **记忆支线**：按 §0.6 的 gate 插入，不阻塞其他支线。
- **最终里程碑**：MIG-04 b 档纵向场景（Paper 26.1.2 + middle + AgentRunner 假 provider），三方证据法照旧（服务端控制台真值 / 事件流 / 观察者）。

## 5. 纪律

1. **测试迁移四则**：逐条对应移植、保留断言语义，不许「读懂后按理解重写」；行为规范 vs 习惯的归类以 [移植计划/测试归类表.md](./移植计划/测试归类表.md) 为准，归入「习惯」重做的在进度日志列名单；每模块 = 实现 + 对应测试同一提交；每批模块完成后接受变异验收（删断言/改条件必须被测试网兜住）。
2. **行为对不上时**：以 TS 实现+测试为准，登记差异；「我觉得更合理」不是改行为的理由（G05/G06a 同款纪律）。
3. **blocked 协议**：任何会改变契约、wire、持久格式或行为语义的未决新问题——feature gate / trait stub / 显式 blocked test 留档，登记进度日志，不以临时实现制造既成事实。
4. **提交**：一个里程碑内也按产出分小提交；提交信息一律中文。探针/fixture 代码**入库**（世界存档与运行日志照旧排除）——不重复 M6 探针散落 gitignore 的缺憾。
5. **azalea**：发现上游 bug 直接修 supplies/azalea 检出（`[patch.crates-io]` 指本地路径），一个 bug 一个提交带理由；行为偏好走 feature/插件。与上游差异必须永远可列举。
6. **无网络**：缺料走各自进度日志的「补给申请」，继续做能做的部分。不需要任何 API 密钥。
7. **禁区**：不修改 supplies/ 与 移植计划/ 下任何文件；不提交密钥、私人聊天、世界存档与未脱敏日志；不替产品决定未决问题。

## 6. 停止条件

- 某支线连续两个工作日无可验证进展 → 停该支线，写清卡点与已尝试路径，登记待决。
- 发现本任务书与附件矛盾、或附件与 oracle 事实矛盾 → 以 oracle 实测为准，登记矛盾，不自行改附件。
- 两代理对公共契约意见冲突 → 冻结争议面，双方各自登记论据，等维护者/Claude 裁定。
