# B3 进度日志：生产 view capability 与轮末 viewport sampler 适配层

> 日期：2026-08-02
> 范围：只覆盖 `middle` 的 viewport reader、模型可见 `view` capability 与真实轮末 sampler；不提交、不 push。

## 已完成

- **Viewport reader/adapter 完成**：`crates/middle/src/capability.rs` 的 `ViewportReader` 接收
  `Arc<dyn MinecraftBackendApi>`，每次 full/directed 调用只绑定一次最新
  `ProtocolObservationSource`，再调用一次对应的 atomic read；不经过 Information runtime，
  不复制 backend viewport 几何，也不把 `source`/`revision` 带入模型结果。
- **Agent control bridge 完成**：同文件的 relay 将取消与单调 deadline 转成 backend
  `OperationControl`，取消优先于同时到期的 deadline；调用前已取消/到期不会绑定 source，
  读取中的控制信号会唤醒 backend future，并以有界 settle 等待其结束。backend
  `Cancelled`/`DeadlineExceeded` 映射为对应 `AgentError`，backend 完成边界会再次检查
  Agent control，其他错误变成最多 300 字符的 `ToolFailed` 摘要。
- **`view` capability 完成**：`ViewCapability` 唯一名称为 `view`，资源为
  `ExecutionResource::Viewport`，definition 直接使用冻结的 `view_parameters_schema()`。
  严格反序列化 `ViewArguments`；full 返回 `ViewportProjection`，directed 返回
  `DirectedViewportProjection`，成功/普通失败均保留 `mineintent.tool-result.v1` 外壳；
  执行前、读取完成后都检查 control 与 scope。
- **真实轮末 sampler 完成**：`crates/middle/src/agent/viewport.rs` 的
  `BackendRoundViewportSampler` 复用同一个 `ViewportReader::read_full`，只返回
  `ViewportProjection`。默认 `SystemUtcTimestampSource` 复用现有 `utc_timestamp_now()`，
  同时提供 `FixedUtcTimestampSource`/`UtcTimestampSource` 注入 seam；frame envelope 仍由既有
  `RoundViewportSampler`/driver 负责。
- **测试完成**：`crates/middle/tests/view_capability.rs` 覆盖 definition/schema/resource、
  full/directed 严格参数、invalid 零 source/read、一次 source/一次 atomic read、坐标转交、
  ordinary failure、backend cancellation/deadline、调用前及读取期间 cancellation/deadline、
  读取完成后的 scope 失效、阻塞 read 唤醒、共享 full reader、backend 完成与 Agent 取消
  同一 poll ready 的竞态，以及 projection-only 与固定 UTC。
  所有并发/阻塞等待均使用 1 秒有界 timeout。

## 验证证据

- 定向：`cargo test -p mineintent-middle --test view_capability --locked --offline` → **11 passed**。
- Mutation：临时移除 `await_backend_future` cancellation 分支的 `bridge.trigger_for(&error)`，
  具名测试 `in_flight_cancel_and_deadline_wake_backend_read_with_bounded_completion` 按预期
  失败于 `source.backend_cancelled` 断言；恢复 relay 后原定向套件恢复为 **8 passed**。
- 管理侧独立竞态回归：新增三个具名边界测试，旧实现均稳定见红：
  `reader_control_wins_when_backend_completion_makes_cancellation_ready` 返回 `ToolFailed` 而非
  `RunCancelled`；`reader_control_wins_when_source_binding_fails_during_cancellation` 在 source
  绑定错误处丢失取消优先级；`reader_relays_cancellation_arriving_during_source_read_creation`
  没有把 future 创建期间到达的取消 relay 给已绑定的 backend control。实现现已在 source
  绑定后复查 control、future 创建后晚到控制先 relay，并把 select 顺序固定为 cancellation →
  deadline → backend，backend result 分支也再次复查 control；三个红测全部转绿。
- 组合：`cargo test --workspace --all-targets --locked --offline` → **352 passed, 0 failed**；
  `cargo check --workspace --all-targets --locked --offline` 通过。
- 格式与空白：默认 `cargo fmt --all -- --check`、`cargo +stable fmt --all -- --check`、
  `git diff --check` 均通过。

## 完成边界与 deferred

- 本切片没有实现 `ToolDispatcher`、Participant Runtime、app composition root 或 concrete
  `MinecraftBackendApi` facade；后续装配可直接使用 `mineintent_middle::capability` 的 reader/
  capability，以及 `mineintent_middle::agent::BackendRoundViewportSampler`。
- 没有改 backend kernel、full/directed DTO、serde/schema、frame 触发规则、prompt、
  Information runtime 控制语义，也没有新增模型可见 wire 字段或第二套 viewport contract。
- fake-backed 定向测试只证明进程内 contracts/adapter 行为，不宣称 Paper 到 Agent 的端到端发布。
- 未发现需要改变冻结公开 wire 的问题，因此没有创建 `需要决策-B3.md`。
