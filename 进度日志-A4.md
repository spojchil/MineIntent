# A4 进度日志：Update 07 / NEW-08 `out_of_world`

## 2026-08-03｜设计审计与范围

- 按 Update 07 复核了 Update 06 §4、`需要决策的新问题.md` 的 NEW-08、A3 进度日志以及当前 contracts/backend directed 实现与测试。
- Update 07 裁定 `DirectedWhy` 增补严格 wire 值 `out_of_world`；既有四值顺序不变，新值排在 rank 尾部。生产 directed capability、participant/app 装配仍不在本 A4 范围内。
- 目标自身 `BlockReadResult::OutOfWorld` 必须逐行返回 `why:[out_of_world]`；显式高度 bounds 只用于几何阶段的 O(1) 零读取预判及 `outside_fov`/`too_far` 叠加。邻居、暴露面或射线路径的 `OutOfWorld` 不冒充查询目标；本实现沿用 full kernel 的保守边界处理并跳过该邻居。

## 2026-08-03｜实现

- `DirectedWhy` 现在为五值：`outside_fov`、`too_far`、`occluded`、`chunk_not_loaded`、`out_of_world`。schema 使用 31 个非空规范子集，并保持 rank 稳定顺序；旧四值 wire 仍可反序列化。
- `DirectedUnseenBlock::validate` 与 directed result schema 同步禁止任何含 `out_of_world` 的 unseen 行携带 `by`；`too_far` 仍独占 `distance/max` 条件纪律。新增 DTO/schema 负例覆盖 `[occluded,out_of_world] + by`。
- 新增 `WorldHeightBounds { min_y: i32, height: u32 }`。kernel 使用 `i64` 计算 `min_y + height` 上界；runtime 从同一个持有 world read guard 的 atomic capture 读取 `min_y/height`，直接传递 `chunks.height()`，没有 `as i32` 窄化。
- directed kernel 先按几何与 bounds 分类，越界目标不读目标、不 raycast、不返回 block/by；目标读取自身返回 `OutOfWorld` 时逐行落为 `out_of_world`。mixed batch 不再整批失败；合法边界目标仍按 full predicate/边界纪律处理。
- runtime 保留同一 capture 的 generation/world pointer/source/epoch、revision、取消/deadline、retry/stale 纪律；`read_block_from_world` 也改为 i64 上界比较。

## 2026-08-03｜回归与变异

- kernel directed 具名回归：9/9；覆盖 lower/upper boundary、in-FOV/in-range zero-read、geometry 叠加 `[outside_fov,too_far,out_of_world]`、mixed rows、目标闭包不一致、duplicate 与隐私 wire。
- runtime directed 具名回归：6/6；覆盖当前 world bounds、atomic capture/revision、generation retry、stale epoch、取消、deadline、worker wake operation name。
- contracts capability target：12/12；contracts all-targets：76/76；backend all-targets：58/58；middle viewport provider：9/9。
- 真实变异：临时删除 geometry 阶段追加 `DirectedWhy::OutOfWorld` 的分支，运行 `viewport::tests::directed_out_of_world_geometry_short_circuits_and_mixes_with_other_rows`；按预期失败，实际为 `Backend(InvalidCommand { field: "directed", message: "directed unseen why must not be empty" })`。变异已恢复。
- 全 workspace：`cargo test --workspace --all-targets --locked --offline` 汇总 341 passed、0 failed、0 ignored；`cargo check --workspace --all-targets --locked --offline` 通过；默认与 `+stable` rustfmt check 通过；`git diff --check` 通过。
- 测试环境的当前 worktree 缺少 `.cargo/config.toml` 指向的 `vendor/` 目录；标准无额外参数门禁期间临时建立了指向只读主仓库 vendor 的 ignored junction，门禁后移除，未改主仓库或其他 worktree。

## 2026-08-03｜交接

- NEW-08 对外 `out_of_world` 已在 directed DTO/schema/kernel/runtime 收口；没有新的第三档协议缺口，也未创建 `待决问题-A4.md`。
- 生产 directed capability、participant/app assembly 仍按任务范围 deferred；没有声称其已发布。
- 未 commit、未 push。
