# 中期更新 01（2026-08-02）

> 下发对象：代理 A、B 与新加入的 A2。重启后先读本文件，再继续各自队列。
> 本文件是任务书的增补与澄清，与任务书冲突处以本文件为准。

## 1. 新增授权复用物料：supplies/simple-agent/

维护者亲写的 Rust agent 微型实现（形状借鉴 Rig），已按最终裁定改造并测试通过。**代理 B 在阶段 5 实现 AgentRunner 时直接复用其代码**（typed Tool trait、单点类型擦除、统一结果信封、逐调用 panic 围栏、无副作用可步进 AgentRun 状态机、数组顺序资源裁决）。适配点见其 `_供给说明.md`。

**轮内工具语义澄清**（补充任务书 §3 AGENT 行）：同轮多个工具调用，资源裁决在驱动侧按**数组顺序**完成——先出现的调用预定资源，后到的冲突调用不执行、立即获得写明持有者的 `resource_busy` 结果；**无论执行还是拒绝，每个 `function_call` 都必须有对应的 `function_call_output`，N 进 N 出**（模型 API 硬约束）。移植期 oracle 的「同轮顺序全执行」仍是默认行为；租约化轮内并行属移植后增强，届时 `[move,move]` 从「先后都执行」变为「第一个执行、第二个 busy」是行为变化，须登记后实施。

## 2. 等价迁移的豁免规则（重要澄清）

默认仍是机械迁移。新增一条精确豁免：**凡已有正式裁定要替换的结构，不迁旧结构，直达裁定后的新形态**。「感觉将来会清理」不构成豁免——想跳过某个旧结构，先取得裁定并登记，再跳。

当前豁免名单（每条都有裁定锚点）：

| 旧结构 | 裁定锚点 | 直达形态 |
|---|---|---|
| `memory/` 结构化存储、top-5 检索、`remember` 工具 | 主仓库 Issue #127（含 2026-08-02 修订评论） | **B 直接在 crates/middle 实现单文本记忆**：单文件、组装时读全文直送、追加/锚定唯一替换/整文重写三操作、每次写前 M09 滚动备份、旧 memories.json 一次性迁移带备份、agent-context 升 v4。TS 侧不再动；测试归类表中 memory 相关行为按 #127 规格重写为新规范测试，逐条登记映射与豁免理由 |
| `models/agent-service-client.ts` + `tool-bridge.ts` 的 HTTP 边界 | 任务书 §3 AGENT-01–04 | AgentRunner 内联（按 §1 复用 simple-agent）；只迁行为规范（循环上限/幂等拒绝/组装规则/transcript 格式），HTTP 边界专属测试列名单消亡 |
| `1.21.1` 字面量与旧配置值 | 任务书 §3 SEAM-07 / MIG-02 | 版本化 fixture + 显式报错 |

其余一切照旧机械迁移；拿不准豁免与否的，登记待决，不自行豁免。

## 3. 编队变更：A2 加入（底层补课）

- **任务**：能力面差距表「底层缺口清单」11 条 + 任务书 §3 中归属 backend 的裁定落地（SEAM-03 后端侧完成边界、SEAM-04 完整重连策略、SEAM-11 epoch 绑定、SEAM-12 dimension 信封、SEAM-13 观测生产者、SEAM-14 复合移动、SEAM-15 首次 attempt、VIEW-02 原子读、offline auth 枚举）。每条缺口 = 实现 + 测试 +（适用时）实服证据。
- **所有权**：A2 独占 `crates/backend/**`。契约中 `mineintent.minecraft.*`（I01 面）的修订**改由 A2** 以独立小提交落地并在日志预告；A 转为该面的只读消费方，A 的 contracts 写入集中在 information 面。其余所有权划分不变。
- **工作区**：`MineIntent-backend-rs-A2`（分支 `port/a2-backend-gaps`），`进度日志-A2.md`。实服探针使用 **25570 起的端口段**，避免与 A/B 的探针撞车。
- azalea 补丁政策照旧（任务书 §5.5）：上游 bug 修 supplies/azalea 检出，一 bug 一提交；改动对全部 worktree 可见，动手前在日志预告。

## 4. 杂项

- nightly `rustfmt` 已预装（B 的补给申请闭环）。
- 里程碑合 `main` 维持验收后合入；上次的自行快进既往不咎，此后留给验收侧。
