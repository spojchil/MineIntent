# 中期更新 10（2026-08-03）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-09，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## NEW-09 裁定：A（验收侧按三档制第二档裁定）

**v2 accepted kind 集合 = 九个 oracle 产品事实 kind + `overflow` 唯一基础设施 marker。**现有 strict DTO（`BackendOverflowPayload`）与 v2 fixture 保留。

1. **措辞修订（正式）**：更新-02 §1 NEW-03 与更新-09 §1 中「kind 集合回到 oracle 九个」修订为「产品事实 kind 回到 oracle 九个；`overflow` 为集合中唯一的基础设施 marker」。NEW-03 的本意不变：**管道不得冒充产品事实**（keep_alive/motor/error 仍按原裁定出流）；overflow 显式自证非产品事实，是丢失的诚实标记，不属冒充。
2. **overflow 的语义边界**：仅当 SEAM-10 允许丢弃的「可由快照重建的高频事实」实际被丢弃时发出，坐在丢失发生的流位置；payload 按 contracts 既有冻结（含丢弃计量）。不得用于其他用途；新增任何基础设施 kind 须另行裁定——marker 集合是闭合的（当前恰一个）。
3. **一致性说明**：这是全系统同一条诚实纪律的第三处成员——AgentFrame `omittedEvents`、视口 `truncated`、事件流 `overflow`：**沉默不得伪装成完整**。
4. 安全处理照旧解除：v1→v2 原子切换（NEW-03/更新-09 项 1）现无契约障碍，可按登记排期实施；切换与 SEAM-10 有界通道的启用可分先后，最终集合以本裁定为准。

## 台账

NEW-09 已裁定；后续新增待决从 NEW-10 起。维护者对本裁定保留否决权（第二档），异议走下一期更新翻案。
