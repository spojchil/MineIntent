# 中期更新 04（2026-08-02）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-03，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## NEW-06 裁定：A（单 drainer + FIFO），附实现细化（验收侧按三档制第二档裁定）

死锁链已由验收侧按引证核实（A2 树 `crates/backend/src/runtime.rs:391-401` emit 持 `event_dispatch` 期间执行 `broadcast_event`，`:438-454` callback 在该锁内运行；callback 内调 `stop()` 同线程重入非重入 mutex）。选 **A**，并细化为**排水循环（drain loop）**形态：

1. **短锁段**：`event_dispatch` 只覆盖「事件编号 +（begin_connection_attempt 时的身份分配）+ 入队 FIFO」。交付顺序 = 编号顺序 = 入队顺序，`begin_connection_attempt` 的交错保证（新 attempt 与 `connection_requested` 之间无插入）由短锁段原样保住——这是该锁存在的本来理由（`:457-459` 注释），不得丢失。
2. **排水段**：入队后，若当前无 drainer 则本线程成为 drainer，循环弹出队列逐事件交付（channel send + observation callback），**全程不持任何锁**；若已有 drainer（含本线程自身，即 callback 内嵌套 emit 的情形），只入队即返回，由活跃 drainer 在当前事件之后按序交付。
3. **语义**：顶层 emit 返回时队列已排空（含嵌套产生的事件）——「emit 返回即已交付」对顶层调用保持；嵌套 emit 返回时交付未完成（在外层 callback 返回后、外层 emit 返回前完成）。跨线程并发 emit 的返回时机不作跨线程交付承诺，交付顺序仍由编号唯一确定。用循环不用递归：无递归深度、无栈增长。
4. **不改契约面**：`ObservationEventListener` 契约不新增「callback 内禁止调用 runtime 操作」的禁令（即明确否掉 C 案）——callback 内 emit/stop 合法，语义为「排在当前事件之后交付」，写入契约注释。

## 必须新增的回归（合并本修复的同一批提交内）

- callback 内调用 `stop()`：期限内返回（有界等待，不许无限挂），并精确锁定顺序——原事件 → stop 产生的事件按编号尾随 → 其余 listener 完整收到全部事件。
- 嵌套 emit FIFO：callback 内 emit 的事件排在当前事件之后、同一 drain 内交付。
- `begin_connection_attempt` 交错保证：并发生产者压力下，新 attempt 与 `connection_requested` 之间编号连续、无插入事件。
- 既有测试不得弱化：并发 unsubscribe 线性化、pending reservation 取消、panic 隔离、同 epoch 过滤全部保持绿。

## 台账状态

「需要决策的新问题.md」NEW-06 标记已裁定；后续新增待决从 NEW-07 起。维护者对本裁定保留否决权（三档制第二档），异议走下一期中期更新翻案。
