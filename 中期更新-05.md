# 中期更新 05（2026-08-02）

> 下发对象：代理 A、B、A2 与管理进程。重启后先读本文件与更新 01-04，再继续各自队列。
> 本文件是任务书与前序更新的增补；冲突处以本文件为准。

## 1. 观察系统裁定（维护者 2026-08-02 批，依据主仓库 #94/#96 研究）

以下为**模型可见行为的正式变更裁定**，participant runtime 与相关能力**直达新形态移植**（见 §2 豁免增补），不先移植 oracle 旧形态：

1. **`view` 工具改为模式参数化**：`mode = "full" | "directed"`，schema 留新模式扩展位（未来模式不预设）。跨供应商兼容采用平铺 schema（`mode` 枚举 + 可选 `positions`）+ 运行时校验；oneOf 判别式是否可用按供应商实测定，语义不变。
   - `full`：现有全量视口，**只列正面证据，不枚举缺席，不返回任何不可见语义**。
   - `directed`：输入一组坐标，逐坐标返回当前能否建立视觉事实；不可见时只返回原因（视锥外/超距/遮挡等），**不返回目标位置的方块身份与状态**；说明遮挡物时只能使用视线首先命中的可见表面。批量上限与输出预算实现时定初值、登记后可调。
2. **发送时机改为轮级一帧**：同一轮工具调用中执行过 ≥1 个身体工具（成功失败都算）时，在全部 `function_call_output` 之后追加**一份**全量视口帧；**身体工具结果不再逐个附带视口**。纯 say/view 轮不追加。理由（维护者）：N 进 N 出批量返回下，逐工具附带的前几份视口构造上就是过期的，只会误导。同轮 `[view, move]` 时 view 结果与轮末帧并存，模型按 `at` 时间辨新旧，不做抑制。
3. **开场帧维持不带视口**，模型需要时用 `view` 主动拉取（oracle 现状不变）。
4. **工具描述承担模式引导**：full 的描述写明「存在截断——应该看到但未列出的情况存在；想看的坐标没出现，用 directed 查」；directed 的描述写明逐坐标可见性与原因语义。legend/描述是契约的一部分，与 DTO 同批冻结。
5. **增量模式整线搁置**：不实现、不设前置条件；相关研究材料留档主仓库。
6. **#96 收口**：viewport `informationRevision` 维持现状（每读自增铸新，oracle 语义原样移植）；「方块写入计数器」仅记为将来出现消费方（缓存/跳扫描）时的既定方向，现在不实现。

## 2. 等价迁移豁免名单增补（接更新 01 §2）

| 旧结构 | 裁定锚点 | 直达形态 |
|---|---|---|
| 身体工具结果逐个附带全量视口（TS `move-input.ts`/`look-relative.ts` 的 viewport 字段）| 本更新 §1.2（维护者 2026-08-02） | 轮级一帧：身体工具只返回动作效果；轮末统一追加一份视口帧 |
| `view` 无参数工具（TS `view.ts`）| 本更新 §1.1 | `mode` 参数化的 full/directed 双模式工具 |

受影响的 TS 测试（身体工具附带视口、view 无参数拒绝等）在测试归类表逐条登记映射与豁免理由，按新规范重写为新测试；其余视口行为（预算 256/32m、暴露面判据、实体上限 8、truncated 标志、legend 随数据）仍按 oracle 等价移植。

## 3. 「写法可改」澄清（维护者 2026-08-02）

等价迁移约束的是**行为**，不是代码形状。Rust 侧有更好的写法、结构或惯用法，直接改，无需登记；**可观察行为**的任何变化仍须先有裁定（本更新 §1 即通道示例）。此澄清不改变更新 01 §2 的豁免规则条文。
