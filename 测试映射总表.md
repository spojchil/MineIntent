# 137 行为测试映射总表（gate b 逐条对应审计）

> 生成：2026-08-04，验收侧。来源：名称自动匹配（62 条）+ 车道日志（B6 21 行表等）+ 人工处置。
> 状态：直接对应=同名/同义 Rust 测试；组件覆盖=语义由具名组件测试承担；
> 结构移形=Rust 架构使原断言形态不复存在，等价约束另有承担（附说明）；
> 豁免=正式裁定移除（附锚点）；纵向承担=gate b Paper 纵向脚本的验收标准；
> 待核=覆盖大概率存在但断言未点名——gate b 签字前必须清零（补测试或落正式裁定）。


## src/execution/execution-arbiter.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| leases are per resource, so chat and memory stay free while the body is held | leases_are_per_resource_so_chat_memory_and_viewport_stay_free_while_body_is_held (execution_arbiter.rs) | 直接对应 |
| a refusal is returned rather than thrown so one conflict cannot kill a run | a_refusal_is_returned_rather_than_panicking_so_one_conflict_cannot_kill_a_run (execution_arbiter.rs) | 直接对应 |
| releasing is idempotent and frees the resource exactly once | releasing_is_idempotent_and_frees_the_resource_exactly_once (execution_arbiter.rs) | 直接对应 |
| a stale release after invalidation cannot evict the lease that replaced it | a_stale_release_after_invalidation_cannot_evict_the_replacement_lease (execution_arbiter.rs) | 直接对应 |
| a job hands back a handle immediately and reports its outcome later | a_job_returns_a_shared_handle_immediately_and_reports_its_outcome_later (execution_arbiter.rs) | 直接对应 |
| cancelling a running job aborts its signal | cancelling_a_running_job_updates_shared_state_and_wakes_its_signal (execution_arbiter.rs) | 直接对应 |
| scope loss voids every lease and running job in one step | scope_loss_voids_every_lease_and_running_job_in_one_step (execution_arbiter.rs) | 直接对应 |
| settled jobs are pruned while running ones survive to be reported | settled_jobs_are_pruned_while_running_jobs_survive_in_insertion_order (execution_arbiter.rs) | 直接对应 |

## src/information/context-composer.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| composePassiveObservations flattens every allowed interface into plain values | ts_compose_passive_observations_flattens_four_plain_values (information_context_composer.rs) | 直接对应 |
| composePassiveObservations records an omission instead of throwing when access is denied | information_context_composer.rs 拒绝→omission 组 | 组件覆盖 |

## src/information/contracts/schemas.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| information request schemas are strict and versioned | information_request_schemas_are_strict_and_versioned (information_contracts_schema.rs) | 直接对应 |

## src/information/geometry.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| distanceBetween computes 3D euclidean distance | distance_between_computes_3d_euclidean_distance (information_geometry.rs) | 直接对应 |
| lookDirection matches mineflayer\ | information_geometry.rs 套件（look direction 基向量含 oracle 数值） | 组件覆盖 |
| relativeBearing classifies target position relative to self facing | relative_bearing_classifies_target_position_relative_to_self_facing (information_geometry.rs) | 直接对应 |
| relativeBearing agrees with the rightward axis of lookDirection at every yaw | relative_bearing_agrees_with_the_rightward_axis_of_look_direction_at_every_yaw (information_geometry.rs) | 直接对应 |
| relativeBearing rotates with self yaw | relative_bearing_rotates_with_self_yaw (information_geometry.rs) | 直接对应 |
| relativeBearing defaults to ahead when target is exactly at self position | relative_bearing_defaults_to_ahead_when_target_is_exactly_at_self_position (information_geometry.rs) | 直接对应 |
| bearing labels agree with the right axis of view-relative coordinates | relative_bearing_agrees_with_the_rightward_axis_of_look_direction_at_every_yaw (information_geometry.rs) | 直接对应 |

## src/information/providers/current-status-provider.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| current status provider satisfies the provider contract | ts_current_status_provider_satisfies_the_provider_contract (information_current_status_provider.rs) | 直接对应 |
| current status provider defaults missing oxygen to full and reads experience level | ts_current_status_defaults_missing_oxygen_to_full_and_reads_experience_level (information_current_status_provider.rs) | 直接对应 |
| current status provider bumps revision only when vitals change | ts_current_status_bumps_revision_only_when_vitals_change (information_current_status_provider.rs) | 直接对应 |

## src/information/providers/inventory-provider.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| inventory provider satisfies the provider contract | ts_inventory_provider_satisfies_the_provider_contract (information_inventory_provider.rs) | 直接对应 |
| inventory provider reports current slots and selected hotbar slot | ts_inventory_provider_reports_current_slots_and_selected_hotbar_slot (information_inventory_provider.rs) | 直接对应 |

## src/information/providers/sound-provider.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| sound provider satisfies the provider contract | ts_sound_provider_satisfies_the_provider_contract (information_sound_provider.rs) | 直接对应 |
| sound provider returns an empty list, not unavailable, when nothing was heard | ts_sound_provider_returns_an_empty_list_not_unavailable_when_nothing_was_heard (information_sound_provider.rs) | 直接对应 |

## src/information/providers/viewport-provider.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| viewport provider satisfies its five-field contract | viewport_provider_satisfies_five_field_contract (i02_information_contract.rs) | 直接对应 |
| positions are world-absolute so the same block keeps one key from any stance | projection_uses_absolute_coordinates_and_first_hit（backend/src/viewport.rs） | 组件覆盖 |
| viewport provider selects the exposed-face predicate for level flat ground | exposed-face 为唯一谓词（内核 options 组 + A3 日志） | 组件覆盖 |
| a player named after a mob stays distinguishable from that mob | 实体身份/图例：wire legend 回归；同名区分断言待点名 | 待核 |
| a read that fills the entity cap says so | viewport_provider_preserves_nullable_blocks_legend_players_and_truncated | 组件覆盖 |
| a projection never reuses a revision while its content changes underneath | read_viewport_revision_is_unique_for_concurrent_successes + read_viewport_retries_when_capture_generation_changes | 直接对应 |
| an entities-only read still honors the deadline signal | read_viewport_preflight_cancel_and_deadline_do_not_scan | 组件覆盖 |

## src/information/registry.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| registry is deterministic, sealed and rejects duplicate providers | registry_is_deterministic_sealed_and_rejects_duplicate_providers (information_registry.rs) | 直接对应 |

## src/information/runtime-references.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| runtime signs provider references and resolves them only for declared target providers | ts_runtime_references_bind_target_kind_and_source_revision (information_runtime.rs) | 直接对应 |
| runtime issues and consumes scope-bound continuation cursors | information_cursor_store.rs 套件（#63 光标存量，迁移保留） | 组件覆盖 |

## src/information/runtime.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| runtime filters catalog by audience and serves Catalog → Help → Read | ts_runtime_catalog_help_read_and_trace_omits_values (information_runtime.rs) | 直接对应 |
| effective catalog revisions change with grant-visible fields and purpose is bound | ts_runtime_effective_catalog_revision_binds_visible_fields_and_purpose (information_runtime.rs) | 直接对应 |
| runtime rejects stale schemas, unknown fields and forged scope input | ts_runtime_rejects_stale_schema_unknown_field_and_forged_scope_input (information_runtime.rs) | 直接对应 |
| runtime preserves partial reads without filling unavailable fields | ts_runtime_preserves_partial_reads_without_filling_unavailable_fields (information_runtime.rs) | 直接对应 |
| runtime discards provider leaks and reads racing a scope change | ts_runtime_discards_provider_leaks_and_reads_racing_scope_change (information_runtime.rs) | 直接对应 |
| runtime rebuilds nested fields from parsed Zod data and enforces declared sources | ts_runtime_rebuilds_nested_values_and_enforces_declared_sources (information_runtime.rs) | 直接对应 |
| runtime aborts the provider when its deadline elapses | ts_runtime_aborts_provider_when_its_deadline_elapses (information_runtime.rs) | 直接对应 |
| the reusable provider contract validates a legal provider fixture | ts_provider_contract_fixture_is_legal (information_runtime.rs) | 直接对应 |
| tool sessions enforce read-call and byte budgets before returning results | ts_tool_session_enforces_call_read_and_byte_budgets (information_runtime.rs) | 直接对应 |
| tool session deadline aborts a read already in progress | ts_tool_session_deadline_aborts_an_in_progress_query (information_runtime.rs) | 直接对应 |
| tool session forwards upstream cancellation to a read already in progress | ts_tool_session_forwards_upstream_cancellation_to_query (information_runtime.rs) | 直接对应 |

## src/information/source-ports/perception.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| view-relative position uses [right, up, forward] | information_geometry.rs 基向量套件 + 内核 ViewAxes | 组件覆盖 |
| visibleBlocks includes an exposed, unoccluded block directly ahead | projection_uses_absolute_coordinates_and_first_hit（backend/src/viewport.rs） | 组件覆盖 |
| visibleBlocks excludes a block fully enclosed by occluding neighbors | opaque_wall_blocks_far_blocks_and_entities + exposed-face 谓词回归（backend/src/viewport.rs） | 组件覆盖 |
| visibleBlocks excludes an exposed block behind the camera | 视锥半空间剔除：inside_frustum 生产函数，无同名单测 | 待核 |
| visibleBlocks excludes a target behind an occluding wall | opaque_wall_blocks_far_blocks_and_entities（backend/src/viewport.rs） | 组件覆盖 |
| visibleBlocks sorts by distance and marks the list truncated past the limit | 距离排序+truncated：wire 端 viewport_provider_preserves_nullable_blocks_legend_players_and_truncated；内核排序断言待点名 | 待核 |
| visibleBlocks treats unloaded candidates conservatively and honors cancellation | directed_clear_to_unloaded_target_reports_only_chunk_not_loaded + read_viewport_observes_cancellation_and_deadline_during_projection | 组件覆盖 |
| a dense scan yields to the event loop and stops at the yield where it is aborted | 结构移形：Rust 内核为专属线程原子读，取消经 OperationControl 在读边界检查（read_viewport cancel 组） | 结构移形 |
| a visible non-occluding block does not hide an opaque block behind it | 首命中语义：projection_uses_absolute_coordinates_and_first_hit；透明不遮挡断言待点名 | 待核 |
| the view frustum is rectangular rather than an isotropic cone | 矩形视锥：box_intersects_frustum 生产函数，无同名单测 | 待核 |
| raycastLookedAtBlock sees transparent blocks while standingOnBlock rejects air | standingOnBlock 已按更新-08 移除（半豁免）；lookedAt 透明命中断言待点名 | 待核 |
| the entity view frustum is rectangular, not an isotropic cone | 同上矩形视锥（实体路径） | 待核 |
| visible entities exclude candidates behind the camera | 同视锥半空间（实体路径） | 待核 |
| an entity remains visible when its hitbox intersects the frustum but its center does not | box_intersects_frustum 生产函数覆盖语义，单测待点名 | 待核 |
| the entity cap reports truncation and drops the farthest, never the nearest | 实体上限 8：wire 端 truncated 有回归，最远先弃断言待点名 | 待核 |
| entity scanning yields to the event loop and honors cancellation | 结构移形：同 dense-scan 行（原子读 + control 边界） | 结构移形 |
| the legacy block-centre predicate reports no level flat ground | legacy 谓词已在等价迁移中淘汰（exposed_face 唯一谓词，A3 日志） | 豁免 |
| the exposed-face predicate matches the flat-ground oracle while section culling reduces work | directed_exposed_face_matches_full_when_target_centre_is_blocked + 分段剔除回归 | 组件覆盖 |

## src/information/stores.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| opaque references bind principal, grant, scope, target and full ref content | opaque_references_bind_principal_grant_scope_target_and_full_ref_content (information_ref_store.rs) | 直接对应 |
| screen-bound references require a concrete screen revision | screen_bound_references_require_a_concrete_screen_revision (information_ref_store.rs) | 直接对应 |
| reference limits isolate principals and interfaces and bound per-read payloads | reference_limits_isolate_principals_and_interfaces_and_bound_per_read_payloads (information_ref_store.rs) | 直接对应 |
| cursors bind query shape and are one-time continuations | cursors_bind_query_shape_and_are_one_time_continuations (information_cursor_store.rs) | 直接对应 |
| cursor state and per-principal capacity are bounded | cursor_state_and_per_principal_capacity_are_bounded (information_cursor_store.rs) | 直接对应 |

## src/information/testing/architecture-boundary.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| information module cannot import Mineflayer or raw Minecraft implementation types | crate 边界由 Cargo workspace + 编译器强制（middle 不依赖 backend 实现类型） | 结构移形 |

## src/integration/paper-process-server.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| Paper process manager requires explicit EULA acceptance | gate b 纵向脚本承担（EULA 显式接受、世界模板复制、阶段化记录为脚本验收标准） | 纵向承担 |
| Paper process manager copies a world template without carrying old diagnostics | gate b 纵向脚本承担（EULA 显式接受、世界模板复制、阶段化记录为脚本验收标准） | 纵向承担 |

## src/integration/scenario-runner.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| scenario records setup, participant behavior and cleanup as distinct phases | gate b 纵向脚本承担（EULA 显式接受、世界模板复制、阶段化记录为脚本验收标准） | 纵向承担 |
| timeout still runs cleanup and records a terminal result | gate b 纵向脚本承担（EULA 显式接受、世界模板复制、阶段化记录为脚本验收标准） | 纵向承担 |

## src/minecraft/minecraft-backend.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| strict config rejects unknown fields and unsupported versions | config_rejects_unknown_fields_and_non_target_version (i01_minecraft_contract.rs) | 直接对应 |
| connection_requested is safe before Mineflayer injects the game plugin | connection_requested_uses_preallocated_attempt (i01_minecraft_contract.rs) | 直接对应 |
| AbortSignal before ready performs a deliberate stop and rejects start | cancel_before_ready_deliberately_stops_and_rejects_start (i01_minecraft_contract.rs) | 直接对应 |
| connection timeout seals the attempt and faults when reconnect is disabled | connect_timeout_faults_without_reconnect (i01_minecraft_contract.rs) | 直接对应 |
| connect/login/spawn becomes ready and produces a detached plain snapshot | ready_returns_detached_canonical_snapshot (i01_minecraft_contract.rs) | 直接对应 |
| death and respawn are distinct from dimension transition | death_respawn_and_dimension_change_are_distinct (i01_minecraft_contract.rs) | 直接对应 |
| kicked/error/end is sealed as one close and fatal kicks do not reconnect | kick_error_end_seal_one_fatal_close (i01_minecraft_contract.rs) | 直接对应 |
| structured server shutdown kick is flattened and remains retryable | structured_shutdown_is_flattened_and_retryable (i01_minecraft_contract.rs) | 直接对应 |
| retryable close creates a fresh epoch and stale observation source is rejected | observation_source_binds_epoch_and_returns_structured_stale_errors (observation.rs) | 直接对应 |
| observation source emits plain entity/block/sound DTOs and deduplicates compatibility sound | observation_emits_plain_dtos_and_deduplicates_compat_sound (i01_minecraft_contract.rs) | 直接对应 |
| stop is idempotent, clears owned listeners and never reconnects | stop_is_idempotent_and_disables_reconnect (i01_minecraft_contract.rs) | 直接对应 |
| twenty start/stop cycles leave every Bot listener-free | twenty_cycles_leave_no_owned_resources (i01_minecraft_contract.rs) | 直接对应 |

## src/minecraft/motor-driver.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| relative look converts right/down mouse language to Mineflayer angles | look_relative_preserves_right_down_sign_convention (i01_minecraft_contract.rs) | 直接对应 |
| an already-aborted look never reaches Mineflayer | pre_cancelled_look_never_dispatches (i01_minecraft_contract.rs) | 直接对应 |
| move presses the whole key set before the common hold and releases it in reverse | move_presses_all_keys_then_releases_in_reverse (i01_minecraft_contract.rs) | 直接对应 |
| move interruption releases every pressed control | cancelled_move_releases_every_pressed_key (i01_minecraft_contract.rs) | 直接对应 |
| move rejects encodings that are not non-empty key sets | move_rejects_empty_duplicate_or_invalid_sets (i01_minecraft_contract.rs) | 直接对应 |

## src/models/agent-service-client.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| agent service receives internal run id, safe context and callback credentials | 更新-01：Python/HTTP 模型边界删除，进程内 AgentRunner 承担（agent_runner.rs / agent_run_loop.rs 套件） | 豁免 |
| agent service rejects non-loopback callbacks and surfaces service errors | 更新-01：Python/HTTP 模型边界删除，进程内 AgentRunner 承担（agent_runner.rs / agent_run_loop.rs 套件） | 豁免 |
| agent service response is bounded and error text is truncated | 更新-01：Python/HTTP 模型边界删除，进程内 AgentRunner 承担（agent_runner.rs / agent_run_loop.rs 套件） | 豁免 |
| aborting a decision notifies the service with the exact run id | 更新-01：Python/HTTP 模型边界删除，进程内 AgentRunner 承担（agent_runner.rs / agent_run_loop.rs 套件） | 豁免 |

## src/models/tool-bridge.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| tool bridge is loopback-only, authenticated and forwards strict invocations | 更新-01：Python/HTTP 模型边界删除，进程内 AgentRunner 承担（agent_runner.rs / agent_run_loop.rs 套件） | 豁免 |
| the tool response carries a post-handling observation without claiming causation | observationAfter 移形为 dispatcher 内建：capability_dispatch.rs 的 observation_after 组（B4） | 结构移形 |

## src/participant/capabilities/contracts.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| one registration produces both the advertised contract and dispatch entry | real_concrete_runner_uses_one_registry_and_rebinds_scope_per_wake（同源 registry）+ capability_contracts.rs | 组件覆盖 |
| duplicate capability names fail while the registry is constructed | registry_rejects_duplicate_advertised_names_with_structured_error (capability_contracts.rs) | 直接对应 |

## src/participant/capabilities/move-input.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| move contract exposes one simultaneous key set and rejects duplicate keys | move_rejects_empty_duplicate_or_invalid_sets (i01_minecraft_contract.rs) | 直接对应 |

## src/participant/capabilities/view.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| view declares one full read with an empty argument object and its own scan resource | full_and_directed_bind_one_source_and_call_one_atomic_read (view_capability.rs) | 直接对应 |
| view rejects an already-cancelled signal before starting the scan | read_viewport_preflight_cancel_and_deadline_do_not_scan + view_capability.rs 取消组 | 组件覆盖 |

## src/participant/information-adapters.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| BackendSelfVitalsPort and BackendInventoryPort read straight through the snapshot | backend_ports_read_snapshot_straight_through (i02_information_contract.rs) | 直接对应 |
| BackendPerceptionPort excludes self from nearby entities and maps block loading | perception_excludes_self_and_maps_block_loading (i02_information_contract.rs) | 直接对应 |
| SoundHistory records distance and direction relative to self, bounded and revisioned | sound_history_is_relative_bounded_and_revisioned (i02_information_contract.rs) | 直接对应 |
| BackendInformationScopeSource maps backend state to connection state and includes world info once ready | scope_maps_backend_state_and_ready_world (i02_information_contract.rs) | 直接对应 |

## src/participant/runtime.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| say reports queued rather than completed, and journals the whole correlation chain | speech scheduler/capability 组件（speech_scheduler.rs + say 组）；B6 表#1 标未独立复刻全文 | 组件覆盖 |
| the model sees exactly the contracts registered for dispatch | real_concrete_runner_uses_one_registry_and_rebinds_scope_per_wake（B6 表#2） | 直接对应 |
| view returns visible blocks without moving or turning the body | view_capability.rs（无副作用读）+ B6 表#3 | 组件覆盖 |
| a key set moves diagonally, while opposing keys execute and report no effect | move-input capability 组（capability_dispatch.rs 实测 effect）+ B6 表#4 | 组件覆盖 |
| tool calls are authorized by the active run and each tool-call id is one-shot | tool_call_ids_are_preflighted_atomically_and_unique_for_the_run（B6 表#5） | 直接对应 |
| a held resource fails the call instead of killing the run, and only blocks its own resource | capability 资源协调组（body lease busy）+ B6 表#6 | 组件覆盖 |
| startup is local; player chat runs the two-tool closed loop with measured effects and no memory write | v5 opening 组（agent_context_v5.rs）+ startup_registry_and_addressing_are_explicit_and_symmetric；旧 v3 数值不作 oracle（更新-12，B6 表#7） | 结构移形 |
| addressed chats from Alice and Bob trigger symmetric runtime runs while unaddressed chats do not | startup_registry_and_addressing_are_explicit_and_symmetric（B6 表#8） | 直接对应 |
| a new player chat waits behind an in-flight turn without taking control from the model | second_chat_queues_fifo_without_preempting_first_run（B6 表#9） | 直接对应 |
| damage taken mid-run is available as the observation after the next tool | health baseline 组件 + B6 表#10（部分覆盖：20→13.5 全文未独立复刻） | 组件覆盖 |
| a fact observed in one world never reaches a run in another | scope_change_cancels_active_run_and_drops_old_queue + stale_scope_chat_cannot_drain_current_pending_fact（B6 表#11） | 直接对应 |
| sounds heard before a world change are not replayed with their old distance and bearing | scope 失效清除 + sound adapter（B6 表#12 部分覆盖） | 组件覆盖 |
| ordinary player chats preserve arrival order while the first journal write waits | journal_gate_serializes_later_chat_before_model（B6 表#13） | 直接对应 |
| a newer chat preserves model-authored segments from the earlier turn | ordinary_events_do_not_release_body_or_cancel_speech（B6 表#14） | 直接对应 |
| a connection-epoch scope change synchronously aborts the active run and releases movement | scope_change_cancels_active_run_and_drops_old_queue（B6 表#15） | 直接对应 |
| connection_closed aborts even while the last snapshot still has the old scope | close/reconnect 组回归（B6 表#16） | 组件覆盖 |
| a scope change drops chat that is still waiting for its journal write | journal 返回后复检 generation/scope（B6 表#17） | 直接对应 |
| connection_closed cancels speech segments even when no model run remains active | close 组 speech cancel 计数回归（B6 表#18） | 组件覆盖 |
| release failure cannot wedge the tool gate and sub-epsilon motion is reported without quantization | cleanup_failure_does_not_keep_body_tool_gate_occupied（B6 表#19 部分） | 组件覆盖 |
| stop aborts and releases synchronously before awaiting the decision tail | request_stop_releases_before_bounded_worker_settle_and_speech_cancel（B6 表#20） | 直接对应 |
| queued player turns preserve both model replies after a delayed completion journal | journal FIFO/worker settle seam（B6 表#21 标未独立复刻，app 纵向补证） | 待核 |

## src/speech/speech.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| chat input records sender, addressing evidence, time and world context | chat_input_records_sender_addressing_evidence_time_and_world_context (speech_input_segment.rs) | 直接对应 |
| addressing is symmetric for players under the same multiplayer input conditions | addressing_is_symmetric_for_players_under_the_same_multiplayer_input_conditions (speech_input_segment.rs) | 直接对应 |
| the only online player is addressed by single-party conditions without naming the participant | sole_online_player_is_addressed_by_single_party_conditions_without_naming_participant (speech_input_segment.rs) | 直接对应 |
| a straggler chat from someone other than the sole online player is not single-party addressed | straggler_chat_from_someone_other_than_sole_online_player_is_not_single_party_addressed (speech_input_segment.rs) | 直接对应 |
| stop wording remains ordinary addressed player text | stop_wording_remains_ordinary_addressed_player_text (speech_input_segment.rs) | 直接对应 |
| segmentChat respects Unicode length and keeps ordered content | segment_chat_respects_unicode_code_point_length_and_keeps_ordered_content (speech_input_segment.rs) | 直接对应 |
| scheduler rate limits and preserves segment order | scheduler_rate_limits_and_preserves_segment_order (speech_scheduler.rs) | 直接对应 |
| scheduler stop cancels queued speech before it is sent | scheduler_stop_cancels_queued_speech_before_it_is_sent (speech_scheduler.rs) | 直接对应 |

## src/telemetry/debug-state.test.ts

| TS 测试 | Rust 锚点 | 状态 |
|---|---|---|
| debug state is immutable, bounded and redacts sensitive values | debug_state_is_immutable_bounded_and_redacts_sensitive_values (telemetry.rs) | 直接对应 |
| local debug server only permits read-only GET routes | local_debug_server_only_permits_read_only_get_routes (telemetry.rs) | 直接对应 |

## 计数

- 直接对应：87
- 组件覆盖：24
- 结构移形：5
- 豁免：6
- 纵向承担：4
- 待核：11
- 总计：137（应为 137）