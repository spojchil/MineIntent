# Rust 增量视口实验

分支：`experiment/rust-viewport-incremental`

基线：`origin/archive/rust-port-wip@bc7dae9`

## 目标

这一分支先实现可以独立验证的增量内核，不改已有
`mineintent.viewport-frame.v1/v2`，也不在 Participant Runtime 尚未存在时猜测最终装配。

增量镜像记录的是“已经成功送入当前模型上下文的事实”，不是第二份世界真相。
因此发送顺序必须是：

1. 从 backend 得到一次观察。
2. `ViewportMirror::propose` 生成 keyframe 或 delta，但不改变已提交基线。
3. 先把 pending frame 编码并成功追加到下一次模型请求。
4. 最后调用 `ViewportMirror::commit`。
5. 如果并行 proposal 期间已有另一帧提交，旧 proposal 以 stale 失败。

## 基线有效域

每帧同时携带：

- `contextId`：模型上下文/转录链身份；
- `processSessionId` 与 `connectionEpoch`；
- `worldId` 与 `dimension`；
- 世界绝对坐标系；
- 增量算法版本；
- `baselineId = { epoch, sequence }`；
- delta 的 `baseBaselineId`。

因此，同一世界中的两个并行模型上下文不能只因为都处于
`epoch=0, sequence=1` 而串线。换连接、世界、维度、上下文或算法版本，都必须换
scope；同 scope 内的失效会递增 mirror epoch 并从 keyframe 重建。

接收端 `ViewportIncrementalReducer` 只接受恰好以前一 baseline 为 base、且 sequence
连续递增的 delta。丢帧、乱序、重复帧和 scope 混用都不会修改接收端状态。

## 事实语义

当前第一阶段只增量化有稳定整数体素键的方块：

```text
block:<x>,<y>,<z>
```

一次变化明确分为：

- `added`：模型基线中没有、本次正向观察到；
- `changed`：同一稳定键的值变化，或不确定事实被重新确认；
- `confirmedRemoved`：生产者有权威证据确认删除；
- `unverified`：这次无法确认，保留最后已知事实，绝不当作空气。

现有 `ViewportFullV2` 只有正向可见方块和 `truncated`，没有“已确认消失”集合。
所以 adapter 对缺席只能产生 `not_observed` 或 `output_budget`，不能产生
`confirmedRemoved`。后续要得到可靠删除，必须接 backend 的逐块权威复核或 block
事件，并继续区分视野外、遮挡、太远、区块未加载和真实空气。

实体暂不做 delta。当前 `VisibleEntityView` 没有稳定实体身份；按位置 diff 会把同一实体
移动误报成“一只消失、另一只出现”。

## 截断与修复

keyframe 和 delta 都受输出预算限制。未发出的操作保存在 mirror 的 pending 队列，不会
直接丢弃；后续 proposal 即使没有重复观察证据，也会继续发送 pending 变化。

`complete=false` 或 `omitted>0` 会让接收端标记 `repairRequired=true`。pending 按 FIFO
排队；同一 key 的新证据只更新待发送的目标状态，不把它重新排到队尾，因此持续发现
新坐标也不会饿死旧修复。只有 pending 已清空、当前观察未截断且没有未确认事实，后续
帧才会清除该标记；如果状态本身没有事实变化，mirror 仍会发送一个空的 complete delta
作为 completion acknowledgement。

keyframe 的 `viewport` 是强类型的 `{facts: {...}}`，不会接受无法重放的任意 JSON。
`unverified` 可以和 `facts` 使用同一 key：这表示保留最后已知值，同时标记当前值不确定，
而不是把它当作空气。forced keyframe 也会从已提交事实和 pending 目标构造 checkpoint，
不会因为本次扫描缺少坐标而静默删除事实。

## 与 view 工具数组的关系

当前 Rust `AgentLoopDriver` 仍使用 `dispatch_in_order`，所以现在并不是真正的无序并行
工具批次。这个实验分支因此没有改旧 driver、旧 tool result 或旧 round frame。

在现有顺序语义下，显式 `view` 继续作为普通 tool result 最容易保持调用与结果配对。
如果 Participant Runtime 后续把一个模型响应里的工具真正并行执行，则应在 dispatch 前做
批次级准入：一个批次最多接受一次 viewport scan，重复 `view` 确定性拒绝；实际视口 payload
在全部工具完成后作为唯一 frame 追加，tool result 只返回状态和 frame identity。不能依赖
“第二个 view 失败”，因为无序数组里没有可靠的第一个和第二个。

## 尚未接线

- `ViewCapability` 与 `RoundViewportSampler` 仍输出冻结的 v2 全量 payload；
- `AgentLoopDriver` 仍追加冻结的 `viewport-frame.v2`；
- Participant Runtime 和应用 composition root 在 Rust 主迁移中仍未实现；
- v3 暂时只在 contracts/mirror/reducer 层存在，不是生产模型 wire；
- Paper/backend 全栈验证需要主迁移先补齐可运行入口。

下一批接线应由 Participant Runtime 拥有 mirror，并把“编码成功、请求已追加、最后 commit”
做成一个明确事务边界；不能在采样器内部提前推进 baseline。
