# B7 生产 Participant frame / observation 接缝收口

## 第一批：pending fact 线性化（已通过）

- wake admission 不再把 `pending_facts`、omission count 或 omission types 搬入
  `WakeItem`；worker 真正处理 opening wake 时，才在 capture/memory 之后 drain 当前
  generation + scope。
- opening/body 共用的后续 fact owner 会沿同一 admission 顺序收口。本批先把 opening
  drain 改为先取 `admission_serial`、再取 `state`；producer 的
  `enqueue_work -> record_fact` 仍完整位于该 serial 内，因此 worker 不可能在 record
  间隙先 drain。
- 新增 `ParticipantAdmissionObserver` 仅作为 deterministic runtime-shaped test probe，
  不参与生产 app 装配或模型 wire；测试在 enqueue 已发布、record 尚未完成的 Condvar
  间隙放行 opening，验证 `self_hurt` 只在 opening 看到。测试使用 Tokio 两 worker，避免
  同步 source gate 阻塞 current-thread executor。

证据：

- `cargo +stable test -p mineintent-middle --test participant_runtime fact_recorded_after_wake_admission_is_drained_at_opening_processing_boundary --locked --offline --target-dir target\\b7-narrow`：1 passed。
- 同一 target 的 `participant_runtime` 聚焦套件：28 passed，0 failed。

本批未修改 A7 backend/contracts 改动、supplies/vendor、workspace members 或冻结 wire。

## 第二批：生产 frame source / observationAfter / startup（已通过 middle 回归）

### 线性化与所有权

- `ParticipantFactOwner` 是 opening 与 body observationAfter 的唯一 pending-fact owner，
  上限仍为 20 条，omitted count/type 也在同一 owner 中有界保存。
- producer、scope bind、opening drain、body drain 都在同一 `admission_serial` 顺序上收口；
  owner 的公开 `drain` 先取 serial，再检查 generation + 精确
  `ParticipantScope(process_session_id, connection_epoch, world_id, dimension)`。scope/
  generation 改变先清空旧 owner，因此旧 scope 不能泄漏。
- addressed wake admission 只 retain trigger，不取 pending facts。capture 成功、capture
  失败、stale item、journal failure、enqueue/stop 取消、terminal、handler fault、startup
  rollback 和普通 stop 都有逐项或全量 release 边界。生产 chat pin 的硬上限是 control lane
  8 条加同步 admission 中的 1 条，不依赖“通常小于 8”。
- observation source 只持有 `Weak<ParticipantFactOwner>`；runtime drop 后的 body sample
  返回 `ScopeInvalid`，不会由 dispatcher/source 反向保活 runtime。生产 frame source 的
  backend listener 也是 weak，`dispose()`/Drop 取消订阅并阻断后续交付。

### ProductionParticipantFrameSource

- `ProductionParticipantFrameSource::new`、`with_sound_history`、`dispose` 和
  `is_disposed` 已公开；每次 `capture` 只调用一次
  `MinecraftBackendApi::capture_frame_facts()`，从该 DTO 读取 captured_at、pose、status、
  inventory、armor、light，radians 转 degrees，并严格检查 process/epoch/world/dimension。
- armor 只接受 0..=20（0/None 省略），light 只接受 0..=15（None 保持缺失，仍由
  Participant fail closed）；Player menu 36..=44 映射 hotbar 0..=8、45 映射 offHand，
  selected 保持 0..=8，重复/非法槽、空名、0 count 结构化失败。
- chat listener 保存完整 username/text/at 与稳定内部 sequence，历史为有界 newest window；
  admission trigger 通过 pin 保证普通聊天洪泛不能驱逐它。assembler 最终仍只输出 newest
  8 并按 source omitted + 前置 window 诚实计算 omitted，trigger 即使早于可见窗口也只在
  events 出现一次。重复正文由 source event id、timestamp、sequence 区分，scope 切换清空
  旧历史，dispose 后零交付。
- B5 `SoundHistory` 新增显式 `recent_for_scope(...)` 读取入口；Participant 把同一个
  原子 frame snapshot 的 scope 传入，不再在一次 frame capture 中通过 `recent()` 触发第二次
  backend snapshot，sound 的记录/过滤/几何解释仍完全复用 B5 bundle。

### ObservationAfter 与 startup

- `ParticipantObservationAfterSource` 是 body-only、无 viewport/stable/opening protocol/
  trigger duplication 的直达 `AgentFrameV5` source。control 与 exact world/chat run scope
  在采样前后检查；Body success 与 ordinary-failure result 都走同一 source，一次 capture
  后一次 owner drain，第二次 body observation events 为空但 passive status 可复采；View/
  Chat/Memory 返回 null 且不 capture。
- `start_worker` 在 Created -> Running 时从一次原子 frame-facts 建立初始 scope，幂等拒绝
  重复 start，精确 seed 一个 `participant.started / AI 参与者已进入世界` pending fact，
  不调用模型。startup capture/scope 失败沿既有 failure + rollback，清 scope、关闭队列、
  释放 pin 边界；首个 addressed opening 消费 seed 一次。

## 确认的 deterministic/runtime-shaped 回归

- `participant_runtime`: 33 passed；覆盖 enqueue 已发布到 record_fact 的受控 gap、active run
  先受伤后 queued wake，再由同一 run 的 production observationAfter 先 drain、第二次 body
  为空、后续 opening 不重放 `self_hurt` 的完整串联时序；另覆盖 scope stale、queued wake
  FIFO、重复正文 timestamp/sequence、startup seed/no-model/failure rollback、body-only
  observation、weak owner drop、control queue stop、terminal queued trigger、normal stop
  release。
- `participant_production`: 4 passed；覆盖单 atomic capture/no extra snapshot、rad->deg、
  hotbar/offhand、armor/light 边界、四元 scope reject、chat pin + 9+ flood + omitted、
  duplicate text FIFO identity、scope switch、dispose zero delivery、SoundHistory explicit
  scope path。
- 既有 `capability_dispatch` 的 12 个测试全部通过，其中 body ordinary failure 仍只
  observation 一次，View/Say/Remember 零 observation；middle `--all-targets` 全部通过。

## 可恢复 mutation 证据

- 临时 mutation：将 `ParticipantFactOwner::drain_locked` 的
  `state.facts.drain(..).collect()` 改为 `Vec::new()`。
- 命令：
  `cargo +stable test -p mineintent-middle --test participant_runtime production_observation_after_is_body_only_and_drains_facts_once --locked --offline --target-dir target\\b7-mutation`
  结果：预期失败，具名测试在读取 `self_hurt` event 时失败（`no entry found for key`）。
- 已立即恢复原实现；同一命令再次 `1 passed`。mutation 未保留在工作树。

## 门禁状态与 app 边界

## 父级并发复核：body drain 与满 control lane

- 新增 deterministic multi-thread 回归
  `body_observation_progresses_while_full_control_admission_waits`：第一条 addressed
  wake 在 active model run 中保持未完成，control lane 填满 8 条，下一条
  `spawn_blocking` admission 经现有 queue waiter probe 确认等待容量；production
  `ParticipantObservationAfterSource` 在 2 秒有界窗口内 drain `self_hurt`，blocked
  producer 在 active run 释放前保持未完成，释放后完成。随后 9 条 queued wake 的
  model request 顺序与 journal ticket 均保持 FIFO，stop 收束且 10 个 trigger retention
  全部释放。
- 修复前同一回归曾稳定以 `body observation must finish while the producer waits:
  Elapsed(())` 暴露闭环；证实 producer 持 `admission_serial` 等 queue 容量时 body
  `owner.drain` 无法取得 serial。
- 最小修复位于 middle runtime：`ParticipantEventQueue::enqueue` 在前序 ticket 或
  lane 容量 Condvar 等待前释放 `AdmissionSerialGuard`，唤醒后先释放 queue lock、再
  重取 serial，之后才重新校验 scope/generation 并 commit。queue commit、fact
  record/omission、admission observer 与返回前处理仍在 serial 下；没有引入
  `queue -> admission` 锁序。跨 scope/generation 的旧 wake/fact 在 commit 边界取消
  其 ticket 并返回 `ParticipantAdmission::Ignored`，不会污染新 owner；新增
  `waiting_old_wake_is_ignored_after_scope_generation_changes` 回归覆盖该边界。

### 本轮最终 gates

- 新增窄测：
  `cargo +stable test -p mineintent-middle --test participant_runtime body_observation_progresses_while_full_control_admission_waits --locked --offline --target-dir target\\b7-narrow-body`：1 passed；
  `cargo +stable test -p mineintent-middle --test participant_runtime waiting_old_wake_is_ignored_after_scope_generation_changes --locked --offline --target-dir target\\b7-narrow-scope`：1 passed。
- `cargo +stable test -p mineintent-middle --test participant_runtime --locked --offline --target-dir target\\b7-deadlock`：35 passed，0 failed。
- `cargo +stable test -p mineintent-middle --all-targets --locked --offline --target-dir target\\b7-full-final`：全部通过（含 participant_runtime 35、participant_production 4 及其余 middle targets）。
- `cargo check --workspace --all-targets --locked --offline --target-dir target\\b7-workspace-final`：通过。
- `cargo fmt --all -- --check`、`cargo +stable fmt --all -- --check`、`git diff --check`：全部通过；diff gate 仅有既有 LF/CRLF 工作副本提示，无 whitespace error。
- 本轮未进入 app，未修改 A7/contracts wire，未提交或 push；父级已有的
  `ProductionParticipantFrameSource::capture/chat_context` 先验证 backend scope 再
  bind 修复保持不变。

- app 仍负责 backend ready 后调用 `start_worker`、以当前 runtime scope/generation/trigger
  构造每 wake 的 registry/dispatcher 并注入
  `with_observation_after(ParticipantObservationAfterSource)`、装配真实 model endpoint、
  stop 时等待 runtime 并 dispose production frame source。B7 未创建 app crate、未实现模型
  endpoint，也未声称 Paper/MIG-04 完成。

## 父级回归复核：瞬时 waiter 计数不是协议事实（本轮确定性修复）

### 父级发现的 flaky

- 父级把旧 A7/B7 19 个文件逐字节同步到 `ac07d57` 后执行
  `cargo +stable test -p mineintent-middle --all-targets --locked --offline --target-dir target\integration-middle`，
  结果 34/35，唯一失败：
  `waiting_old_wake_is_ignored_after_scope_generation_changes` /
  `scope change must wait behind the full control lane: Elapsed(())`，
  即测试对 `runtime.wait_for_queue_waiters_for_test(2)` 的要求超时。
- 本轮在改动前复现：同一 target-dir 连续两次全套 `participant_runtime`，
  第一次 34 passed / 1 failed（同一 panic，位于 `wait_for_queue_waiters_for_test(2)` 超时），
  第二次 35/35；单独跑该测试 10 次全部通过。确认是调度竞态而非确定性死锁。

### 根因（验证了父级判断，未盲从）

- scope-change producer 在 `admit_explicit_scope` 内先发布 generation 变更并在
  `perform_cleanup` 中取消/abort active run，之后才进入自己的 `enqueue`。worker 可能
  在 scope producer 注册为第二个 queue waiter 之前就被 generation watch 唤醒、弹出旧
  control 项并唤醒旧 producer。
- 旧 producer 被唤醒后先阻塞在 `admission_serial` 上；scope producer 进入 FIFO wait 时
  计数确实短暂到过 2，但该窗口极短。waiter Notify 的“先注册通知、再检查、未达则等待”
  循环可能错过瞬时 2，而 worker 已弹出足够项，scope producer 首轮唤醒即 Accepted，
  之后不再有第二次 2，测试永久等待到超时。
- 因此“两名 producer 必须在某个时刻同时计入 `waiting_producers`”不是必然协议事实，
  是测试调度假设；生产语义无缺陷：旧 wake 的 `Ignored` 只能发生在 scope/generation
  变更发布之后（`admission_item_is_current` 在 serial 内复查），ticket 链由 worker 弹
  旧项 + Ignored 推进，scope item 最终 Accepted，stop 有界。

### 最小确定性修复

- runtime 仅新增测试 probe：`wait_for_generation_for_test(expected)`（`#[doc(hidden)]`，
  复用现有 generation watch，不触碰任何生产路径）。变更发布发生在 scope producer 仍
  持有 admission serial 时，因此观察到新 generation 就确定性地证明旧 admission 此刻
  必然仍未解决（它需要该 serial 才能复查）。
- 测试删除瞬时的 `wait_for_queue_waiters_for_test(2)` 断言，改为在 spawn scope
  producer 后等待 `generation + 1` 已发布；随后旧 producer 有界返回
  `ParticipantAdmission::Ignored`，scope producer 有界返回 `Recorded`，
  `current_scope()` 精确等于新 scope，stop 有界完成，所有 trigger retention 释放。
- 保留了原有 `wait_for_queue_waiters_for_test(1)` + control lane 满的稳定断言，
  继续证明旧 wake 已确定阻塞在满 control lane。

### Mutation 证据

- 临时把 `wait_for_generation_for_test` 的 `>= expected` 改为 `> expected`
  （破坏新增同步条件）：具名测试在新增断言
  `scope change must be published while the old wake waits for capacity: Elapsed(())`
  失败（0 passed / 1 failed，2.01s）。
- 立即恢复 `>=`，同一命令 1 passed。变异前后均已向
  `.codex-tmp/progress-deepseek-flaky.jsonl` 上报 `temporary_change_active` 状态。

### 重复证据与最终 gates

- 具名测试连续 20 次（`target\integration-middle`，stable）：20 passed，0 failed。
- `participant_runtime` 全套：修复后累计 4 次全过（35 passed，0 failed），
  其中含复现后的对照运行。
- `cargo +stable test -p mineintent-middle --all-targets --locked --offline --target-dir target\integration-middle`：
  全部通过（含 participant_runtime 35、participant_production 4 及全部 middle targets）。
- `cargo check --workspace --all-targets --locked --offline --target-dir target\integration-workspace`
  （默认工具链）：exit 0。
- `cargo fmt --all -- --check`、`cargo +stable fmt --all -- --check`：exit 0。
- `git diff --check`：exit 0（仅有既有 LF/CRLF 工作副本提示，无 whitespace error）。
- 本轮修改仅：`crates/middle/tests/participant_runtime.rs`（测试同步）、
  `crates/middle/src/participant/runtime.rs`（仅测试 probe）、`进度日志-B7.md`、
  `.codex-tmp/progress-deepseek-flaky.jsonl`。未修改 A7/backend/contracts/vendor/supplies、
  休眠 viewport、Cargo manifest/lock；未 commit/push。
